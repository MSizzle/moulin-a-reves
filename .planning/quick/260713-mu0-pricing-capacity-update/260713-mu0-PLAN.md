---
phase: quick-260713-mu0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/i18n/translations.json
  - src/i18n/translations.ts
  - src/pages/index.astro
  - src/pages/the-compound.astro
  - src/pages/groups.astro
  - src/content/pages/compound.md
  - src/pages/contact.astro
autonomous: true
requirements: [MU0-CAP, MU0-PRICING]

must_haves:
  truths:
    - "Site copy states the estate welcomes up to 20 lodging guests (never 10) in EN and FR"
    - "Events copy states up to 50 guests (never 35) in EN and FR"
    - "Contact/availability page shows a 'Pricing & Events' section with the new rates in EN and FR"
    - "npm run build passes"
  artifacts:
    - path: "public/i18n/translations.json"
      provides: "Updated capacity/events copy + new contact.pricing.* keys (EN+FR)"
      contains: "contact.pricing"
    - path: "src/pages/contact.astro"
      provides: "Pricing & Events section between contact grid and AvailabilityCalendar"
      contains: "contact.pricing"
  key_links:
    - from: "src/pages/contact.astro"
      to: "public/i18n/translations.json"
      via: "data-i18n / data-i18n-html attributes"
      pattern: "contact\\.pricing"
---

<objective>
Client copy update from property owner (Patricof): raise lodging capacity from 10 to 20 guests site-wide, raise event capacity from 35 to 50, and add a new "Pricing & Events" section to the contact/availability page with published rates.

Purpose: The owner is repositioning the compound (more guests, steeper price) and wants pricing public to drive direct inquiries.
Output: Updated bilingual copy (EN+FR) across translations + inline defaults + SEO metadata, and a new Pricing & Events section on the contact page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<constraints>
- CRITICAL i18n rule: a translation value containing markup (`<a>`, `<br>`, `<span>`) MUST use `data-i18n-html`; plain text uses `data-i18n`. The inline default HTML in each `.astro` file is the English static default and MUST match the new EN value in `translations.json`.
- `translations.json` is hand-edited here — preserve JSON validity and 2-space formatting. Append new keys near the existing `contact.*` keys (block runs roughly lines 1578-1720).
- FR numbers/currency use French style: "2 500 €", "10 000 €", "5 000 €", "250 €".
- Do NOT change per-house "Sleeps 10 / 6 / 4" numbers — those stay. Only the aggregate estate capacity (10→20) and event capacity (35→50) change.
- Do NOT touch editor/feedback flows, vercel.json, or middleware.
- The €10,000 full-compound rate (> 3×€2,500) is intentional ("steeper price") — do not "correct" it.
</constraints>

<interfaces>
Existing JSON entry shape (EN+FR, markup uses escaped quotes):
```json
"contact.getting.summary": {
  "en": "About 1 hour from Paris... <a href=\"/about/#getting-here\">See the full directions FAQ →</a> for airport transfers...",
  "fr": "Environ 1 heure de Paris... <a href=\"/about/#getting-here\">Voir la FAQ complète sur l'accès →</a> avec transferts aéroport..."
}
```
Existing contact.astro section pattern (line ~152 closes the contact grid `</section>`; AvailabilityCalendar at line ~155). Insert the new `<section class="section">` between them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capacity 10→20 and events 35→50 site-wide (EN+FR)</name>
  <files>public/i18n/translations.json, src/i18n/translations.ts, src/pages/index.astro, src/pages/the-compound.astro, src/pages/groups.astro, src/content/pages/compound.md</files>
  <action>
Update every location where the estate's aggregate lodging capacity reads 10/ten to 20/twenty, and event capacity 35 to 50, keeping EN and FR in sync (MU0-CAP).

In public/i18n/translations.json:
- `home.homes.intro`: rewrite so the old "although ten bedrooms, maximum of ten guests" privacy framing is replaced — the estate now welcomes up to twenty guests across ten bedrooms in three houses. Update EN and FR ("…un maximum de dix hôtes…" → twenty). The privacy/exclusivity tone should remain, but the "although/maximum ten" logic must go since it no longer holds.
- `homes.rentall.heading`: "…for up to 10 guests." → 20, EN+FR.
- `homes.rentall.text`: EN and FR "…jusqu'à 10 personnes…" → 20.
- `about.place.p4`: "…up to ten guests." / FR "…jusqu'à dix personnes…" → twenty / vingt.
- `journal.family.p4`: EN "…up to ten guests." → twenty. FR is empty — leave it empty.
- `home.groups.events`: "Events welcome for up to 35 guests, for an additional fee." / FR "…jusqu'à 35 invités…" → 50 / 50 guests. May optionally mention the €5,000 facility fee since it is now public.

In src/i18n/translations.ts (static fallback — keep in sync): `homes.rentall.text` (~line 121, "up to 10 guests" / "jusqu'à 10 personnes") and `about.place.p4` (~line 606, "up to ten guests").

In src/pages/index.astro: line 33 meta description "up to 10 guests"; line 40 JSON-LD description; lines 86 & 94 FAQ JSON-LD "maximum of 10 lodging guests"; line 309 inline default text for home.homes.intro (must match the new translations.json EN exactly); line 446 inline default "Events welcome for up to 35 guests" (must match new home.groups.events EN).

In src/pages/the-compound.astro: line 87 title "Up to 10 Guests" + its description; line 95 JSON-LD; line 133 FAQ JSON-LD "maximum of 10 lodging guests"; line 198 body "welcomes up to 10 guests".

In src/pages/groups.astro: line 86 meta description "up to 10 lodging guests".

In src/content/pages/compound.md: line 16 summary + line 26 body "welcomes up to 10 guests".

Do NOT alter per-house "Sleeps 10/6/4" values. Already-correct spots (journal/planning-family-reunion-france.astro:93, wellness copy ~line 1463) need no change.
  </action>
  <verify>
    <automated>cd "/Users/Montster/Melissa/Maison Website/moulin-a-reves" && node -e "JSON.parse(require('fs').readFileSync('public/i18n/translations.json','utf8')); console.log('json-ok')" && ! grep -rn "up to 10 guests\|up to ten guests\|maximum of 10 lodging\|up to 10 lodging\|jusqu'à 10 personnes\|jusqu'à dix personnes\|up to 35 guests\|jusqu'à 35 invités" public/i18n/translations.json src/i18n/translations.ts src/pages/index.astro src/pages/the-compound.astro src/pages/groups.astro src/content/pages/compound.md</automated>
  </verify>
  <done>All targeted 10/ten/35 capacity strings updated to 20/twenty/50 in EN+FR; translations.json remains valid JSON; inline .astro defaults match new EN translation values; per-house sleeps numbers untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Pricing & Events" section to contact page (EN+FR)</name>
  <files>src/pages/contact.astro, public/i18n/translations.json</files>
  <action>
Add a "Pricing & Events" section to the contact/availability page (MU0-PRICING). Insert a new `<section class="section">` in src/pages/contact.astro between the close of the contact grid `</section>` (~line 152) and the `<AvailabilityCalendar>` element (~line 155). Follow the existing page style: `<section class="section">` → `<div class="container">` → `<h2>` → paragraphs. Give every string a `data-i18n` attribute (or `data-i18n-html` when it contains markup) with new keys under `contact.pricing.*`, and the inline default HTML must carry the English copy.

Content (source: client notes verbatim — do not simplify or omit any line):
- Heading `contact.pricing.heading` (plain, data-i18n): EN "Pricing & Events" / FR "Tarifs et Événements".
- `contact.pricing.rates` (plain, data-i18n): nightly rate €2,500 per house; to rent all three homes / the entire compound, €10,000 per night. Two-night minimum. A linens and housekeeping fee of €250 per home is additional. FR uses "2 500 €", "10 000 €", "250 €".
- `contact.pricing.events` (markup → data-i18n-html): events with a maximum of 50 people including all lodging guests are welcome for a €5,000 facility fee. You may decorate La Grange. Catering may be arranged directly through our recommended vendors — wrap the vendors phrase in `<a href="/catering/">…</a>`. FR uses "5 000 €" and a French-language catering link label.

Append the three new keys to public/i18n/translations.json near the other `contact.*` keys (the contact block runs roughly lines 1578-1720), each with `en` and `fr`. Preserve 2-space formatting and JSON validity; markup values use escaped quotes as in the existing `contact.getting.summary` entry.

Do not add pricing to the form or calendar logic — this is a static informational section only.
  </action>
  <verify>
    <automated>cd "/Users/Montster/Melissa/Maison Website/moulin-a-reves" && node -e "const t=JSON.parse(require('fs').readFileSync('public/i18n/translations.json','utf8')); ['contact.pricing.heading','contact.pricing.rates','contact.pricing.events'].forEach(k=>{if(!t[k]||!t[k].en||!t[k].fr)throw new Error('missing '+k)}); console.log('keys-ok')" && grep -q "contact.pricing.events" src/pages/contact.astro && grep -q 'data-i18n-html="contact.pricing.events"' src/pages/contact.astro && npm run build</automated>
  </verify>
  <done>Contact page renders a Pricing & Events section with all client-specified rate/event lines in EN+FR; the catering-link string uses data-i18n-html; the three contact.pricing.* keys exist with en+fr; npm run build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (final gate in Task 2).
- No stale "10 guests / ten guests / 35 guests" aggregate-capacity strings remain in the targeted files.
- Pricing & Events section appears on the contact page above the availability calendar, bilingual.
</verification>

<success_criteria>
- Estate capacity reads 20/twenty everywhere (EN+FR); event capacity reads 50.
- Per-house sleeps 10/6/4 unchanged.
- Pricing & Events section present with all six client content points, correctly split across data-i18n / data-i18n-html.
- FR currency/number formatting used.
- Build green.
</success_criteria>

<output>
Create `.planning/quick/260713-mu0-pricing-capacity-update/260713-mu0-SUMMARY.md` when done.
Suggested commits (1-3 atomic): `copy: capacity 20 guests + events for 50 site-wide` and `feat(contact): pricing & events section`. Push to main is handled by the orchestrator afterward, not the executor.
</output>
