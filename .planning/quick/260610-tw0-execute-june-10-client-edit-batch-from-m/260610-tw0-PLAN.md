---
phase: quick-260610-tw0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - public/images/**            # processed webp outputs (Items 0, 16)
  - public/videos/**            # processed mp4 + poster outputs (Items 0, 9, 17)
  - src/components/RoomShowcase.astro
  - src/components/AmenitiesSection.astro
  - src/layouts/BaseLayout.astro
  - src/pages/homes/le-moulin.astro
  - src/pages/homes/hollywood-hideaway.astro
  - src/pages/homes/maison-de-la-riviere.astro
  - src/pages/the-compound.astro
  - src/pages/groups.astro
  - src/pages/catering.astro
  - src/pages/contact.astro
  - src/pages/art.astro          # new
  - public/i18n/translations.json
autonomous: true
requirements: [JUN10-ITEM-0, JUN10-ITEM-1-3, JUN10-ITEM-4, JUN10-ITEM-5, JUN10-ITEM-6, JUN10-ITEM-7A, JUN10-ITEM-7B, JUN10-ITEM-8, JUN10-ITEM-9, JUN10-ITEM-10, JUN10-ITEM-11, JUN10-ITEM-12-14, JUN10-ITEM-15, JUN10-ITEM-16, JUN10-ITEM-17, JUN10-ITEM-18]

must_haves:
  truths:
    - "Raw .mov source files are gitignored and never enter git history; only processed mp4/webp/poster outputs are committed"
    - "Clicking any room/gather/shared-space gallery card opens the existing fullscreen hero lightbox (not the white-box room-modal)"
    - "Gather cards show italic room name only — no description, no 'N photos' badge"
    - "The gather heading reads 'Where you'll gather & dine' on all three house pages (EN + FR)"
    - "Sleep cards show a second line of 3 emoji-prefixed qualities per room"
    - "House pages show a 4-square 'Explore the Estate' section (Gardens, Grange, Spa, Chicken Coop & Wishing Well) replacing 'What's here'; homepage keeps its 6 tiles"
    - "Calendar grey sync-text lines are gone; exactly one arrow on 'Inquire About Dates'"
    - "Groups page has clickable galleries + a data-driven video-testimonial row above the written quotes + a 4th Florence quote"
    - "A standalone /art/ page exists in nav + footer (EN + FR) with DRAFT copy"
    - "Le Moulin hero gallery has the 2 owner photos removed; HH duplicate photos removed; Grange gallery edits applied"
    - "Catering hero uses the processed bon-appetit video (or documented photo fallback)"
    - "Every text change is mirrored into both EN and FR strings (FR machine-translations flagged for review)"
  artifacts:
    - path: .gitignore
      provides: "June 2026 Moulin/ ignore rule"
      contains: "June 2026 Moulin"
    - path: src/pages/art.astro
      provides: "Standalone Art & Antiques page scaffold"
    - path: public/i18n/translations.json
      provides: "EN+FR strings for all new/changed copy"
  key_links:
    - from: "RoomShowcase / gather+sleep cards"
      to: "#lightbox (BaseLayout)"
      via: "data-gallery click handler"
      pattern: "data-gallery"
    - from: "src/pages/groups.astro video row"
      to: "/videos/testimonial-*.mp4"
      via: "video element + poster"
      pattern: "testimonial-.*\\.mp4"
---

<objective>
Execute the entire June 10, 2026 client edit batch defined in the authoritative spec
`./moulin-a-reves-edit-prompts (2).md` (18 numbered items, locked decisions Q1–Q10,
explicit run order). Each item becomes one atomic commit using the project's commit
conventions. The spec's per-item prompts are the binding action spec — they carry the
locked decisions and the defensive "diagnose-first / stop-and-list" guardrails. Do not
paraphrase away those guardrails.

Purpose: Convert the owner's June 10 review notes into shipped site changes for the
2026-05-06… (next-milestone) deliverable, while protecting the fragile editor/feedback
flow and keeping the site bilingual.

Output: ~16 atomic commits (one per item / item-group), processed media assets, a new
/art/ page, and EN+FR translation updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./moulin-a-reves-edit-prompts (2).md
@./CLAUDE.md
@.planning/STATE.md

<!-- The spec doc is THE authoritative source. Read it in full before any task.
     Every <action> below references the doc's per-item prompt by item number;
     execute that prompt verbatim, preserving its guardrails. -->

<interfaces>
<!-- Grounding facts verified against the codebase — executor should NOT re-discover these. -->

TWO modal systems exist in src/layouts/BaseLayout.astro:
- room-modal (white box): markup ~lines 186-204, opened by elements carrying a
  data-room JSON payload (RoomShowcase cards). THIS is the "white box" being replaced.
- lightbox (fullscreen hero): markup ~lines 210-217 (#lightbox, .lightbox__image,
  data-lightbox-prev/next, #lightbox-counter), wired by a global click handler on
  [data-gallery] .card__image (~lines 794-818). THIS is the reuse target for Items 1-3.

src/components/RoomShowcase.astro:
- `export interface Room { title; photos: Photo[]; details?: Detail[]; description?; ... }`
- Renders `.card__photo-count` badge (`{room.photos.length} photos`) — Item 4 removes this.
- Has `hideTileSummary` prop already in use on gather + sleep sections.
- Per-room `description` / `details` text is the source for Item 6 quality lines.

src/components/AmenitiesSection.astro:
- Currently renders the house-page "Explore the Estate" heading but with the OLD
  "What's here" features+amenities structure. Item 7a replaces the 4-tile section;
  the "Also included" list (amenity-list__heading) must be preserved below it.

House pages: src/pages/homes/{le-moulin,hollywood-hideaway,maison-de-la-riviere}.astro
  — each uses <RoomShowcase heading="Where you'll sleep"> then
  <RoomShowcase heading="Where you'll gather"> then <AmenitiesSection> then
  <AvailabilityCalendar>.

src/pages/the-compound.astro: gallery data arrays for "La Grange — Covered Pavilion"
  (11 photos) and "Gardens, Bridge & Stream" — source for Items 12-14, 16.

src/pages/groups.astro: three <split> sections (Yoga/Family/Creative, single image each)
  + .testimonials-grid with q1/q2/q3 (Sophie&Laurent, Anika, David) — Items 8, 9.

i18n: public/i18n/translations.json is the runtime store; keys shaped
  `{ en, fr }`, dot-namespaced. Static fallback in src/i18n/translations.ts.
  data-i18n = textContent swap; data-i18n-html = innerHTML swap (use for any value with
  inline markup). Mirror EVERY copy change into both en+fr; flag machine FR for review.

nav + footer links live in src/layouts/BaseLayout.astro (~lines 87-93, 121-124, 149-157),
  each an <a data-i18n="nav.*"> / data-i18n="footer.*">. Item 10 adds /art/ here.

Verified asset folder at repo root: `June 2026 Moulin/` (NOT assets-inbox/) — the spec
  says any name works; the Item-0 prompt's first action is to gitignore the inbox.
  Contents: testimonials moulin a reves/ (5 .mov, 15-56MB), Grange_*.{jpeg,png},
  bon appetit video.mov (5.8MB), Moulin a reves notes for monty.docx (ignore), .DS_Store.
  ffmpeg/ffprobe at /opt/homebrew/bin; sharp in node_modules.
</interfaces>
</context>

<execution_note>
This is a large quick batch. Tasks are ordered by the spec's run order and are mostly
independent after Task 1 (asset intake) and Task 2 (lightbox). An orchestrator MAY split
execution across multiple sequential executors on the main tree (assets are untracked, no
worktrees). Suggested split points if context climbs past ~35%:
- Group A: Task 1 (assets) + Task 2 (lightbox 1-3) — the foundation.
- Group B: Tasks 3-5 (house-page card/heading/Explore edits — share the 3 house pages).
- Group C: Tasks 6-7 (calendar + gallery cleanups 7b/11/12-14/16).
- Group D: Tasks 8-9 (groups galleries+videos, art page).
- Group E: Tasks 10-11 (catering hero, map formatting).
Tasks 4, 5 and 7a all touch the same three house-page files, so keep them sequential
within one executor (no parallel house-page editing).
</execution_note>

<tasks>

<task type="auto">
  <name>Task 1: Item 0 — Asset intake (gitignore FIRST, transcode, route)</name>
  <files>.gitignore, public/videos/**, public/images/**</files>
  <action>
Execute the Item 0 prompt verbatim from `./moulin-a-reves-edit-prompts (2).md §"Item 0 — Asset intake"`.
The source folder is at repo root as `June 2026 Moulin/` (the spec's `assets-inbox/` — same
intent, the prompt finds it). GUARDRAIL ORDER IS BINDING:
1. FIRST add `June 2026 Moulin/` to .gitignore, then run `git status` / `git log --stat` to
   prove no .mov/.jpeg/.png from that folder is staged or in history. Raw .movs exceed
   GitHub's 100MB-history concern and must NEVER be committed.
2. Inventory every file (type, dimensions/duration, orientation, size) using ffprobe.
3. Transcode with ffmpeg per the prompt: testimonial .mov → H.264 + AAC mp4,
   `-movflags +faststart`, max 1080p, CRF ~23. Catering `bon appetit video.mov` →
   compress harder (target <~10MB, strip audio — plays muted). Generate a representative
   (NOT frame-0) poster .webp per video. Convert the two Grange images to .webp matching the
   existing pipeline sizes (sharp, max 2000px, q85 per scripts/process-photos.mjs).
   Use web-safe kebab-case names exactly as the prompt suggests
   (testimonial-rave-review.mp4, testimonial-friends-birthday.mp4,
   testimonial-guests-luxembourg.mp4, testimonial-family-french.mp4,
   testimonial-husband-english-tag.mp4, catering-bon-appetit.mp4,
   grange-fireplace-flame.webp, grange-screening-netflix.webp).
   Choose a committed destination dir consistent with the repo (videos under public/videos/,
   posters + stills under public/images/...). State the dir in the summary.
4. SIZE CHECK every output. If any testimonial mp4 still exceeds ~50MB, DO NOT commit it —
   flag it and propose external hosting (Vercel Blob / CDN URL, matching the Vercel deploy)
   per the prompt. Do not silently drop it.
5. Print the ROUTE mapping table (processed file → item 9 / 16 / 17 destination).
6. FLAG anything unexpected: the French testimonial (subtitle/caption question — surface,
   don't decide), portrait-vs-landscape orientation problems, audio-level issues.
Do NOT wire anything into pages in this task — wiring happens in Items 9/16/17.
  </action>
  <verify>
    <automated>grep -q "June 2026 Moulin" .gitignore && git status --porcelain "June 2026 Moulin/" | grep -q . && echo "ignored+present OK"; for f in public/videos/testimonial-*.mp4 public/videos/catering-bon-appetit.mp4 public/images/**/grange-fireplace-flame.webp; do test -e $f 2>/dev/null && echo "have $f"; done</automated>
  </verify>
  <done>June 2026 Moulin/ is gitignored and confirmed absent from history; processed mp4s (faststart), poster webps, and 2 Grange webps exist with web-safe names; size-check + route table + flags reported in summary. Raw .mov outputs not committed.</done>
</task>

<task type="auto">
  <name>Task 2: Items 1-3 — Sitewide hero-style lightbox (inventory first)</name>
  <files>src/layouts/BaseLayout.astro, src/components/RoomShowcase.astro</files>
  <action>
Execute the Items 1-3 prompt verbatim from `§"Items 1–3 — Sitewide hero-style lightbox"`.
GUARDRAIL: inventory ALL galleries/modals in the codebase first and list which ones you are
converting so nothing is missed or double-converted — include this list in the summary.
Reuse the EXISTING fullscreen hero lightbox (#lightbox, .lightbox__image, data-lightbox-prev/next,
#lightbox-counter, wired on [data-gallery] in BaseLayout.astro). Do NOT build a new component.
Change every white-box room-modal gallery (RoomShowcase "Where you'll sleep" + "Where you'll
gather" on the 3 house pages, plus any Shared Spaces white-box galleries on /the-compound/) so
clicking a card opens its OWN photos in the fullscreen lightbox: full-bleed photo, ‹ › arrows,
counter, close. Each gallery's lightbox contains only its own photos. Card grids stay exactly
as laid out. Decision Q1: this applies to every gallery sitewide.
DO NOT TOUCH the editor/feedback flow (?edit=1, editor-inject.js, public/editor/, api/site/*,
guardrails.js, feedback-inject.js) — keep changes confined to the public gallery click behavior.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -c "data-gallery" src/components/RoomShowcase.astro src/layouts/BaseLayout.astro 2>/dev/null</automated>
  </verify>
  <done>Inventory of converted galleries listed in summary; every house-page sleep/gather gallery + applicable compound galleries open the fullscreen lightbox (not the white box); build passes; editor/feedback files untouched (git diff confirms).</done>
</task>

<task type="auto">
  <name>Task 3: Item 4 — Gather cards: italic name only, drop badge</name>
  <files>src/components/RoomShowcase.astro, src/pages/homes/le-moulin.astro, src/pages/homes/hollywood-hideaway.astro, src/pages/homes/maison-de-la-riviere.astro</files>
  <action>
Execute the Item 4 prompt verbatim from `§"Item 4 — Gather cards"`. Decision Q2: remove the
"N photos" badge too. In the "Where you'll gather" sections on all three house pages, each card
shows ONLY the room name beneath the photo, styled italic. Remove ALL other text on gather cards:
descriptions, captions, AND the `.card__photo-count` badge (RoomShowcase ~line 78). Keep room
names exactly as written. Lightbox view = photos only, no caption text. DO NOT change the
"Where you'll sleep" cards — those keep description lines and badges (handled separately).
If the badge is controlled by a shared RoomShowcase prop, scope the removal to the gather
instances only (e.g., a prop flag), do not strip it from sleep cards. Mirror any changed
visible copy into FR.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Gather cards on all 3 houses show italic room name only — no description, no photo-count badge; sleep cards unchanged; build passes.</done>
</task>

<task type="auto">
  <name>Task 4: Item 5 + Item 6 — Gather heading rename + sleep card quality lines</name>
  <files>src/pages/homes/le-moulin.astro, src/pages/homes/hollywood-hideaway.astro, src/pages/homes/maison-de-la-riviere.astro, src/components/RoomShowcase.astro, public/i18n/translations.json</files>
  <action>
Two related house-page copy edits — commit as one atomic copy commit (or two sequential commits
if cleaner; prefer one `copy(houses):` commit).
ITEM 5 — execute `§"Item 5 — Rename heading"` verbatim: change heading "Where you'll gather" →
"Where you'll gather & dine" (ampersand exactly) on all three house pages. Update nav anchors,
aria-labels, AND FR-locale strings referencing the old heading. Decision Q3 confirms wording.
ITEM 6 — execute `§"Item 6 — Sleep cards: second line"` verbatim: in "Where you'll sleep" on all
three pages, add a SECOND line under each room card's existing description: 3 short qualities
derived from that room's modal/description text (RoomShowcase `description`/`details`), each with a
relevant emoji, separated by middle dots (e.g. `🛌 King bedroom · 🛁 Ensuite bath · 🪜 Upstairs`).
GUARDRAIL: no staircase emoji exists — 🪜 (ladder) is closest; if it reads wrong, fall back to
plain text with middle dots and SHOW BOTH VERSIONS in the summary for the owner to choose.
Apply to every room (Le Moulin ×5, HH ×3, La Maison ×2), each room's qualities from its own text.
Mirror every new EN string into FR (flag machine FR for review).
  </action>
  <verify>
    <automated>grep -rl "gather & dine" src/pages/homes/ | wc -l; npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>All 3 house pages read "Where you'll gather & dine" (EN+FR, anchors/aria updated); every sleep card has a second emoji-qualities line; emoji fallback question surfaced if 🪜 reads wrong; build passes.</done>
</task>

<task type="auto">
  <name>Task 5: Item 7a — "Explore the Estate" 4-square section on house pages</name>
  <files>src/pages/homes/le-moulin.astro, src/pages/homes/hollywood-hideaway.astro, src/pages/homes/maison-de-la-riviere.astro, src/components/AmenitiesSection.astro, src/pages/the-compound.astro, public/i18n/translations.json</files>
  <action>
Execute the Item 7a prompt verbatim from `§"Item 7a — Explore the Estate on house pages only"`.
Decisions Q4 + Q5 are binding: replace the "What's here" section (currently rendered via
AmenitiesSection with heading "Explore the Estate" but old features structure) on the THREE house
pages with a 4-equal-square section (2x2, stacked on mobile), identical across all three:
1. The Gardens  2. The Grange  3. The Spa (= current Solarium & Jacuzzi content/photos, RETITLED)
4. The Chicken Coop & Wishing Well.
Each square = representative photo + title; clicking opens the fullscreen hero-style lightbox for
that area (reuse Task 2's lightbox). Reuse the photo sets already in the Shared Spaces galleries on
/the-compound/ (La Grange, Gardens/Bridge/Stream, Solarium & Jacuzzi). For Chicken Coop & Wishing
Well, gather existing chicken-coop/wishing-well/garden imagery and leave a clearly-marked TODO array
for more photos. KEEP the "Also included" amenity list on each house page — place it directly below
the new section — and note in the summary where it ended up. DO NOT change the homepage's existing
6-tile "Explore the Estate" section. Mirror all new EN titles into FR.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -rl "Chicken Coop" src/pages/homes/ src/components/ | wc -l</automated>
  </verify>
  <done>All 3 house pages show the 4-square Explore the Estate section (Gardens/Grange/Spa/Chicken Coop&Wishing Well) opening the hero lightbox; "Also included" list preserved below (location noted); homepage 6 tiles untouched; TODO array marked for chicken-coop photos; build passes.</done>
</task>

<task type="auto">
  <name>Task 6: Item 15 — Delete house-page Gardens cards (after Explore exists)</name>
  <files>src/pages/homes/hollywood-hideaway.astro, src/pages/homes/maison-de-la-riviere.astro</files>
  <action>
Execute the Item 15 prompt verbatim from `§"Item 15 — Delete both house-page Gardens cards"`.
RUN AFTER Task 5 (Explore the Estate must exist). Decision Q9: remove the "Gardens" gallery card
from "Where you'll gather" on BOTH homes/hollywood-hideaway and homes/maison-de-la-riviere. DO NOT
touch the "Gardens, Bridge & Stream" gallery on /the-compound/. GUARDRAIL: before removing, DIFF
the photo lists and MOVE any photos unique to these two cards into the new Explore the Estate
"The Gardens" photo set so no images are lost — report the diff in the summary.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Gardens card removed from gather on HH + La Maison; compound gallery untouched; unique photos migrated into Explore→The Gardens (diff reported); build passes.</done>
</task>

<task type="auto">
  <name>Task 7: Item 7b — Calendar cleanup (diagnose-first arrow)</name>
  <files>src/components/AvailabilityCalendar.astro, src/layouts/BaseLayout.astro, src/pages/contact.astro, public/i18n/translations.json</files>
  <action>
Execute the Item 7b prompt verbatim from `§"Item 7b — Calendar cleanup"`. On every page with the
availability calendar (3 house pages + contact): Decision Q6 — delete BOTH grey/italic sync-text
lines ("Live calendar synced from Airbnb and VRBO. Greyed dates are booked." and "Syncing live
from Airbnb + VRBO…"). Keep the calendar, the Available/Booked/Today legend, and the sync
functionality — only the visible text goes (remove EN+FR strings/keys). GUARDRAIL (diagnose-first):
the owner reports a duplicate arrow next to "Inquire About Dates". Render the section and inspect —
it may be a doubled arrow (text "→" plus CSS-injected arrow) or the month-nav "›" sitting visually
adjacent. DESCRIBE what you find in the summary, then remove the duplicate so exactly ONE arrow
remains on "Inquire About Dates →", with the month nav visually separated.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && ! grep -rq "Syncing live from Airbnb" src/ public/i18n/translations.json && echo "sync-text removed"</automated>
  </verify>
  <done>Both grey sync-text lines gone (EN+FR) across 3 houses + contact; legend + sync kept; duplicate arrow diagnosed in summary and resolved to exactly one arrow; build passes.</done>
</task>

<task type="auto">
  <name>Task 8: Items 11, 12-14, 16 — Gallery photo edits (diagnose/stop-and-list)</name>
  <files>src/pages/homes/le-moulin.astro, src/pages/homes/hollywood-hideaway.astro, src/pages/the-compound.astro</files>
  <action>
Three gallery-content edits; commit as one or three `fix(gallery):` commits (executor's choice,
prefer per-item). Each carries a STOP-AND-LIST guardrail — honor it; do not guess.

ITEM 11 — execute `§"Item 11 — Le Moulin hero gallery: delete 2 photos of her"` verbatim: in the
homes/le-moulin hero lightbox gallery, delete exactly two owner photos — (1) pink robe in front of
house (likely le-moulin-welcome-rose.webp) and (2) Marie Antoinette wig BITING a croissant looking
at camera. KEEP the Marie Antoinette photo looking AT the croissant (likely
le-moulin-marie-antoinette-patio.webp). GUARDRAIL: if only ONE Marie-Antoinette photo exists in the
gallery data, STOP and LIST every Marie-Antoinette-related image by filename for the owner to confirm
before deleting. Renumber the counter after deletion.

ITEMS 12-14 — execute `§"Items 12–14 — HH duplicate photos"` verbatim (diagnose-first): on
homes/hollywood-hideaway remove duplicates in two galleries — "Through the Looking Glass" (6 photos,
duplicated sitting-room-with-books shot) and "Gazebo & Patio" (4 photos, duplicate patio-table-facing-
French-doors shots). Compare the image arrays for identical/near-identical files; keep one. GUARDRAIL:
if no exact duplicate exists (two different files of same scene), LIST candidates instead of guessing.
Update any remaining photo-count badges.

ITEM 16 — execute `§"Item 16 — Grange gallery edits"` verbatim: on /the-compound/ "La Grange — Covered
Pavilion" (11 photos), DELETE (1) patio shot facing blue French doors (it's HH, not Grange), (2)
dining-room photo with blue tablecloth + armoire, (3) woman+dog next to antique carriage. REPLACE (4)
flameless fireplace ("bottles, fireplace, tablecloth") → grange-fireplace-flame.webp (from Task 1) —
GUARDRAIL: open both images and visually confirm same scene before swapping; the new file's source name
mentions "cake and champagne", so if it's clearly a DIFFERENT scene, ADD it instead of replacing and
tell the owner. (5) screening-room shot facing black/off TV → grange-screening-netflix.webp. GUARDRAIL:
if any description doesn't match the gallery, SHOW the full photo list before deleting. Update the
"11 photos" badge to the new count.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -c "la-grange" src/pages/the-compound.astro</automated>
  </verify>
  <done>Item 11 two photos removed (or stop-and-list reported); HH duplicates removed (or candidates listed); Grange deletions + replacements applied with visual-confirm note; all counters/badges renumbered; build passes.</done>
</task>

<task type="auto">
  <name>Task 9: Items 8 + 9 — Groups galleries + video-testimonial row + Florence quote</name>
  <files>src/pages/groups.astro, src/layouts/BaseLayout.astro, public/i18n/translations.json</files>
  <action>
Two Groups-page edits; prefer two commits (`feat(groups):` galleries, then `feat(groups):` videos).

ITEM 8 — execute `§"Item 8 — Groups page: 3 lightbox galleries"` verbatim: convert each of the three
single-image sections (Yoga retreats / Family reunions / Creative retreats) into a clickable gallery —
keep one lead image in the current layout; clicking opens the fullscreen hero lightbox paging multiple
items. The lightbox must support VIDEO slides mixed with photos (<video> native controls + poster).
Populate each gallery with relevant EXISTING repo images now, and create clearly-marked TODO arrays per
gallery for incoming files. State the exact directory + naming convention for new photos/videos in the
summary, and handle webp conversion/sizing for stills when they arrive. (NOTE: her extra gallery photos
are NOT in the Drive folder — scaffold with existing images; this is NOT a blocker.)

ITEM 9 — execute `§"Item 9 — Groups testimonials: video row"` verbatim (run after Task 1): in "From past
groups" KEEP the three existing written quotes exactly; add a NEW row ABOVE them of video-testimonial
cards using the Task-1 processed files: testimonial-rave-review.mp4, testimonial-friends-birthday.mp4,
testimonial-guests-luxembourg.mp4, testimonial-family-french.mp4, testimonial-husband-english-tag.mp4.
Each card = generated poster thumbnail + play button; clicking opens the video in the fullscreen
lightbox with native controls. Build the row DATA-DRIVEN (renders however many videos are in the list —
5 now). Order as listed (strongest standalone first; French + husband's "tag" last). For the French
video add a small "(en français)" label and SURFACE in the summary whether to add subtitles — don't
decide. Pause any visible autoplay; click-to-play only. ALSO add a FOURTH written quote from Florence H.
(VRBO) per the prompt's exact text, with a "read the full review" expander (full text ⏳ owner will paste
— scaffold the expander with a TODO/placeholder; this is NOT a blocker). GUARDRAIL: flag in the summary
that Florence's placement is the assistant's guess so the owner can move it. Mirror new EN copy into FR.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -c "testimonial-" src/pages/groups.astro</automated>
  </verify>
  <done>Three Groups galleries open the lightbox with video support + TODO arrays + documented drop dir; data-driven 5-video row sits above unchanged quotes with posters + "(en français)" label + subtitle question surfaced; Florence 4th quote added with expander scaffold + placement flagged; build passes; FR mirrored.</done>
</task>

<task type="auto">
  <name>Task 10: Item 10 — Standalone /art/ page in nav + footer (DRAFT)</name>
  <files>src/pages/art.astro, src/layouts/BaseLayout.astro, src/pages/about.astro, public/i18n/translations.json</files>
  <action>
Execute the Item 10 prompt verbatim from `§"Item 10 — Art: standalone page in nav"`. Decision Q8:
create a new standalone page /art/ ("The Art & Antiques" — working title) and add it to the main
navigation AND footer (EN + FR — add nav.art / footer.art keys to translations.json + the static
fallback). Scaffold in the site's existing style: hero image, intro narrative section, and a
hero-style lightbox gallery grid (reuse Task 2's lightbox). SEED the narrative from the art story
already on /about/ (Barbara Guggenheim, 18th-century country antiques, original Folies Bergère
posters, fabric-upholstered walls, the Louis Vuitton trunk coffee table) WITHOUT removing it from
/about/ — link the two pages. SEED the gallery with existing repo photos showing art/antiques (LV
trunk living room, salon, dining rooms) and leave TODO arrays for the owner's dedicated art photos
and text. Mark ALL copy as DRAFT for the owner's review. (Art photos/text ⏳ from owner — scaffold
only; NOT a blocker.)
  </action>
  <verify>
    <automated>test -f src/pages/art.astro && grep -q "/art/" src/layouts/BaseLayout.astro && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>/art/ page exists, scaffolded in site style with hero + narrative + lightbox grid; linked in nav + footer (EN+FR); narrative seeded from /about/ (about kept + cross-linked); gallery seeded + TODO arrays; all copy marked DRAFT; build passes.</done>
</task>

<task type="auto">
  <name>Task 11: Items 17 + 18 — Catering hero video + map formatting (diagnose-first)</name>
  <files>src/pages/catering.astro, src/pages/contact.astro, public/i18n/translations.json</files>
  <action>
Two unrelated single-page edits; commit separately (`feat(catering):` and `fix(contact):`).

ITEM 17 — execute `§"Item 17 — Catering hero"` verbatim (run after Task 1): replace the catering hero
(catering-group-table.webp — no food visible) with the owner's video processed in Task 1 as
catering-bon-appetit.mp4: muted, autoplaying, looped, playsinline, preserve the existing "Catering"
title overlay, and use the generated poster-frame webp as static fallback (also for reduced-motion /
slow connections). GUARDRAIL: check the video's orientation/aspect first — if portrait or it crops
badly at hero aspect, DO NOT force it; instead use it as a click-to-play feature just below the hero
and swap the hero photo to an existing food-visible image (candidates: catering-friends.webp,
maison-dinner-light.webp, catering-cake.webp, catering-meat.webp, images/catering-table.webp). SHOW
the result either way in the summary.

ITEM 18 — execute `§"Item 18 — Map/directions formatting"` verbatim (diagnose-first): on /contact/ the
owner reports the "Getting Here" formatting above the map is "wonky" (unverified from outside). Render
the page and diagnose: verify the OpenStreetMap iframe renders with no raw URL visible; check
spacing/alignment/heading hierarchy of the "Getting Here" text and the "Méréville, France · One hour
south of Paris" caption. Describe the BEFORE problem in the summary before fixing. Verify the
/about/#getting-here link still works after the change.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "catering-bon-appetit\|catering-friends\|catering-cake\|maison-dinner-light\|catering-meat" src/pages/catering.astro && echo "hero updated"</automated>
  </verify>
  <done>Catering hero is the bon-appetit video (muted/autoplay/loop/playsinline + poster) OR documented photo-fallback with click-to-play feature; "Catering" overlay preserved; contact "Getting Here" formatting diagnosed (before described) and fixed; about#getting-here link verified; build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes after each task.
- `git log --stat` shows NO file from `June 2026 Moulin/` ever committed; .gitignore contains the rule.
- No edits to: public/editor-inject.js, public/editor/**, src/pages/api/site/**, public/feedback-inject.js,
  public/feedback-match-inject.js, public/guardrails.js (git diff --name-only confirms).
- Every new/changed visible string has BOTH en+fr in public/i18n/translations.json; machine FR flagged for review.
- Each item is a single atomic commit using project conventions (feat/fix/copy + scope).
- All ⏳ gaps (Item 8 extra photos, Item 9 Florence full text, Item 10 art photos/text) are scaffolded with
  clearly-marked TODO arrays — they did not block the batch.
- Every "diagnose-first" / "stop-and-list" guardrail (7b, 11, 12-14, 16, 18) was honored and reported in summary.
</verification>

<success_criteria>
All 18 spec items (Items 0, 1-3, 4, 5, 6, 7a, 7b, 8, 9, 10, 11, 12-14, 15, 16, 17, 18) executed per their
verbatim prompts and locked decisions Q1-Q10, shipped as atomic commits, bilingual, with the editor/feedback
flow untouched and no raw .mov in git history.
</success_criteria>

<output>
Create `.planning/quick/260610-tw0-execute-june-10-client-edit-batch-from-m/260610-tw0-SUMMARY.md` when done,
including: the Item-0 asset route/size table, the lightbox-conversion inventory, all diagnose-first findings
(7b arrow, 11 Marie-Antoinette list if triggered, 12-14 duplicate candidates, 16 visual-confirm note, 18
before/after), the emoji-fallback decision for Item 6, the Groups video drop-dir convention, the Florence
placement flag + French-subtitle question, and the catering hero outcome (video vs photo-fallback).
</output>
