---
phase: 07-build-time-edit-catalog-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/locator-signals.mjs
  - tests/edit-catalog/locator-parity.test.mjs
  - package.json
autonomous: true
requirements:
  - CATALOG-03
must_haves:
  truths:
    - "OPS-02 fence: public/feedback-inject.js is FROZEN for v1.3. The new Node-side helper is an intentional duplicate of the canonical browser logic at public/feedback-inject.js:169-238. Do not 'fix' the duplication — `git diff main -- public/feedback-inject.js` MUST return zero lines after this plan."
    - "Maps Success Criterion #3: a shared Node-side helper duplicated from feedback-inject.js:169-238 is consumed by the catalog walker. Helper output is fixture-pinned against hand-computed expected tuples for { i18nKey, i18nAttr, currentText, nearestHeading, domPath, imageRef, galleryAttrRaw, galleryIndex } AND a substring-presence regex against public/feedback-inject.js forces sync — if either implementation drifts, the test fails."
    - "imageRefOf parity holds for `<img src>` and inline `style='background-image: url(...)'`. Stylesheet-based background-image elements (resolved only via getComputedStyle in the browser) are emitted by the walker (07-03) with `requiresManualSelection: true` because the catalog cannot anchor them without layout. The grep pre-flight in Task 0 verifies the codebase currently has ZERO such addressable elements — only the SVG data-URI chevron in AvailabilityCalendar.astro:164, which is not a `/images/*.webp` ref and is filtered out by normImage()."
    - "Parity is mechanically pinned by tests/edit-catalog/locator-parity.test.mjs. The signalCount() body is inline-duplicated from src/pages/api/feedback/validate.ts and a normalized-string parity test pins it byte-for-byte to that source — drift fails the test."
  artifacts:
    - path: src/lib/locator-signals.mjs
      provides: "Node-side closestAttr / i18nOf / nearestHeading / domPath / imageRef / galleryOf / visibleText / signalCount"
      exports:
        - closestAttr
        - i18nOf
        - imageRefOf
        - galleryOf
        - nearestHeading
        - domPathOf
        - visibleText
        - signalCount
    - path: tests/edit-catalog/locator-parity.test.mjs
      provides: "node --test harness pinning the helper to the browser captureLocator shape for a fixture DOM AND pinning signalCount to validate.ts"
  key_links:
    - from: tests/edit-catalog/locator-parity.test.mjs
      to: src/lib/locator-signals.mjs
      via: "ESM import of all named exports + assertion of byte-identical tuple"
      pattern: "import .* from '\\.\\./\\.\\./src/lib/locator-signals\\.mjs'"
    - from: tests/edit-catalog/locator-parity.test.mjs
      to: public/feedback-inject.js
      via: "fixture-only read of the browser closestAttr / i18nOf / domPathOf source as the parity reference"
      pattern: "fs\\.readFileSync.*feedback-inject\\.js"
    - from: tests/edit-catalog/locator-parity.test.mjs
      to: src/pages/api/feedback/validate.ts
      via: "fixture-only read of the validate.ts source to pin signalCount() body via normalized-string comparison"
      pattern: "fs\\.readFileSync.*validate\\.ts"
---

<objective>
Lift the locator-signal extraction logic that drives the v1.1 per-element click flow (public/feedback-inject.js:169-238 — closestAttr, i18nOf, imageRefOf, galleryOf, headingNear, domPathOf, visibleText) into a new pure-ESM Node module, then pin parity with a node --test harness that constructs a fixture DOM and asserts the new helper returns a byte-identical `{ i18nKey, i18nAttr, currentText, nearestHeading, domPath, imageRef }` tuple for the same element that the browser captureLocator() at line 289 would compute.

Purpose: CATALOG-03 is the spine of Phase 7 — the other five requirements all assume the locator signals the catalog emits at build time and the locator signals the click flow captures at runtime are character-for-character identical. Without this helper + parity test, the matcher endpoint in Phase 8 cannot trust catalog IDs to round-trip through the v1.1 batch submit validator (which calls signalCount() against the same fields).

Output: A new `src/lib/locator-signals.mjs` ESM module + a parity test under `tests/edit-catalog/` + `linkedom` added to devDependencies (server-side DOM with the same getAttribute / closest / querySelectorAll API the helper relies on). `public/feedback-inject.js` is NOT modified — the duplication is intentional per the OPS-02 fence.

Pre-flight result (Task 0, documented inline here so the executor does NOT re-derive): grep of src/layouts, src/components, src/pages, src/styles for `background-image` and `backgroundImage` returns exactly ONE match — `src/components/AvailabilityCalendar.astro:164`, which sets a `data:image/svg+xml;utf8,...` chevron on a `<select>` element. That value does NOT match the `/images/` regex inside normImage() and is therefore filtered out at the source level. The codebase has ZERO addressable elements whose primary image source is a CSS or inline-style `background-image` rule pointing at `/images/*.webp`. Conclusion: the Node-side imageRefOf can safely omit the layout/computed-style branch AND additionally parse inline `style="background-image: url(...)"` as a defensive measure for any future hand-edit. Stylesheet-only background-image is treated as a known unreachable path; if a future maintainer adds one, the walker emits the entry with `requiresManualSelection: true` (07-03's contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Source-of-truth files for this plan
@public/feedback-inject.js
@src/pages/api/feedback/validate.ts
@src/lib/feedback-version.ts

<interfaces>
<!-- Canonical browser-side logic that the new Node module must duplicate byte-for-byte. -->
<!-- Lines 169-238 of public/feedback-inject.js — extracted verbatim. -->
<!-- The executor MUST NOT modify feedback-inject.js; this block is the read-only source-of-truth reference. -->

From public/feedback-inject.js:

  function closestAttr(el, attr) {
    var n = el;
    while (n && n.nodeType === 1) {
      if (n.hasAttribute(attr)) return n;
      n = n.parentElement;
    }
    return null;
  }

  function i18nOf(el) {
    var order = ['data-i18n-html', 'data-i18n', 'data-i18n-placeholder'];
    for (var i = 0; i < order.length; i++) {
      var host = closestAttr(el, order[i]);
      if (host) return { key: host.getAttribute(order[i]), attr: order[i] };
    }
    return { key: null, attr: null };
  }

  function normImage(url) {
    if (!url) return null;
    var m = String(url).match(/\/images\/[^"')?#]+/);
    return m ? m[0] : null;
  }

  function imageRefOf(el) {
    if (el.tagName === 'IMG' && el.getAttribute('src')) {
      var r = normImage(el.getAttribute('src'));
      if (r) return r;
    }
    var innerImg = el.querySelector && el.querySelector('img[src]');
    if (innerImg) {
      var ri = normImage(innerImg.getAttribute('src'));
      if (ri) return ri;
    }
    // Node-side imageRefOf ALSO parses inline style="background-image: url('...')"
    // before giving up — see Node port. The browser's getComputedStyle branch is
    // intentionally omitted in Node (no layout). Pre-flight grep (Task 0)
    // confirmed zero addressable elements rely on stylesheet-only bg-image.
    return null;
  }

  function galleryOf(el) {
    var host = closestAttr(el, 'data-gallery') || closestAttr(el, 'data-room');
    if (!host) return { raw: null, index: null };
    var raw = host.getAttribute('data-gallery') || host.getAttribute('data-room');
    var idx = null;
    try {
      var imgs = host.querySelectorAll('img');
      for (var i = 0; i < imgs.length; i++) {
        if (imgs[i] === el || imgs[i].contains(el) || el.contains(imgs[i])) { idx = i; break; }
      }
    } catch (e) {}
    return { raw: raw, index: idx };
  }

  function headingNear(el) {
    var hs = document.querySelectorAll('h1,h2,h3,h4,h5,h6');
    var best = null;
    for (var i = 0; i < hs.length; i++) {
      var pos = el.compareDocumentPosition(hs[i]);
      if (pos & Node.DOCUMENT_POSITION_PRECEDING || pos & Node.DOCUMENT_POSITION_CONTAINS) best = hs[i];
    }
    return best ? best.textContent.replace(/\s+/g, ' ').trim().slice(0, 200) : null;
  }

  function domPathOf(el) {
    var parts = [];
    var n = el;
    while (n && n.nodeType === 1 && n.tagName !== 'BODY' && parts.length < 12) {
      var seg = n.tagName.toLowerCase();
      if (n.id) { seg += '#' + n.id; parts.unshift(seg); break; }
      var cls = (n.className && n.className.baseVal !== undefined ? n.className.baseVal : n.className) || '';
      cls = String(cls).split(/\s+/).filter(function (c) { return c && !/^data-fb/.test(c); }).slice(0, 2).join('.');
      if (cls) seg += '.' + cls;
      var p = n.parentElement;
      if (p) {
        var same = Array.prototype.filter.call(p.children, function (c) { return c.tagName === n.tagName; });
        if (same.length > 1) seg += ':nth-of-type(' + (Array.prototype.indexOf.call(p.children, n) + 1) + ')';
      }
      parts.unshift(seg);
      n = n.parentElement;
    }
    return parts.join(' > ');
  }

  function visibleText(el) {
    if (el.tagName === 'IMG') return (el.getAttribute('alt') || '').trim();
    return (el.textContent || '').replace(/\s+/g, ' ').trim();
  }

From src/pages/api/feedback/validate.ts (lines 54-61 — the canonical signalCount source-of-truth):

  export function signalCount(p: any): number {
    let n = 0;
    if (p.i18nKey && p.i18nAttr) n += 1;
    if (p.imageRef) n += 1;
    if (p.galleryAttrRaw && Number.isInteger(p.galleryIndex) && p.galleryIndex >= 0) n += 1;
    if (String(p.nearbyText || '').trim().length >= MIN_VAGUE_LEN && p.nearestHeading) n += 1;
    return n;
  }

  // MIN_VAGUE_LEN is 25 (validate.ts:19). The Node port inline-duplicates this
  // CONSTANT and the function body, with a parity test pinning both to validate.ts
  // via normalized-string comparison (strip whitespace and comments, then string
  // equality). This is the OPS-02-style fence applied to validate.ts: any future
  // hand-edit to either copy fails the test until the maintainer syncs both.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight grep — confirm no addressable element uses stylesheet-only background-image as its image source</name>
  <files>(no files modified; investigation only — result documented in this plan's `<objective>`)</files>
  <read_first>
    - src/layouts/BaseLayout.astro — global chrome, most CSS lives here or is imported here
    - src/components/PhotoCarousel.astro, src/components/RoomShowcase.astro, src/components/AmenitiesSection.astro, src/components/AvailabilityCalendar.astro
    - src/styles/global.css — the single global stylesheet
    - src/pages/index.astro and the home pages under src/pages/homes/ — hero/gallery patterns likeliest to use CSS bg-image
  </read_first>
  <action>
    Run two greps and record the result inline in this plan body (already done — see `<objective>` pre-flight result). Step 1: `grep -rn "background-image\|backgroundImage" src/layouts src/components src/pages 2>/dev/null` — must return ONLY src/components/AvailabilityCalendar.astro:164 (a data: URI SVG chevron on a `<select>`, NOT a `/images/*.webp` photo). Step 2: `grep -rn "background-image\|backgroundImage" src/styles 2>/dev/null` — must return zero matches. If either grep surfaces a NEW addressable element using a `/images/`-pointing background-image (added between planning and execution), STOP and re-open this plan: the walker (07-03) must then mark such entries `requiresManualSelection: true`, and Task 1's expected tuples must be updated. Verified at plan-time: zero such elements exist. No code changes in this task — the investigation result is the deliverable, anchored in the plan body so the executor does not re-derive it.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -rn "background-image\|backgroundImage" src/layouts src/components src/pages src/styles 2>/dev/null | grep -v "AvailabilityCalendar.astro:164" | wc -l | awk '{ if ($1 == 0) print "ok"; else print "fail: new bg-image usage found" }' | grep -c ok</automated>
  </verify>
  <acceptance_criteria>
    - The grep returns 0 lines after filtering the known AvailabilityCalendar.astro:164 chevron entry
    - The plan's `<objective>` paragraph documenting this pre-flight result is unchanged (matches the verified state)
    - No source files are modified by this task
  </acceptance_criteria>
  <done>Pre-flight confirmed; Task 1 can proceed with the imageRefOf contract documented in `<interfaces>` (no-layout branch + inline style="background-image" parse as defensive).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add linkedom devDependency and write the parity test scaffold (RED)</name>
  <files>package.json, tests/edit-catalog/locator-parity.test.mjs</files>
  <read_first>
    - package.json — confirm engines.node === ">=24.0.0" and that no DOM library is currently a direct dep
    - public/feedback-inject.js lines 169-238 — canonical browser logic; READ-ONLY, MUST NOT be edited
    - src/pages/api/feedback/validate.ts lines 18-61 — signalCount() rule plus MIN_VAGUE_LEN constant
    - .planning/REQUIREMENTS.md — CATALOG-03 verbatim
  </read_first>
  <behavior>
    - Test 1: parity for a `<p data-i18n="home.intro.text">Hello world</p>` inside `<section><h2>Welcome</h2>...</section>` returns { i18nKey: 'home.intro.text', i18nAttr: 'data-i18n', currentText: 'Hello world', nearestHeading: 'Welcome', domPath: 'section > p' (or similar with classes), imageRef: null }
    - Test 2: parity for an `<img src="/images/homes/le-moulin-hero.webp" alt="Le Moulin hero">` returns { i18nKey: null, currentText: 'Le Moulin hero', imageRef: '/images/homes/le-moulin-hero.webp', ... }
    - Test 3: parity for an `<img>` inside `<div data-gallery="le-moulin">...</div>` returns galleryAttrRaw: 'le-moulin' and galleryIndex >= 0
    - Test 4: data-i18n-html beats data-i18n (priority ladder: data-i18n-html > data-i18n > data-i18n-placeholder)
    - Test 5: signalCount() against a populated tuple returns >= 2; against a domPath-only tuple returns 0 (domPath NEVER counts per validate.ts:54)
    - Test 6 (parity-anchor for browser locator code): the test reads public/feedback-inject.js as a string, asserts that regex-matching substrings for closestAttr/i18nOf/domPathOf/headingNear are present and align with the duplicated logic in src/lib/locator-signals.mjs so that any future hand-edit to the browser file fails the test
    - Test 7 (parity-anchor for signalCount): the test reads BOTH public/feedback-inject.js (for browser locator code) AND src/pages/api/feedback/validate.ts as strings; normalizes the signalCount() function body in validate.ts (strip leading/trailing whitespace per line + strip `//` line-comments + collapse multi-space runs) and asserts the equivalently-normalized signalCount() body in src/lib/locator-signals.mjs is byte-equal. Drift in either copy fails the test and forces the maintainer to sync.
    - Test 8 (inline-style bg-image): parity for a `<div style="background-image: url('/images/bridge-garden.webp')">...</div>` returns imageRef: '/images/bridge-garden.webp' from the Node helper — defensive coverage for the future-edit case even though Task 0 confirmed zero current usage
  </behavior>
  <action>
    Add `linkedom` (latest 0.18.x line) to devDependencies in package.json — run via the standard `npm install --save-dev linkedom` flow. Do NOT touch any other dependency. Create tests/edit-catalog/locator-parity.test.mjs using node:test (built-in, no extra runner needed on Node 24). The test imports from `../../src/lib/locator-signals.mjs` (which does not exist yet — file creation is Task 2; RED phase). Use linkedom's parseHTML to build the fixture DOM. The 8 behavior cases above each become a separate `test('...', ...)` block. For Test 6 the test reads `public/feedback-inject.js` via fs.readFileSync and asserts that a regex matching the canonical closestAttr body (substring `'while (n && n.nodeType === 1) {'` plus `'if (n.hasAttribute(attr)) return n;'`) is present — this regex pins the browser file's content as the parity anchor; if a future maintainer rewrites the browser closestAttr, the test fails and forces them to update the duplicate too. For Test 7 the test reads `src/pages/api/feedback/validate.ts` via fs.readFileSync, extracts the signalCount function body via a regex anchored on `export function signalCount(p: any): number {` ... matching the closing brace, normalizes by stripping line-leading/trailing whitespace, stripping `//` comments, and collapsing runs of spaces; does the equivalent extraction + normalization against `src/lib/locator-signals.mjs` (where signalCount is inline-duplicated); asserts `assert.strictEqual(normalizedNode, normalizedTs)`. Add an npm script `"test:catalog": "node --test tests/edit-catalog/"` to package.json scripts. The test MUST be authored so that `npm run test:catalog` fails at this point with a clear "Cannot find module '../../src/lib/locator-signals.mjs'" error (RED phase).
  </action>
  <verify>
    <automated>npm run test:catalog 2>&amp;1 | grep -E "(Cannot find module|MODULE_NOT_FOUND)" | grep -c "locator-signals" — returns 1 (RED: helper does not exist yet)</automated>
  </verify>
  <acceptance_criteria>
    - package.json includes `linkedom` in devDependencies and a `test:catalog` script
    - tests/edit-catalog/locator-parity.test.mjs exists and uses `node:test` + `linkedom`
    - The test file declares >= 8 test() blocks covering the behaviors enumerated above
    - The test file imports from '../../src/lib/locator-signals.mjs' (path-relative, ESM)
    - The test file reads BOTH public/feedback-inject.js AND src/pages/api/feedback/validate.ts as parity anchors
    - `npm run test:catalog` fails with module-not-found pointing at `src/lib/locator-signals.mjs`
    - No `.astro` or `.mjs` source contains curly quotes (smart-quote pitfall) — `grep -rE "['‘’“”]" tests/edit-catalog/ src/lib/locator-signals.mjs 2>/dev/null | grep -v '^#' | grep -cE "[‘’“”]"` returns 0
    - `git diff main -- public/feedback-inject.js | wc -l` returns 0
  </acceptance_criteria>
  <done>Test file exists, fails with the expected RED error, linkedom installed, npm script wired. public/feedback-inject.js untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/lib/locator-signals.mjs as a pure duplicate of the browser logic and make the parity test pass (GREEN)</name>
  <files>src/lib/locator-signals.mjs</files>
  <read_first>
    - public/feedback-inject.js lines 169-238 — canonical source; copy-port to ESM
    - src/pages/api/feedback/validate.ts lines 18-61 — MIN_VAGUE_LEN constant + signalCount() rule to inline-duplicate
    - tests/edit-catalog/locator-parity.test.mjs — the contract the implementation must satisfy
    - package.json — confirm `linkedom` is now installed
  </read_first>
  <behavior>
    All 8 tests in tests/edit-catalog/locator-parity.test.mjs pass with `npm run test:catalog` exit 0.
  </behavior>
  <action>
    Create src/lib/locator-signals.mjs as an ESM module that exports `closestAttr`, `i18nOf`, `imageRefOf`, `galleryOf`, `nearestHeading` (named `nearestHeading`, not `headingNear` — the validator-side field name), `domPathOf`, `visibleText`, and `signalCount`. The function bodies are character-for-character ports of public/feedback-inject.js:169-238 with these constrained adaptations: (a) `var` becomes `const`/`let` where the original lacks reassignment so the file passes a standard ESLint-strict reading even though no linter runs here — keep the same control-flow and short-circuit behavior; (b) the getComputedStyle branch of imageRefOf is omitted (no layout in Node — documented in a comment), BUT before returning null the Node port additionally checks `el.getAttribute('style')` for an inline `background-image: url('...')` pattern via regex `/background-image\s*:\s*url\(\s*['"]?([^'")]+)['"]?\s*\)/i` and runs normImage() against the captured group; this gives parity coverage for inline-style bg-image (Task 0 confirmed only one such site-wide usage and it is filtered out by normImage's /images/ regex) without requiring layout; (c) `document.querySelectorAll('h1,h2,h3,h4,h5,h6')` in nearestHeading takes a document argument instead of relying on a global (signature: `nearestHeading(el, doc)`), and the browser-equivalent caller passes `el.ownerDocument` — the catalog walker MUST pass the doc explicitly.

    For signalCount: Path A (inline-duplicate, recommended per checker fix_hint). Inline-duplicate the signalCount body verbatim from src/pages/api/feedback/validate.ts:54-61 into src/lib/locator-signals.mjs, ALSO inline-duplicate the MIN_VAGUE_LEN = 25 constant. The duplication carries an explicit comment block:

      // ──────────────────────────────────────────────────────────────────────
      // signalCount: inline-duplicated from src/pages/api/feedback/validate.ts
      // (lines 54-61) + MIN_VAGUE_LEN constant from validate.ts:19.
      //
      // Why duplicate instead of import? Node 24 ESM cannot load .ts files
      // without an explicit tsx/ts-node loader, which this repo does not
      // configure. validate.ts is v1.1 production code; widening scope to add
      // a loader OR refactoring validate.ts to import from a shared .mjs is
      // out of scope for OPS-02-fenced v1.3 Phase 7.
      //
      // Drift is mechanically prevented by Test 7 in tests/edit-catalog/
      // locator-parity.test.mjs — a normalized-string parity check between
      // this signalCount body and validate.ts:54-61. Hand-edits to either
      // copy fail the test until the maintainer syncs both.
      // ──────────────────────────────────────────────────────────────────────

    Top-of-file header comment MUST state: "PARITY MIRROR of public/feedback-inject.js:169-238 AND src/pages/api/feedback/validate.ts:54-61 (signalCount). OPS-02 fence: both source files are frozen for v1.3. Any change to closestAttr / i18nOf / domPathOf / nearestHeading / imageRefOf / galleryOf / visibleText / signalCount here MUST be matched by a hand-edit to the corresponding source file AND vice versa, pinned by tests/edit-catalog/locator-parity.test.mjs."

    NEVER use curly quotes anywhere in this file.
  </action>
  <verify>
    <automated>npm run test:catalog</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/locator-signals.mjs exists and exports the 8 named functions listed above
    - `node -e "import('./src/lib/locator-signals.mjs').then(m => console.log(Object.keys(m).sort().join(',')))"` prints `closestAttr,domPathOf,galleryOf,i18nOf,imageRefOf,nearestHeading,signalCount,visibleText` (or a superset)
    - `npm run test:catalog` exits 0 (all 8 tests pass, including the signalCount normalized-string parity check)
    - Top-of-file comment contains the literal string "OPS-02 fence" AND references both "public/feedback-inject.js:169-238" AND "src/pages/api/feedback/validate.ts:54-61"
    - The signalCount block carries the inline-duplication comment block (literal substring "inline-duplicated from src/pages/api/feedback/validate.ts" present)
    - The file does NOT contain `import ... from '../pages/api/feedback/validate.ts'` (Node ESM cannot load .ts; drift is pinned by Test 7 instead)
    - `grep -cE "[‘’“”]" src/lib/locator-signals.mjs` returns 0
    - `git diff main -- public/feedback-inject.js | wc -l` returns 0
  </acceptance_criteria>
  <done>The helper module exists, all 8 parity tests pass, the OPS-02 fence header is present, signalCount is inline-duplicated with a normalized-string parity anchor, the browser file is untouched.</done>
</task>

</tasks>

<verification>
- npm run test:catalog exits 0
- src/lib/locator-signals.mjs exports the 8 functions and carries the OPS-02 fence header
- signalCount is inline-duplicated from validate.ts:54-61, pinned by a normalized-string parity test
- git diff main -- public/feedback-inject.js returns zero lines
- linkedom is in devDependencies
</verification>

<success_criteria>
CATALOG-03 partially satisfied (the helper exists and is parity-tested; consumption by the walker happens in 07-03). The phase's Success Criterion #3 byte-identical-locator-signals claim is now mechanically pinned by a regression test that covers BOTH the locator-extraction code (vs feedback-inject.js) AND the signalCount rule (vs validate.ts). imageRefOf parity is established for `<img src>` and inline-style background-image; stylesheet-only background-image is unreachable in the current codebase per Task 0, and 07-03 will emit such entries with `requiresManualSelection: true` if any are added later.
</success_criteria>

<output>
Create `.planning/phases/07-build-time-edit-catalog-generator/07-01-SUMMARY.md` when done.
</output>
