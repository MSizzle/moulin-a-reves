---
phase: 04-batch-pipeline-implementation
plan: 08
type: execute
wave: 6
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06", "04-07"]
files_modified:
  - scripts/smoke-feedback-v2.mjs
autonomous: true
requirements:
  - OPS-02
user_setup: []
tags:
  - feedback
  - verification
  - merge-gate
  - integration-smoke

must_haves:
  truths:
    - "`git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts` returns EXACTLY zero lines (OPS-02 / D-13 - editor flow byte-for-byte unchanged - non-negotiable merge gate)."
    - "An end-to-end v2 smoke: POSTing a 3-edit batch to /api/feedback/submit (with auth + GitHub mocks) produces ONE issue whose title matches `^[Feedback] batch of 3 edits — `, whose body contains exactly 3 `### Edit i of 3` blocks + exactly one ```json``` fence + the autonomy hint string."
    - "A v1 back-compat smoke: POSTing a v1 single-edit payload still produces the existing single-edit issue with no behavioral change (API-02 / D-16)."
    - "A cap-violation smoke: POSTing a v2 batch with 11 edits returns HTTP 422 with `cap:'edits'`; POSTing with totalBytes > MAX_BATCH_BYTES returns HTTP 422 with `cap:'bytes'`."
    - "A per-edit-validation smoke: POSTing a v2 batch with one invalid edit returns HTTP 422 with `errors:[{index, error}]`."
    - "The generated HTML for any route that imports BaseLayout contains `feedback-inject.js?v=2` (proves Plan 06 took effect through the build)."
    - "CLAUDE.md has the new 'Batch submissions' bullet (Plan 07) and CLAUDE_FEEDBACK.md has the new §8 (Plan 05)."
    - "The full PR diff is coherent: validate.ts NEW; submit.ts MODIFIED (v1 wired + v2 implemented); feedback-inject.js MODIFIED (chip + panel + state machine); feedback-version.ts MODIFIED (1→2); CLAUDE_FEEDBACK.md MODIFIED (+§8); CLAUDE.md MODIFIED (+1 bullet); scripts/smoke-feedback-v2.mjs NEW (Task 2 harness). NO other files."
    - "Plan-level commits inside this single phase-4 PR follow the atomic per-REQ-ID convention from v1.0 (D-18): one REQ-ID (or a small coherent group like ISSUE-01..04) per commit, so PR review can scan REQ-by-REQ and `git log --oneline` traces directly to REQUIREMENTS.md rows. Single-PR scope, atomic-commit granularity — both hold."
  artifacts:
    - path: "scripts/smoke-feedback-v2.mjs"
      provides: "Real Node test harness that imports the submit handler, stubs fetch with GitHub mocks, runs the 5 behavior scenarios, exits non-zero on any failure"
      contains: "scenario"
    - path: ".planning/phases/04-batch-pipeline-implementation/04-08-SUMMARY.md"
      provides: "Merge-gate report documenting OPS-02 diff verification + integration smoke results + the complete file change inventory for the PR"
      contains: "OPS-02"
  key_links:
    - from: "All prior plans (01-07)"
      to: "OPS-02 merge gate"
      via: "git diff verification + integration smoke + build verification"
      pattern: "git diff main"
    - from: "scripts/smoke-feedback-v2.mjs"
      to: "src/pages/api/feedback/submit.ts (dynamic import of POST)"
      via: "import('../src/pages/api/feedback/submit.ts') with stubbed fetch + env"
      pattern: "smoke-feedback-v2"
---

<context_note>
**Planning iteration 1 (revision):** ROADMAP.md and REQUIREMENTS.md were updated during this iteration to add three emergent requirements per D-05:
- **STAGE-06** (client cap enforcement) — addressed by 04-04
- **STAGE-07** (chip 'limit reached' UX) — addressed by 04-04
- **API-06** (server-side cap re-validation) — addressed by 04-03

This plan no longer needs to surface these emergent requirements as TBD updates; they are part of the locked requirements list and traceability table. The smoke matrix below already exercises the behaviors associated with each.
</context_note>

<objective>
Final gate before the PR is considered ready: verify the OPS-02 additive-only diff property (D-13) returns ZERO lines, run end-to-end integration smokes for the v1 back-compat path and the v2 batch path (via a real Node test harness — `scripts/smoke-feedback-v2.mjs`), verify the build picks up the cache-bust, and assemble a PR-ready file change inventory in the summary.

This plan modifies NO production files — only adds `scripts/smoke-feedback-v2.mjs` as a verification harness. Failure here means a prior plan needs revisiting before merge.

Purpose: One-shot pre-merge confidence check that the phase delivers what ROADMAP §"Phase 4" success criteria 1-6 require, that the editor flow is unchanged, and that v1 + v2 paths coexist correctly. Real HTTP-level behavior is exercised by the new smoke script (path (a) per checker B4); the `<automated>` verify block invokes it directly so the gate has teeth.

Output: New `scripts/smoke-feedback-v2.mjs` harness + 04-08-SUMMARY.md containing the verification report + the file change inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@.planning/phases/04-batch-pipeline-implementation/04-01-PLAN.md
@.planning/phases/04-batch-pipeline-implementation/04-02-PLAN.md
@.planning/phases/04-batch-pipeline-implementation/04-03-PLAN.md
@.planning/phases/04-batch-pipeline-implementation/04-04-PLAN.md
@.planning/phases/04-batch-pipeline-implementation/04-05-PLAN.md
@.planning/phases/04-batch-pipeline-implementation/04-06-PLAN.md
@.planning/phases/04-batch-pipeline-implementation/04-07-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: OPS-02 additive-only diff verification — the merge gate</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 4" success criterion #5 (the literal git diff invocation)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-13 (additive-only constraint)
  </read_first>
  <behavior>
    - Test: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns EXACTLY 0.
    - Test: If the count is non-zero, the failing files MUST be enumerated and the task MUST fail with a clear diff dump showing what changed.
  </behavior>
  <action>
    Run the exact OPS-02 merge-gate command from ROADMAP success criterion #5:
    ```
    git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts
    ```
    Capture its output. The output line count MUST be 0.

    If non-zero:
    1. Print the full diff to the task's verify output for inspection.
    2. Identify which prior plan touched the file (it should be NONE - if any plan did, that plan is in violation of D-13 / OPS-02 and the phase cannot merge).
    3. FAIL this task (non-zero exit) so the gate blocks.

    If zero:
    1. Record in 04-08-SUMMARY.md that OPS-02 passed.
    2. Proceed to Task 2.

    DO NOT modify any file in this task. This is a read-only verification.
  </action>
  <verify>
    <automated>diff_lines=$(git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l | tr -d ' '); if [ "$diff_lines" != "0" ]; then echo "FAIL: OPS-02 violated — $diff_lines lines of editor-flow diff:"; git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts; exit 1; fi; echo "OPS-02 PASS: 0 lines of editor-flow diff"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns exactly 0.
    - The verify script above exits 0.
    - 04-08-SUMMARY.md records "OPS-02: PASS (0 lines)" as a top-level finding.
  </acceptance_criteria>
  <done>OPS-02 merge gate has been run and passes (zero lines of editor-flow diff). If failed, this task blocks the phase from completing and the offending plan(s) must be revised.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build smoke harness scripts/smoke-feedback-v2.mjs and run integration smokes (v1 back-compat, v2 happy path, v2 cap violations, v2 per-edit errors, build cache-bust)</name>
  <files>scripts/smoke-feedback-v2.mjs</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 4" success criteria 1-6 (the contracts being smoked)
    - All prior plans' acceptance criteria (the spec these smokes execute against)
    - src/pages/api/feedback/submit.ts (post-Plan-03 - the implementation being verified, including the POST APIRoute export and the dispatch to handleV1 / handleV2Batch)
    - src/pages/api/feedback/validate.ts (post-Plan-01 - shared helpers; smoke imports indirectly via submit)
    - public/feedback-inject.js (post-Plan-04 - the client being verified)
    - src/lib/feedback-version.ts (post-Plan-06 - the cache-bust)
    - src/lib/auth.ts (to understand the cookie shape the smoke must construct to pass checkAuth)
    - scripts/check-public-clean.mjs (sibling script — convention for repo-root Node `.mjs` scripts: top-level await, ESM imports from `src/`, exits non-zero on failure)
  </read_first>
  <behavior>
    - Test: `node --check public/feedback-inject.js` exits 0.
    - Test: `npx astro check` reports no new errors.
    - Test: `scripts/smoke-feedback-v2.mjs` exists and is executable; running `node scripts/smoke-feedback-v2.mjs` exits 0 when all 5 HTTP scenarios pass.
    - Test: Scenario 1 (v1 back-compat) — a constructed v1 payload (`{schemaVersion:1, intent:'change-wording', pageRoute:'/', confirmationAccepted:true, intentDetail:{newTextEn:'X', okToTranslate:true}, ...locator}`) sent through the imported POST handler with a valid mocked auth cookie + mocked GitHub fetch returns HTTP 200 + a body matching the pre-refactor v1 shape (ok:true + issueNumber populated).
    - Test: Scenario 2 (v2 happy path) — a constructed v2 payload with 3 valid edits returns HTTP 200 with `{ok:true, issueNumber:N, issueUrl:'https://...'}`; the captured GitHub mock call log shows exactly 1 createIssue call with title matching `^[Feedback] batch of 3 edits — ` and labels `['client-feedback']`.
    - Test: Scenario 3 (v2 batch-cap violation) — a v2 payload with 11 edits returns HTTP 422 with body containing `"cap":"edits"`, `"limit":10`, `"actual":11`.
    - Test: Scenario 4 (v2 photo-cap violation) — a v2 payload with edits whose summed image.bytes exceeds MAX_BATCH_BYTES returns HTTP 422 with body containing `"cap":"bytes"` and the right `limit` and `actual` values.
    - Test: Scenario 5 (v2 per-edit error) — a v2 payload with 2 edits where edit #0 has no newTextEn returns HTTP 422 with body shape `{ok:false, error:'One or more edits failed validation', errors:[{index:0, error:'New wording (English) is required'}]}`.
    - Test: `npm run build` completes; rendered HTML for the homepage (or any BaseLayout-importing page) contains the string `feedback-inject.js?v=2` (proves Plan 06's cache-bust survived the build).
    - Test: All documentation artifacts are present: CLAUDE.md contains "Batch submissions (v2 schema)" bullet; .github/CLAUDE_FEEDBACK.md contains `## 8. Batch submissions`.
  </behavior>
  <action>
    Create `scripts/smoke-feedback-v2.mjs` (a new file at the repo root following the convention of other `.mjs` scripts under `scripts/`). The file's responsibilities:

    1. **Header / preamble:**
       - Single-line shebang `#!/usr/bin/env node` (optional but matches sibling scripts).
       - Top-level doc comment explaining: "Phase 4 merge-gate smoke. Imports POST from src/pages/api/feedback/submit.ts (dynamic import), stubs globalThis.fetch with GitHub API mocks, runs 5 scenarios, exits 0 if all pass, non-zero (with diagnostic) on first failure. Invoked by Plan 04-08 Task 2 verify."

    2. **Environment setup:**
       - Set required env vars so the handler does not 500 on missing config: `process.env.GITHUB_TOKEN ||= 'smoke-token'`, `process.env.GITHUB_REPO ||= 'MSizzle/moulin-a-reves'`, `process.env.DASHBOARD_PASSWORD ||= 'smoke-password'`.
       - Import `createSession` from `src/lib/auth.ts` (dynamic import) so the smoke can mint a valid HMAC-signed cookie value for the `maison_session` cookie.

    3. **Fetch stub framework:**
       - Save `const realFetch = globalThis.fetch;`.
       - Replace `globalThis.fetch` with a function that:
         a. Records every call as `{url, method, body}` into a `fetchLog: Array<{url, method, body}>`.
         b. For URLs matching `/repos/.+/contents/.+` returns a 404 (file-not-exists path) by default.
         c. For URLs matching `/repos/.+/issues` (POST) returns a 201 with `{number: <next-issue-N>, html_url: 'https://github.com/MSizzle/moulin-a-reves/issues/<N>'}`.
         d. For URLs matching `/repos/.+/issues/\\d+` (PATCH) returns 200 with `{number: N}`.
         e. For URLs matching `/repos/.+/contents/.+` (PUT — photo commit) returns 201 with `{content:{sha:'mock-sha'}}`.
       - Provide a `resetFetchLog()` helper between scenarios.

    4. **POST handler import:**
       - `const { POST } = await import('../src/pages/api/feedback/submit.ts');`
       - If the dynamic import fails (TypeScript not resolvable at runtime), fall back to running through `npx tsx`: detect via try/catch and `console.error('Use: npx tsx scripts/smoke-feedback-v2.mjs')` then exit 2. Document this in the script's preamble.

    5. **Request constructor helper:**
       - `function makeRequest(body, opts = {}) { const cookie = 'maison_session=' + createSession('smoke'); return new Request('http://localhost/api/feedback/submit', { method: 'POST', headers: { 'Content-Type': 'application/json', cookie }, body: JSON.stringify(body) }); }`
       - Use the global `Request` (available in Node 24).

    6. **Scenario runner:**
       - `async function run(name, fn) { try { resetFetchLog(); await fn(); console.log('PASS:', name); passed++; } catch (err) { console.error('FAIL:', name, '-', err.message); failed++; } }`
       - Counters `let passed = 0, failed = 0;` at the top; final `process.exit(failed > 0 ? 1 : 0)` after all scenarios.

    7. **Five scenarios** (use the request constructor + POST handler):

       a. **Scenario 1 — v1 back-compat:**
          ```
          await run('v1 back-compat (change-wording)', async () => {
            const req = makeRequest({
              schemaVersion: 1,
              intent: 'change-wording',
              pageRoute: '/',
              confirmationAccepted: true,
              intentDetail: { newTextEn: 'Hello world', okToTranslate: true },
              i18nKey: 'home.hero.title',
              i18nAttr: 'data-i18n',
              nearbyText: 'Welcome to the homepage',
              nearestHeading: 'Welcome',
            });
            const res = await POST({ request: req });
            if (res.status !== 200) throw new Error('expected 200, got ' + res.status);
            const body = await res.json();
            if (!body.ok || typeof body.issueNumber !== 'number') throw new Error('bad v1 response: ' + JSON.stringify(body));
            const createCalls = fetchLog.filter(c => /\\/issues$/.test(c.url) && c.method === 'POST');
            if (createCalls.length !== 1) throw new Error('expected 1 createIssue call, got ' + createCalls.length);
          });
          ```

       b. **Scenario 2 — v2 happy path (3 valid edits):**
          ```
          await run('v2 happy path (3 valid edits)', async () => {
            const edits = [
              { intent: 'change-wording', pageRoute: '/', confirmationAccepted: true, intentDetail: { newTextEn: 'A', okToTranslate: true }, i18nKey: 'k1', nearbyText: 'long enough description here for signals' },
              { intent: 'change-wording', pageRoute: '/about/', confirmationAccepted: true, intentDetail: { newTextEn: 'B', okToTranslate: true }, i18nKey: 'k2', nearbyText: 'long enough description here for signals' },
              { intent: 'change-wording', pageRoute: '/', confirmationAccepted: true, intentDetail: { newTextEn: 'C', okToTranslate: true }, i18nKey: 'k3', nearbyText: 'long enough description here for signals' },
            ];
            const req = makeRequest({ schemaVersion: 2, batch: true, edits });
            const res = await POST({ request: req });
            if (res.status !== 200) throw new Error('expected 200, got ' + res.status);
            const body = await res.json();
            if (!body.ok || typeof body.issueNumber !== 'number') throw new Error('bad v2 response: ' + JSON.stringify(body));
            const createCalls = fetchLog.filter(c => /\\/issues$/.test(c.url) && c.method === 'POST');
            if (createCalls.length !== 1) throw new Error('expected 1 createIssue, got ' + createCalls.length);
            const createBody = JSON.parse(createCalls[0].body);
            if (!/^\\[Feedback\\] batch of 3 edits — /.test(createBody.title)) throw new Error('bad title: ' + createBody.title);
            if (!createBody.labels?.includes('client-feedback')) throw new Error('bad labels: ' + JSON.stringify(createBody.labels));
          });
          ```

       c. **Scenario 3 — v2 edit-cap violation (11 edits):**
          ```
          await run('v2 cap violation: 11 edits', async () => {
            const edits = Array.from({length: 11}, (_, i) => ({ intent: 'change-wording', pageRoute: '/', confirmationAccepted: true, intentDetail: { newTextEn: 'x' + i, okToTranslate: true }, i18nKey: 'k' + i, nearbyText: 'long enough description here for signals' }));
            const req = makeRequest({ schemaVersion: 2, batch: true, edits });
            const res = await POST({ request: req });
            if (res.status !== 422) throw new Error('expected 422, got ' + res.status);
            const body = await res.json();
            if (body.cap !== 'edits' || body.limit !== 10 || body.actual !== 11) throw new Error('bad cap-violation body: ' + JSON.stringify(body));
          });
          ```

       d. **Scenario 4 — v2 photo-cap violation:**
          ```
          await run('v2 cap violation: photo bytes', async () => {
            // Read MAX_BATCH_BYTES from the source so the test scales with D-03 reconciliation.
            const submitSrc = await import('node:fs').then(fs => fs.readFileSync('src/pages/api/feedback/submit.ts', 'utf8'));
            const m = submitSrc.match(/const MAX_BATCH_BYTES = (\\d+) \\* 1024 \\* 1024/);
            if (!m) throw new Error('cannot find MAX_BATCH_BYTES');
            const capMB = parseInt(m[1], 10);
            const capBytes = capMB * 1024 * 1024;
            // Construct 2 edits whose summed image.bytes > capBytes
            const perEditBytes = Math.ceil((capBytes + 1024) / 2);
            const edits = [
              { intent: 'replace-photo', pageRoute: '/', confirmationAccepted: true, intentDetail: {}, image: { present: true, mime: 'image/jpeg', dataBase64: 'AAAA', bytes: perEditBytes, originalFilename: 'a.jpg' }, i18nKey: 'k1', nearbyText: 'long enough description here for signals' },
              { intent: 'replace-photo', pageRoute: '/', confirmationAccepted: true, intentDetail: {}, image: { present: true, mime: 'image/jpeg', dataBase64: 'AAAA', bytes: perEditBytes, originalFilename: 'b.jpg' }, i18nKey: 'k2', nearbyText: 'long enough description here for signals' },
            ];
            const req = makeRequest({ schemaVersion: 2, batch: true, edits });
            const res = await POST({ request: req });
            if (res.status !== 422) throw new Error('expected 422, got ' + res.status);
            const body = await res.json();
            if (body.cap !== 'bytes' || body.limit !== capBytes) throw new Error('bad cap-violation body: ' + JSON.stringify(body));
          });
          ```

       e. **Scenario 5 — v2 per-edit error:**
          ```
          await run('v2 per-edit error', async () => {
            const edits = [
              { intent: 'change-wording', pageRoute: '/', confirmationAccepted: true, intentDetail: {} },  // missing newTextEn
              { intent: 'change-wording', pageRoute: '/', confirmationAccepted: true, intentDetail: { newTextEn: 'X', okToTranslate: true } },
            ];
            const req = makeRequest({ schemaVersion: 2, batch: true, edits });
            const res = await POST({ request: req });
            if (res.status !== 422) throw new Error('expected 422, got ' + res.status);
            const body = await res.json();
            if (!Array.isArray(body.errors)) throw new Error('expected errors array: ' + JSON.stringify(body));
            if (body.errors[0]?.index !== 0) throw new Error('expected errors[0].index === 0: ' + JSON.stringify(body));
          });
          ```

    8. **Final report:**
       - `console.log('\\n' + passed + ' passed, ' + failed + ' failed');`
       - `process.exit(failed > 0 ? 1 : 0);`

    9. **After writing the script, also perform the static + build smokes:**
       - `node --check public/feedback-inject.js`
       - `npx astro check` (capture output)
       - `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` (must be 1)
       - `grep -c "Batch submissions (v2 schema)" CLAUDE.md` (must be 1)
       - `grep -c "^## 8\\. Batch submissions" .github/CLAUDE_FEEDBACK.md` (must be 1)
       - `npm run build` (must exit 0; verify dist/ contains `feedback-inject.js?v=2`)
       - `git diff --name-only main` (capture for file-inventory check in summary)
       - `git diff --stat main` (capture for LOC-delta check in summary)

    10. **Failure handling:** If any smoke fails, the task FAILS. List the failing smoke(s) in 04-08-SUMMARY.md; the operator decides whether to dispatch revision plans or to accept (rare).

    Implementation notes:
    - The script uses Node 24 globals (`Request`, `fetch`, top-level await) — matches the project's `.nvmrc`.
    - The script imports from `src/` using `.ts` extensions; if Node refuses, run via `npx tsx scripts/smoke-feedback-v2.mjs` (document this fallback in the script's preamble).
    - No new package dependency is introduced. `tsx` is a dev-time fallback only.
  </action>
  <verify>
    <automated>set -e; echo '=== node --check ==='; node --check public/feedback-inject.js; echo '=== astro check ==='; npx astro check 2>&1 | tail -10; echo '=== doc smokes ==='; if [ "$(grep -c 'Batch submissions (v2 schema)' CLAUDE.md)" != "1" ]; then echo 'FAIL: CLAUDE.md missing batch bullet'; exit 1; fi; if [ "$(grep -c '^## 8\. Batch submissions' .github/CLAUDE_FEEDBACK.md)" != "1" ]; then echo 'FAIL: CLAUDE_FEEDBACK.md missing §8'; exit 1; fi; echo '=== cache-bust smoke ==='; if [ "$(grep -c \"FEEDBACK_INJECT_VER = '2'\" src/lib/feedback-version.ts)" != "1" ]; then echo 'FAIL: FEEDBACK_INJECT_VER not bumped to 2'; exit 1; fi; echo '=== smoke harness exists ==='; if [ ! -f scripts/smoke-feedback-v2.mjs ]; then echo 'FAIL: scripts/smoke-feedback-v2.mjs not created'; exit 1; fi; echo '=== run smoke harness ==='; (node scripts/smoke-feedback-v2.mjs || npx tsx scripts/smoke-feedback-v2.mjs); echo '=== build cache-bust ==='; npm run build 2>&1 | tail -20; if ! grep -lr 'feedback-inject.js?v=2' dist/ > /dev/null 2>&1; then echo 'FAIL: dist/ does not contain feedback-inject.js?v=2'; exit 1; fi; echo '=== file inventory ==='; git diff --name-only main; echo '=== diff stat ==='; git diff --stat main; echo 'INTEGRATION SMOKES PASS'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -f scripts/smoke-feedback-v2.mjs` exits 0 (script exists).
    - CLI: `node scripts/smoke-feedback-v2.mjs` (or `npx tsx scripts/smoke-feedback-v2.mjs` as fallback) exits 0; final report shows "5 passed, 0 failed".
    - CLI: `node --check public/feedback-inject.js` exits 0.
    - CLI: `npx astro check` reports no new errors (compared to baseline before phase).
    - CLI: `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns 1.
    - CLI: `grep -c "Batch submissions (v2 schema)" CLAUDE.md` returns 1.
    - CLI: `grep -c "^## 8\\. Batch submissions" .github/CLAUDE_FEEDBACK.md` returns 1.
    - CLI: `git diff --name-only main | sort` outputs exactly:
      ```
      .github/CLAUDE_FEEDBACK.md
      CLAUDE.md
      public/feedback-inject.js
      scripts/smoke-feedback-v2.mjs
      src/lib/feedback-version.ts
      src/pages/api/feedback/submit.ts
      src/pages/api/feedback/validate.ts
      ```
      (subject to operator-discretion additions like 04-XX-SUMMARY.md docs and ROADMAP.md / REQUIREMENTS.md edits from planning iteration 1).
    - CLI: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (re-verifying Task 1).
    - Behavior: Scenario 1 (v1) — smoke harness reports PASS; HTTP 200; v1 response shape.
    - Behavior: Scenario 2 (v2 happy path) — PASS; HTTP 200; createIssue title matches `^[Feedback] batch of 3 edits — `; labels include `client-feedback`.
    - Behavior: Scenario 3 (v2 11-edit cap) — PASS; HTTP 422; `cap:'edits'`, `limit:10`, `actual:11`.
    - Behavior: Scenario 4 (v2 byte cap) — PASS; HTTP 422; `cap:'bytes'`; `limit` matches `MAX_BATCH_BYTES` as read from `submit.ts` source (scales with D-03 reconciliation).
    - Behavior: Scenario 5 (v2 per-edit error) — PASS; HTTP 422; `errors:[{index:0, error:'New wording (English) is required'}]`.
    - Behavior: `npm run build` exits 0; built HTML contains `feedback-inject.js?v=2`.
    - The 04-08-SUMMARY.md records each smoke as PASS/FAIL with the captured evidence (file paths grep'd, response shapes captured).
  </acceptance_criteria>
  <done>Real smoke harness `scripts/smoke-feedback-v2.mjs` exists and exercises the 5 HTTP scenarios against the imported POST handler with stubbed fetch. All integration smokes pass: OPS-02 clean, v1 unchanged, v2 works end-to-end (server + client + caps + per-edit-errors), cache-bust survived the build, docs in place. 04-08-SUMMARY.md is the merge-gate report.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 4 PR diff → main branch (Vercel auto-deploy) | The merge gate. OPS-02 fence and the integration smokes are the last line of defence before changes reach the live site. |
| Smoke harness (scripts/smoke-feedback-v2.mjs) → submit.ts (in-process import) | The harness uses stubbed fetch + minted auth cookies, runs entirely in-process. No real GitHub or Vercel touch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-35 | Tampering / silent regression | A prior plan accidentally touched an OPS-02-fenced file (e.g., a refactor in submit.ts that inadvertently changed editor flow imports) | mitigate | Task 1 runs the exact ROADMAP-mandated git diff and fails loudly. Any output blocks merge. |
| T-04-36 | DoS / silent regression | The cache-bust didn't survive the build (e.g., a misconfigured Astro plugin stripped the query string) | mitigate | Task 2's build smoke greps the dist/ HTML for `feedback-inject.js?v=2`. If missing, fail. |
| T-04-37 | Spoofing | A v2 payload accepted despite missing or false `batch:true` flag bypassing per-edit validation | mitigate | The smoke harness's Scenario 5 covers the per-edit error path; the dispatch check at the POST entry rejects payloads with `schemaVersion:2 && !batch` via the 'Unsupported schema version' fallback. |
| T-04-38 | DoS / silent regression | A v1 cached browser hits the post-merge server and gets a different response than before | mitigate | Scenario 1 (v1 back-compat) in the smoke harness asserts an HTTP 200 + v1 response shape against the imported POST handler. Failure blocks merge. |
| T-04-40 | Tampering | Smoke harness fetch-stub being too permissive and masking real upstream errors | accept | The stub is intentionally permissive (returns 201 / 200) so the test isolates handler behavior, not GitHub behavior. Real GitHub integration is covered by Phase 5 canaries (OPS-05). |

</threat_model>

<verification>
- Task 1: OPS-02 git diff returns 0 lines.
- Task 2: scripts/smoke-feedback-v2.mjs exists; running it reports 5 passed, 0 failed; build cache-bust verified in dist/.
- 04-08-SUMMARY.md exists with the full report.
</verification>

<success_criteria>
- OPS-02 merge gate passes (editor flow byte-for-byte unchanged).
- v1 back-compat smoke passes (cached browsers keep working).
- v2 happy-path smoke passes (one issue per batch, correct title/body/labels).
- v2 cap-violation smokes pass (10-edit and byte caps enforced; the byte cap value scales with D-03 reconciliation).
- v2 per-edit-errors smoke passes (structured response shape).
- Build cache-bust smoke passes (dist/ HTML contains v=2).
- File change inventory matches expectation (7 files: 2 NEW [validate.ts, smoke-feedback-v2.mjs] + 5 MODIFIED).
- Real HTTP-level behavior is exercised by `scripts/smoke-feedback-v2.mjs` invoked from the verify block; static checks and behavior assertions agree (checker B4 path (a) chosen).
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-08-SUMMARY.md` per template. The summary MUST include:

1. **OPS-02 verification result** (PASS / FAIL with diff dump if failed).
2. **Integration smoke matrix** (each smoke, pass/fail, captured evidence from the harness output).
3. **File change inventory** (exact list from `git diff --name-only main`; flag any deviations from the expected 7-file set including the new smoke script).
4. **LOC delta** (from `git diff --stat main`; comparison to ROADMAP's ~450 LOC estimate, plus the smoke harness LOC which is separate from production code).
5. **D-03 reconciliation outcome** — from 04-03-SUMMARY.md, restate the verified Vercel tier + chosen MAX_BATCH_BYTES value. Cross-check Plan 04's client mirror matches.
6. **Pre-merge checklist** the operator runs through before clicking Squash and merge: (a) PR description includes the file inventory; (b) commits are atomic per-REQ where practical (D-18); (c) the PR description references this 04-08-SUMMARY.md; (d) the post-merge Phase 5 canaries (OPS-04, OPS-05) are ready to run.
</output>
