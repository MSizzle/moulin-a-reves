---
phase: 04-batch-pipeline-implementation
plan: 09
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/pages/api/feedback/submit.ts
autonomous: true
requirements: [API-05, API-06, ISSUE-04]
gaps_closed:
  - "CR-02: server `MAX_BATCH_BYTES` cap reads `e.image.bytes` descriptor instead of decoded base64 length (spoofable)"
  - "CR-03: v2 `handleV2Batch` returns `{ ok: true }` even when N of M photos failed to commit (silent partial failure)"
must_haves:
  truths:
    - "Server cap is computed from the decoded base64 byte length of every photo-bearing edit, not from the client-supplied `e.image.bytes` descriptor."
    - "A v2 batch where one or more `commitBase64File` calls failed returns a 200 with `warning` and `commitErrors` fields in the response body; a fully-clean batch is byte-identical to today."
  artifacts:
    - path: "src/pages/api/feedback/submit.ts"
      provides: "Exported `approxDecodedBytes(b64: string): number` helper; decoded-bytes reducer in `handleV2Batch` cap gate; aggregated `commitErrors[]` + `warning` field in 200 response when any per-edit commit failed."
      contains: "approxDecodedBytes"
  key_links:
    - from: "handleV2Batch totalBytes reducer (submit.ts:346-357)"
      to: "approxDecodedBytes helper"
      via: "sum over `e.image.dataBase64` (NOT `e.image.bytes`)"
      pattern: "approxDecodedBytes\\(.*dataBase64"
    - from: "handleV2Batch post-photo-loop response (submit.ts:556-564)"
      to: "finalMachineEdits[].commitError aggregation"
      via: "filter+map into `commitErrors: [{index, error}, ...]` and conditional `warning` field in JSON.stringify body"
      pattern: "commitErrors"
---

<objective>
Close the two server-side BLOCKERs (CR-02 + CR-03) in a single coherent change to `src/pages/api/feedback/submit.ts`. After this plan, the server-side half of Success Criterion #6 is deliverable: the cap reads what was actually shipped, and partial photo-commit failures are surfaced in the HTTP response. The UI-surfacing half lands in 04-10.

Purpose: Today an authenticated client can post `{ image: { bytes: 1, dataBase64: '<10 MB string>' } }` per edit and bypass `MAX_BATCH_BYTES` entirely (CR-02). And a 3-photo batch where 2 commits fail returns `{ ok: true }` while the issue body silently carries `commitError` markers (CR-03). v1 surfaces `warning` in the same scenario; v2 must mirror that semantic.

Output: One file changed (`submit.ts`); smoke harness scenario 4 will break until 04-10 updates it; no other smoke scenario changes behaviour. The OPS-02 fence is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md
@.planning/phases/04-batch-pipeline-implementation/04-REVIEW.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@CLAUDE.md
@src/pages/api/feedback/submit.ts
@src/pages/api/feedback/validate.ts

<interfaces>
<!-- Key landmarks executor needs in submit.ts. Read submit.ts in full before editing. -->

Existing exports/helpers in src/pages/api/feedback/submit.ts:
  - `handleV1(p)`        line ~164  — v1 single-edit path. Surfaces `warning: 'Issue created but the photo upload failed.'` at line ~295-303 when its single photo commit fails. Mirror this semantic for v2.
  - `handleV2Batch(p)`   line ~334  — v2 batch handler. Cap gate at lines 346-357 (the CR-02 site). Photo-commit loop at lines 510-553. Final response at lines 561-564 (the CR-03 site).
  - `fail(...)` / `failCap(...)` / `failBatch(...)`   error helpers above handleV1.
  - `finalMachineEdits[]` accumulator at line 510 with optional `commitError: 'photo commit failed'` field (recorded at line 545, attached to each entry at line 551).

Existing constants (DO NOT touch):
  - `MAX_BATCH_BYTES = 3 * 1024 * 1024`  line 53 — D-03 Hobby-tier cap. The cap value is correct; the reducer that compares against it is wrong.
  - `MAX_BATCH_EDITS = 10`               line 57 — D-01.
  - `SCHEMA_VERSION_V2 = 2`              line 33.

Existing client-side decode reference (for parity check, not for import):
  - `public/feedback-inject.js` already decodes base64 length client-side via the same shape we are building here (length × 3/4 minus padding). Mirror the math; do NOT import client code into the server route.

Existing `void` ritual at submit.ts:16-21 — leave the ritual unchanged; the new `approxDecodedBytes` helper is locally consumed inside `handleV2Batch` and does not need to be pinned.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `approxDecodedBytes` helper and switch the cap gate to read decoded bytes</name>
  <files>src/pages/api/feedback/submit.ts</files>
  <read_first>
    - src/pages/api/feedback/submit.ts (full file — landmarks at lines 16-21 void ritual, 32-57 schema/cap constants, 334-357 handleV2Batch cap gate, 510-553 photo loop)
    - .planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md (gap A truth + CR-02 evidence at lines 30-40, 122-124)
    - .planning/phases/04-batch-pipeline-implementation/04-REVIEW.md (CR-02 section at lines 110-156 with the exact fix-shape sketch)
  </read_first>
  <action>
    Add a module-scope helper `function approxDecodedBytes(b64: string): number` in `submit.ts`, placed just above `handleV2Batch` (after the GitHub helpers, before the v2 handler comment block at line 319). Behaviour per CR-02 fix-shape: return 0 for empty/falsy input; strip a `,` data-URL prefix if present by splitting on `,` and taking the last segment; subtract padding by counting trailing `=` (2 if ends with `==`, 1 if ends with `=`, else 0); compute `Math.floor((s.length * 3) / 4) - pad` and return it. Do not export the helper (module-internal is fine). Do not couple it to `validate.ts` — keep `validate.ts` unmodified per the gap-closure non-goals.

    Then replace the `totalBytes` reducer at submit.ts:346-349 to sum `approxDecodedBytes(e?.image?.dataBase64 || '')` instead of `e.image.bytes`. The cap comparison at line 350-356 stays unchanged — only the data source moves from the descriptor to the decoded payload. Keep the existing comment header at lines 340-345 intact; append a one-line note that the reducer reads decoded bytes per CR-02. Reference the gap by writing the literal token `CR-02` in the new comment line so it is grep-able.

    Do NOT modify `MAX_BATCH_BYTES`, `MAX_BATCH_EDITS`, the per-edit `validateEdit` call site at line 364, the photo-commit loop body (lines 510-553), or anything below line 357. This task is the cap-gate seam only.
  </action>
  <verify>
    <automated>grep -n "function approxDecodedBytes" src/pages/api/feedback/submit.ts | head -1 &amp;&amp; grep -n "approxDecodedBytes(e?\.image?\.dataBase64" src/pages/api/feedback/submit.ts | head -1 &amp;&amp; ! grep -n "sum + (typeof e?.image?.bytes === 'number' ? e.image.bytes : 0)" src/pages/api/feedback/submit.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "function approxDecodedBytes" src/pages/api/feedback/submit.ts` returns `1`.
    - Source assertion: `grep -c "approxDecodedBytes(e?\.image?\.dataBase64" src/pages/api/feedback/submit.ts` returns at least `1`.
    - Source assertion: the old descriptor-based reducer string `sum + (typeof e?.image?.bytes === 'number' ? e.image.bytes : 0)` (or equivalent reading `e.image.bytes` inside a `reduce` against `totalBytes`) is REMOVED from `handleV2Batch`. `grep -n "e\.image\.bytes" src/pages/api/feedback/submit.ts` may still show occurrences inside `imageMeta(...)` (the response-shape echo at line ~441 is fine) but MUST NOT appear inside any reducer that produces `totalBytes`.
    - Behaviour assertion (deferred to 04-10 smoke update): scenario 4 of `scripts/smoke-feedback-v2.mjs` will FAIL after this task because the smoke ships `dataBase64: 'AAAA'` (3 decoded bytes) — this is expected and is the proof that the cap is now reading the real payload. 04-10 updates scenario 4.
    - Behaviour assertion (parity): the existing v1-back-compat (scenario 1) and v2-edit-cap (scenario 3) smoke scenarios MUST still pass after this task — the change only affects the byte-cap reducer.
    - TypeScript: `npx tsc --noEmit -p tsconfig.json` exits 0 (no new type errors from the helper).
    - Token: the literal string `CR-02` appears in at least one comment in the modified region for grep-traceability.
  </acceptance_criteria>
  <done>The cap gate sums decoded bytes; `approxDecodedBytes` is defined module-scope; descriptor-based reducer is gone; TypeScript compiles; smoke scenarios 1, 2, 3, 5 still pass (scenario 4 is expected to fail until 04-10).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Aggregate per-edit `commitError` into `commitErrors[]` + `warning` field on the 200 response</name>
  <files>src/pages/api/feedback/submit.ts</files>
  <read_first>
    - src/pages/api/feedback/submit.ts lines 280-310 (v1 `warning` precedent — the shape v2 must mirror)
    - src/pages/api/feedback/submit.ts lines 510-564 (photo-commit loop + current `{ ok: true, issueNumber, issueUrl }` response — the CR-03 site)
    - .planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md (gap B truth + evidence at lines 42-60)
    - .planning/phases/04-batch-pipeline-implementation/04-REVIEW.md (CR-03 section at lines 160-205 with the exact aggregation shape sketch)
  </read_first>
  <action>
    Between the existing `await patchIssueBody(issueNumber, buildBatchBody(finalMachineEdits))` call at line 559 and the `return new Response(...)` at lines 561-564, add an aggregation step that walks `finalMachineEdits` and collects every entry where `m.commitError` is truthy into a `commitErrors` array of shape `{ index: number; error: string }` where `index` is the 0-based position in `finalMachineEdits` and `error` is the recorded `commitError` string (currently always `'photo commit failed'` from line 545; keep it generic — do not hardcode the string here, read from the entry).

    Then change the 200 response body construction so that when `commitErrors.length > 0` it ALSO includes `warning: '<N> of <M> photo(s) failed to upload'` and `commitErrors` as additional top-level fields alongside the existing `ok`, `issueNumber`, `issueUrl`. Use the same conditional-spread shape v1's success path uses semantically — when zero failures the response is byte-identical to today (`{ ok: true, issueNumber, issueUrl }`), when one or more failures the response gains the two new fields. `<N>` = `commitErrors.length`, `<M>` = `p.edits.length` (total batch size, NOT just photo-bearing edits — the smoke harness and the UI both reason about the batch as a whole).

    Status code stays `200` for both clean and partial-failure cases. This matches v1's behaviour at line 295-303 (200 with `warning` even when the photo failed) and is intentional: the issue WAS created, so the batch was not rejected; the photo-commit failure is a soft warning, not a 4xx. Do NOT change the 500 catch-block at lines 565-570.

    Do not modify the per-edit photo-commit loop body (lines 510-553). Do not modify `buildBatchBody` or `imageMeta`. Do not modify the `finalMachineEdits.push(...)` shape — `commitError` is already attached at line 551 and that is the source of truth for this aggregation.

    Reference the gap by writing the literal token `CR-03` in at least one new comment line in the modified region for grep-traceability.
  </action>
  <verify>
    <automated>grep -n "commitErrors" src/pages/api/feedback/submit.ts | head -5 &amp;&amp; grep -n "warning.*photo" src/pages/api/feedback/submit.ts | head -5 &amp;&amp; grep -c "CR-03" src/pages/api/feedback/submit.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "commitErrors" src/pages/api/feedback/submit.ts` returns at least `2` (one for the aggregation, at least one for the response body field).
    - Source assertion: a `warning` field referencing "photo" appears in the v2 response construction. `grep -n "warning.*photo" src/pages/api/feedback/submit.ts` returns at least 1 match within the `handleV2Batch` function body.
    - Source assertion: the literal token `CR-03` appears in at least one comment in the modified region. `grep -c "CR-03" src/pages/api/feedback/submit.ts` returns at least `1`.
    - Behaviour assertion (clean batch parity): the existing smoke scenario 2 (v2 happy path, 3 valid edits, all commits succeed — and in this harness no PUTs fail because the fetch stub returns 201 for every PUT) MUST still pass with `body.warning === undefined` and `body.commitErrors === undefined`. The response shape for a clean batch stays `{ ok: true, issueNumber, issueUrl }` byte-identical to today.
    - Behaviour assertion (partial-failure shape, manual scenario): if a future test or 04-10 forces `commitBase64File` to return false on one of N photos, the response body MUST be `{ ok: true, issueNumber, issueUrl, warning: '1 of N photo(s) failed to upload', commitErrors: [{ index: <i>, error: 'photo commit failed' }] }` with status 200.
    - TypeScript: `npx tsc --noEmit -p tsconfig.json` exits 0.
    - OPS-02 fence: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0` (this plan does not touch the fence).
  </acceptance_criteria>
  <done>The 200 response includes `warning` + `commitErrors` when any per-edit `commitError` was recorded; clean batches remain byte-identical to today; v1 `warning` semantic now mirrored on v2; OPS-02 fence untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → /api/feedback/submit | Authenticated POST body crosses here. Auth model is the shared dashboard-password HMAC cookie (see CLAUDE.md `src/lib/auth.ts`). Any session-holder is in-scope for the threat surface this plan addresses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04g-01 | Tampering / Repudiation | `handleV2Batch` cap gate (submit.ts:346-357) | mitigate | Replace client-descriptor reducer with `approxDecodedBytes(dataBase64)` so a session-holder posting `{ bytes: 1 }` cannot bypass `MAX_BATCH_BYTES`. Closes CR-02. |
| T-04g-02 | Denial of Service | GitHub Contents API quota + repo clone size | mitigate (bounded by T-04g-01) | Same fix bounds payload to ≤ 3 MB per request; Vercel's ~4.5 MB body limit is the upstream backstop. |
| T-04g-03 | Information Disclosure / Repudiation | v2 200 response after partial photo-commit failure | mitigate | Aggregate `commitErrors` + `warning` so the client cannot close the tab thinking a batch shipped clean when 2 of 3 photos failed. Closes CR-03. v1 already does this; symmetry restored. |
| T-04g-04 | Spoofing | Authentication itself (`DASHBOARD_PASSWORD`-derived HMAC cookie) | accept | Pre-existing project posture; out of scope for gap-closure. The cap fix narrows the impact of a compromised session but does not address the spoofing surface. |
</threat_model>

<verification>
After both tasks:
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `node --check public/feedback-inject.js` exits 0 (sanity — file untouched by this plan).
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0` (OPS-02 fence).
- `npx tsx scripts/smoke-feedback-v2.mjs` scenarios 1, 2, 3, 5 PASS; scenario 4 is EXPECTED TO FAIL after this plan because the smoke ships a tiny `dataBase64: 'AAAA'` stub — 04-10 will update the smoke to ship an actually-oversize base64 string. Document this expected scenario-4 failure in the plan summary so the executor running 04-10 has the breadcrumb.
- Grep traceability: `grep -c "CR-02" src/pages/api/feedback/submit.ts` ≥ 1, `grep -c "CR-03" src/pages/api/feedback/submit.ts` ≥ 1.
</verification>

<success_criteria>
- The byte cap is computed from `approxDecodedBytes(dataBase64)`, not `e.image.bytes`.
- The 200 response includes `warning` + `commitErrors` when one or more per-edit photo commits failed; clean batches return the same shape as today.
- TypeScript compiles; no other files are modified; OPS-02 fence untouched.
- Server-side half of Success Criterion #6 / API-06 is deliverable; ISSUE-04 atomicity surface is now wired both at the issue body AND the HTTP response. UI surfacing lands in 04-10.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-09-SUMMARY.md` per the standard summary template. In the summary, note:
1. Scenario 4 of `scripts/smoke-feedback-v2.mjs` is now EXPECTED TO FAIL and 04-10 must update it.
2. The new response shape for a partial-failure v2 batch is `{ ok: true, issueNumber, issueUrl, warning: '<N> of <M> photo(s) failed to upload', commitErrors: [{ index, error }] }` — this is the contract 04-10's `feedback.astro` forwarder must spread.
3. Confirm that `MAX_IMAGE_BYTES > MAX_BATCH_BYTES` asymmetry (WR-01) is OUT OF SCOPE and was not touched.
</output>
