---
phase: 04-batch-pipeline-implementation
plan: 07
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - OPS-03
user_setup: []
tags:
  - feedback
  - docs
  - claude-md

must_haves:
  truths:
    - "CLAUDE.md §'Feedback mode (`?feedback=1` → auto-code → auto-deploy)' has a new one-line bullet (5th bullet under the existing 4) about v2 batch submissions (OPS-03 / D-19)."
    - "The new bullet matches the style of the existing 'Cache-bust constant' bullet: `**Bold lead.**` followed by 2-3 sentences with inline `` `code` ``."
    - "The new bullet mentions: (a) the v2 schema is detected by `schemaVersion: 2 && batch: true`; (b) the shared validator at `src/pages/api/feedback/validate.ts` is consumed by both v1 and v2 paths; (c) the v1 single-edit path stays for cached browsers indefinitely."
    - "The new bullet implicitly cross-references `.github/CLAUDE_FEEDBACK.md §8` (added in Plan 05) so future devs / AI know where the operating manual lives."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated 'Feedback mode' section with one new bullet about v2 batching"
      contains: "Batch submissions"
  key_links:
    - from: "CLAUDE.md §'Feedback mode'"
      to: ".github/CLAUDE_FEEDBACK.md §8 (Plan 05)"
      via: "implicit cross-reference in the new bullet"
      pattern: "CLAUDE_FEEDBACK"
---

<objective>
Add a one-line note to `CLAUDE.md`'s "Feedback mode" section so future devs and AI agents know v2 batch submissions exist (OPS-03 / D-19).

This plan parallels Plan 06 (same Wave 5, different files - no conflict). It depends on Plan 05 because the bullet implicitly cross-references CLAUDE_FEEDBACK.md §8.

Purpose: Make the v2 schema discoverable from the architectural docs. Without this, a dev opening CLAUDE.md sees only the v1 single-edit pattern documented; the v2 batch flow is invisible at the architecture-doc layer.

Output: One bullet appended to the "Feedback mode" subsection of CLAUDE.md's "Architectural Constraints".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append the v2 batch-submissions bullet to CLAUDE.md "Feedback mode" section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the full §"Architectural Constraints" → "Feedback mode (`?feedback=1` → auto-code → auto-deploy)" subsection; find it - it lives under the broader Architectural Constraints H2)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"`CLAUDE.md` §'Feedback mode'" (the recommended bullet text and placement)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-19 (mandate) and §"specifics" (suggested phrasing)
  </read_first>
  <behavior>
    - Test: After the edit, `grep -c "Batch submissions" CLAUDE.md` returns at least 1.
    - Test: The new bullet appears INSIDE the "Feedback mode" subsection (between the existing bullets and the next H2 / H3 boundary - confirm by reading the surrounding context after edit).
    - Test: The bullet contains the literal substrings: `schemaVersion: 2`, `batch: true`, `src/pages/api/feedback/validate.ts`, and a reference to v1 back-compat.
    - Test: The file remains valid Markdown (no broken structure, all sections still parse).
  </behavior>
  <action>
    Locate the "Feedback mode (`?feedback=1` → auto-code → auto-deploy)" subsection inside CLAUDE.md's "Architectural Constraints" section. The subsection currently has four bullets (per CONTEXT.md "code_context" and PATTERNS.md §"CLAUDE.md §'Feedback mode'"):
    1. Sibling-of-the-editor-flow
    2. Cache-bust constant
    3. `feedback-incoming/` rule
    4. The feedback pipeline self-codes

    Append a 5TH bullet as the last item in that subsection (before the next H2 / H3 boundary). Use the existing bullet style: `**Bold lead.**` then 2-3 sentences with inline `` `code` ``. Use the recommended text from PATTERNS.md §"CLAUDE.md §'Feedback mode'" — verbatim or near-verbatim:

    `- **Batch submissions (v2 schema).** \`feedback-inject.js\` can stage multiple edits via a corner chip before submitting; the server in \`submit.ts\` detects v2 batches by \`schemaVersion: 2 && batch: true\` and creates ONE issue per batch. Per-edit validation runs through the shared helper consumed by both v1 and v2 paths (\`src/pages/api/feedback/validate.ts\`) so they can never drift. The v1 single-edit path stays for cached browsers indefinitely. Operating manual: \`.github/CLAUDE_FEEDBACK.md §8\`.`

    Use the Edit tool to make a SURGICAL change: locate the last bullet of the "Feedback mode" subsection (the "self-codes" bullet) and insert the new bullet immediately after it, on a new line. Do NOT touch any other bullet. Do NOT reformat the surrounding text. Do NOT add or remove any other content in CLAUDE.md.

    Verify by re-reading the section and confirming the bullet count went from 4 to 5 and the new bullet appears as the last one.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('CLAUDE.md','utf8'); const checks = [['Batch submissions \\(v2 schema\\)', true], ['schemaVersion: 2', true], ['batch: true', true], ['src/pages/api/feedback/validate.ts', true], ['CLAUDE_FEEDBACK.md', true], ['cached browsers', true]]; let bad = []; checks.forEach(([pat, want]) => { const has = new RegExp(pat).test(txt); if (has !== want) bad.push(pat); }); if (bad.length) { console.error('FAILED:', bad); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "Batch submissions (v2 schema)" CLAUDE.md` returns 1.
    - Source: `grep -c "schemaVersion: 2" CLAUDE.md` returns at least 1.
    - Source: `grep -c "batch: true" CLAUDE.md` returns at least 1.
    - Source: `grep -c "src/pages/api/feedback/validate.ts" CLAUDE.md` returns at least 1.
    - Source: `grep -c "CLAUDE_FEEDBACK.md" CLAUDE.md` returns at least 1 (cross-reference to Plan 05's §8).
    - Source: `git diff main -- CLAUDE.md | grep "^+" | grep -v "^+++" | wc -l` returns at least 1 (one or two added lines for the new bullet).
    - Source: `git diff main -- CLAUDE.md | grep "^-" | grep -v "^---" | wc -l` returns 0 (no lines removed - this is a pure append).
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 fence).
    - Source: `git diff main -- src/pages/api/feedback/submit.ts public/feedback-inject.js src/lib/feedback-version.ts .github/CLAUDE_FEEDBACK.md | wc -l` returns 0 (this plan only touches CLAUDE.md).
    - Behavior: Manually viewing the Feedback-mode subsection of CLAUDE.md shows 5 bullets (was 4); the new bullet is the last one.
  </acceptance_criteria>
  <done>One new bullet appended to CLAUDE.md "Feedback mode" subsection documenting v2 batch submissions, the shared validator path, v1 back-compat, and a pointer to CLAUDE_FEEDBACK.md §8. Pure append; no other file touched. OPS-02 fence respected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md (architectural documentation) → future dev / AI behavior | The document IS the architectural memory. A missing or misleading bullet could lead a future change to break the v1/v2 coexistence or to bypass the shared validator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-32 | Repudiation | Future change removing v1 back-compat without realizing cached browsers depend on it | mitigate | The new bullet explicitly states "The v1 single-edit path stays for cached browsers indefinitely" - making the constraint visible to future maintainers. |
| T-04-33 | Tampering | A future change re-introducing client/server validation drift by editing one side without updating the other | mitigate | The new bullet explicitly references `src/pages/api/feedback/validate.ts` as the shared validator - directing future readers to the source of truth. |
| T-04-34 | Information Disclosure | Documenting the v2 schema in a public-ish repo file (CLAUDE.md is tracked in git) | accept | This is intentional. CLAUDE.md is internal docs; the architectural patterns are not secrets. The auth gate on /api/feedback/submit is the real defence. |

</threat_model>

<verification>
- `grep -c "Batch submissions (v2 schema)" CLAUDE.md` returns 1.
- New bullet contains schemaVersion: 2, batch: true, src/pages/api/feedback/validate.ts, CLAUDE_FEEDBACK.md, cached browsers.
- No other file modified.
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0.
</verification>

<success_criteria>
- 5th bullet appended to CLAUDE.md "Feedback mode" subsection documenting v2.
- Cross-reference to CLAUDE_FEEDBACK.md §8 in place.
- OPS-02 fence respected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-07-SUMMARY.md` per template.
</output>
