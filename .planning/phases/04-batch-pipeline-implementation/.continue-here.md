---
context: phase
phase: 04-batch-pipeline-implementation
plan: 04-03
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-21T03:37:13.277Z
invoking_command: "/gsd-execute-phase 4 --auto"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered through failure during this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **Orphan worktree branch from prior wave** — Plan 04-03 has an in-flight worktree at `.claude/worktrees/agent-a8bd25cd031b6a9e7/` with 2 of 3 tasks committed on branch `worktree-agent-a8bd25cd031b6a9e7` and Task 3 sitting **dirty** in the worktree's submit.ts. **The orchestrator's safe_resume_gate uses `git log` (not `git log --all`) so it will NOT detect these orphan commits and WILL re-dispatch 04-03 from scratch, causing duplicate work and merge conflicts.** Structural mitigation: **Before re-invoking `/gsd-execute-phase 4 --auto`, run `git branch -a | grep worktree-agent-a8bd25cd031b6a9e7` and address the orphan explicitly** (finish + merge, or clean up + rerun) per the recovery procedure in `<current_state>` below.

- [ ] CONSTRAINT: **Never `git branch -D` a worktree branch before merge is verified on orchestrator HEAD** — The SDK helper `gsd-sdk query worktree.cleanup-wave --manifest <path>` can return `{ok: false, reason: "Usage: ..."}` due to a mktemp `XXXXXX.json` template collision (file-exists race when the manifest path was reused or not unique), but in some cases the merge has *already* succeeded inside the helper before the failure surface. The orchestrator's fallback in this session ran `git worktree remove --force` and `git branch -D` on the *unmerged* state and orphaned the wave-2 commits, recovered manually via `git merge <commit-hash> --no-ff` from the object store. Structural mitigation: after every cleanup-wave call, BOTH of these must be true before deleting a worktree branch: (1) helper JSON shows `ok:true`, AND (2) `git log --oneline -1` shows the orchestrator HEAD has advanced with a merge commit for that branch.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Orphan-worktree-on-resume | Resuming `/gsd-execute-phase 4 --auto` while branch `worktree-agent-a8bd25cd031b6a9e7` still exists (and is not merged) will silently re-dispatch 04-03 from base 4d704de while two commits already exist for it — duplicate commits, conflict on submit.ts merge | blocking | Before any orchestrator re-invocation, run `git branch -a \| grep worktree-agent` AND `git worktree list`. If anything shows up, finish/merge it OR clean it up before continuing. |
| Premature-branch-D | Calling `git branch -D <wt-branch>` before confirming the orchestrator HEAD advanced to a merge commit pointing at that branch | blocking | Gate `git branch -D` on `git log --oneline -1` showing a fresh `Merge … <branch>` commit AND the cleanup-helper JSON showing `ok:true`. |
| Mktemp-template-collision | `mktemp "${TMPDIR}/gsd-worktree-wave-XXXXXX.json"` — on macOS BSD mktemp the `.json` suffix is treated literally and reuse can collide | advisory | If reusing this pattern across waves, regenerate the temp file each wave; do not rely on a single env variable holding the path across waves. |
| Validate.ts-router-warning | `src/pages/api/feedback/validate.ts` sits at an Astro routable path but exports only helpers, not HTTP method handlers. `npm run build` emits `[WARN] [router] No API Route handler exists for the method "GET" …` every time. Build still passes | advisory | By design (PATTERNS.md D-15). Surface to code-review (`/gsd-code-review 04`) once the phase finishes; do not relocate during waves 3-8. |

<current_state>
**Where we are:** Phase 04, mid-execution, paused for environment migration to a container.

**Wave-by-wave status:**

| Wave | Plan | Status | Notes |
|------|------|--------|-------|
| 1 | 04-01 | ✓ merged | `f88a3f0` + merge `af0a9c1` + tracking `b5bef26` — validate.ts (102 LOC, 11 exports) |
| 2 | 04-02 | ✓ merged (recovered) | `0d82e9a` + merge `8c1af6e` + tracking `4d704de` — submit.ts v1 routed through validateEdit; v2 stub returns HTTP 501. Orphan-recovered via `git merge <hash>`. |
| 3 | 04-03 | ◆ in-flight | **Orphan worktree branch + dirty Task 3.** See recovery procedure below. |
| 4 | 04-04, 04-05 | ○ not started | parallel |
| 5 | 04-06, 04-07 | ○ not started | parallel |
| 6 | 04-08 | ○ not started | final OPS-02 gate + smoke harness |

**Orchestrator HEAD:** `4d704de docs(phase-04): update tracking after wave 2` on `main`. Branching strategy is `none`; all work lands on `main`. 10 unpushed commits.

**The 04-03 worktree state:**
- Path: `.claude/worktrees/agent-a8bd25cd031b6a9e7/`
- Branch: `worktree-agent-a8bd25cd031b6a9e7` — still exists, still **locked** (the original agent's lock will survive the session boundary).
- Base: `4d704de3c97da8b1176eea3ff38da0496d0beb9c` (wave 2 tracking — correct).
- Commits on the branch (2 of 3 tasks done):
  - `f835a74` feat(04-03): lock MAX_BATCH_BYTES to 3 MB per D-03 reconciliation
  - `5535b8f` feat(04-03): v2 batch handler — input gates + issue construction
- Dirty (uncommitted) file in the worktree: `src/pages/api/feedback/submit.ts` — Task 3 (~70% written): sequential photo commit loop using `crypto.subtle.digest('SHA-256', …)` inside `handleV2Batch()`, plus the final `patchIssueBody` call. Use `git -C .claude/worktrees/agent-a8bd25cd031b6a9e7 diff src/pages/api/feedback/submit.ts` to see it.
- No `04-03-SUMMARY.md` yet — `gsd-executor` would have written it as the last step.

**The unrelated dirt:** `.planning/config.json` shows `M` in main `git status` — this pre-existed before this session began (config was modified by some earlier `gsd-sdk query config-set` call outside the phase-4 work). Safe to ignore for this handoff; not phase-4 related.
</current_state>

<completed_work>

**This session's accomplishments:**

- Plan 04-01 created `src/pages/api/feedback/validate.ts` (102 LOC; exports validateEdit, signalCount, isVague, clamp, INTENTS, MAX_IMAGE_BYTES, MIN_VAGUE_LEN, MOVE_RESIZE_OPTIONS, VAGUE_STOPLIST, VAGUE_MESSAGE, Intent). API-04 / D-15 fulfilled.
- Plan 04-02 routed submit.ts v1 path through `validateEdit()` (deleted 98 LOC of duplicated helpers, added 76 LOC of dispatch infrastructure; net -22). Added `SCHEMA_VERSION_V2 = 2` + dispatch routing the v2 branch to a stub returning HTTP 501.
- Plan 04-03 — committed Task 1 (D-03: lock MAX_BATCH_BYTES to 3 MB) and Task 2 (input gates + issue construction). Task 3 (photo commit loop + final patchIssueBody) is dirty and 70% written.

Build gate passed on main after wave 1 and wave 2.
</completed_work>

<remaining_work>

1. **Resolve 04-03 orphan** — finish task 3 in the worktree (recommended) OR cleanup + re-dispatch (cleaner but ~10 min slower). See `<next_action>` for the exact recovery procedure.
2. **Wave 4** (parallel): 04-04 client v2 batch state machine in `public/feedback-inject.js` + 04-05 batch submissions section in `.github/CLAUDE_FEEDBACK.md`.
3. **Wave 5** (parallel): 04-06 bump FEEDBACK_INJECT_VER → '2' + 04-07 CLAUDE.md feedback-mode note.
4. **Wave 6**: 04-08 OPS-02 additive-only diff gate (zero lines diff vs main on editor-flow files) + smoke harness (`scripts/smoke-feedback-v2.mjs`) + PR-ready file inventory.
5. **Post-execution gates**: `/gsd-code-review 04` (advisory; will surface the validate.ts router warning), regression gate, schema-drift gate, codebase-drift gate.
6. **Phase verification** via `gsd-verifier` against the 23 phase req IDs (STAGE-01..07, API-01..06, ISSUE-01..04, ACTION-01..03, OPS-01..03).
7. **Roadmap update + transition** — auto-advance to phase 05 (canary verification) since AUTO_MODE is true.
</remaining_work>

<decisions_made>

- **validate.ts placement** at `src/pages/api/feedback/validate.ts` (a routable Astro path). PATTERNS.md D-15 mandates co-location under the same trust boundary. Astro warns at every build but build still passes. Will flag for code-review but do not relocate inside this phase.
- **Wave 2 recovery via `git merge <commit-hash>`** — when a worktree branch is `git branch -D`'d before merging, the commits survive in the git object store (they're unreachable but not garbage-collected) and can be merged back by hash. Used here for `0d82e9a` → became merge `8c1af6e`. Documented as anti-pattern above.
- **MAX_BATCH_BYTES = 3 MB** in plan 04-03 task 1 (commit f835a74) — reconciles D-03 (Vercel Hobby tier request body limit). Defensive lower bound vs the actual 4.5 MB limit, leaves headroom for base64 overhead.
- **Sequential (not parallel) photo commits in handleV2Batch** — task 3 dirty WIP. PATTERNS.md "GitHub Contents API: get-SHA-then-PUT" mandates sequential to avoid parent-SHA races on the shared `feedback-incoming/issue-<N>/` tree.
</decisions_made>

<blockers>
- **Orphan 04-03 worktree branch** — see CONSTRAINT 1 above. Resuming session must address before any orchestrator call.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable summary of the same state
2. This file — full anti-pattern context and recovery procedure
3. `.planning/phases/04-batch-pipeline-implementation/04-03-PLAN.md` — what task 3 is supposed to do; `must_haves` section is the contract for completion
4. `.planning/phases/04-batch-pipeline-implementation/04-02-SUMMARY.md` — current submit.ts dispatch shape (what the v2 stub looks like; task 3 replaces parts of `handleV2Batch`)
5. `.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md` — design decisions (D-01..D-19)
6. `.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md` — "GitHub Contents API: get-SHA-then-PUT" pattern (task 3 mirrors this exactly)
7. `git -C .claude/worktrees/agent-a8bd25cd031b6a9e7 diff src/pages/api/feedback/submit.ts` — the in-flight task 3 dirty diff

## Infrastructure State

- **Git state:** on `main`, 10 unpushed commits ahead of `origin/main`. No active rebase or merge. `.planning/config.json` shows modified but is pre-existing dirt unrelated to phase 04.
- **Worktree state:** one active worktree at `.claude/worktrees/agent-a8bd25cd031b6a9e7/`, branch `worktree-agent-a8bd25cd031b6a9e7`, **locked** (the lock will become stale after session end and can be force-removed without harm).
- **Node version warning:** Astro/Vercel adapter warns that local Node 26 is not officially supported by Vercel Serverless Functions (it falls back to Node 24 at deploy). Cosmetic; not a build blocker.
- **No test framework configured** — package.json has no `test` script. Post-merge "test gate" is skipped each wave; the post-merge build gate (`npm run build`) is what gates progression.
- **No `timeout` binary on macOS zsh** — use the Bash tool's `timeout` parameter instead of shell `timeout` when running long commands.

<context>
**Mental model when paused:**

The phase is a single-PR refactor (~450 LOC) that adds a v2 batch feedback path alongside the existing v1 single-edit path. Wave 1 extracted the shared validator. Wave 2 wired v1 through it and stubbed v2. Wave 3 is filling in v2. Waves 4-7 then add the client, docs, cache-bust, and final OPS-02 gate.

I was halfway through wave 3 when the Agent() tool-result vanished. The executor agent was alive and had committed 2/3 of its tasks. Task 3 — the photo commit loop with SHA-256 hashing — was being written into `submit.ts`'s `handleV2Batch()` between `createIssue()` and the return statement. The dirty diff shows the loop body well underway (about 35 lines of the photo-commit block already typed).

The cleanest resume path is **inside** that worktree: cd in, finish the photo loop, add the final `patchIssueBody` call, commit as `feat(04-03): …`, write `04-03-SUMMARY.md`, commit `docs(04-03): …`, then merge back to main via `git merge <branch-tip>`. **Do NOT delete the orphan branch before that merge lands.**

AUTO_MODE was true. After 04-03 lands, waves 4-6 should run autonomously, then code-review → verifier → roadmap update → transition to phase 05.
</context>

<next_action>

**Step 1 — verify orphan state survived the session boundary:**

```bash
cd "/Users/Montster/Melissa/Maison Website/moulin-a-reves"
git branch -a | grep worktree-agent-a8bd25cd031b6a9e7   # should show the branch
git worktree list                                        # should show 2 entries
ls .claude/worktrees/agent-a8bd25cd031b6a9e7/.planning/phases/04-batch-pipeline-implementation/04-03-PLAN.md   # should exist
git -C .claude/worktrees/agent-a8bd25cd031b6a9e7 status  # should show "modified: src/pages/api/feedback/submit.ts"
git -C .claude/worktrees/agent-a8bd25cd031b6a9e7 log --oneline -3  # should show 5535b8f, f835a74, 4d704de
```

**Step 2 — choose recovery path:**

- **Path A (recommended, ~10 min):** Finish task 3 inside the orphan worktree.
  1. `cd .claude/worktrees/agent-a8bd25cd031b6a9e7`
  2. Read the dirty `src/pages/api/feedback/submit.ts` diff and 04-03-PLAN.md task 3 to understand what's left.
  3. Finish the photo commit loop + the final `patchIssueBody` call.
  4. Run `npm run build` from the worktree (or `tsc --noEmit --strict`) to confirm it compiles.
  5. `git add src/pages/api/feedback/submit.ts && git commit -m "feat(04-03): photo commits + final issue body patch (API-05, ISSUE-04)"`
  6. Write `04-03-SUMMARY.md` per the SUMMARY template (verify, deviations, files, etc.)
  7. `git add .planning/phases/04-batch-pipeline-implementation/04-03-SUMMARY.md && git commit -m "docs(04-03): complete v2 batch handler implementation"`
  8. **`cd` back to project root**, then `git merge worktree-agent-a8bd25cd031b6a9e7 --no-ff --no-edit -m "chore: merge executor worktree (worktree-agent-a8bd25cd031b6a9e7, recovered)"`
  9. **Verify the merge committed and HEAD advanced** before cleaning up: `git log --oneline -1`
  10. `git worktree unlock .claude/worktrees/agent-a8bd25cd031b6a9e7 && git worktree remove .claude/worktrees/agent-a8bd25cd031b6a9e7 --force && git branch -D worktree-agent-a8bd25cd031b6a9e7`
  11. Run post-merge build: `npm run build` — should pass.
  12. Update tracking: `gsd-sdk query roadmap.update-plan-progress 04 04-03 complete` then commit `.planning/ROADMAP.md` + `.planning/STATE.md`.

- **Path B (cleaner, ~15 min):** Discard everything, re-run wave 3 fresh.
  1. `git worktree unlock .claude/worktrees/agent-a8bd25cd031b6a9e7 && git worktree remove .claude/worktrees/agent-a8bd25cd031b6a9e7 --force && git branch -D worktree-agent-a8bd25cd031b6a9e7`
  2. `git worktree prune`
  3. Now `/gsd-execute-phase 4 --auto` will safely re-dispatch 04-03 from base 4d704de.

**Step 3 — after 04-03 lands, resume the orchestrator:**

```bash
/gsd-execute-phase 4 --auto
```

It will detect 04-01, 04-02, 04-03 as done (SUMMARY.md present) and start wave 4 (04-04 + 04-05 in parallel). AUTO_MODE chain will carry through code-review, verifier, roadmap update, and transition to phase 05 automatically.

**Step 4 — when the validate.ts router warning surfaces during `/gsd-code-review 04`:** the decision was deliberate (PATTERNS.md D-15). Acknowledge the warning and move on — do not relocate the file.
</next_action>
