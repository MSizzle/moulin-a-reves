---
phase: 04-batch-pipeline-implementation
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - public/feedback-inject.js
autonomous: true
requirements:
  - STAGE-01
  - STAGE-02
  - STAGE-03
  - STAGE-04
  - STAGE-05
  - STAGE-06
  - STAGE-07
user_setup: []
tags:
  - feedback
  - client
  - inject
  - state-machine
  - chip
  - panel

must_haves:
  truths:
    - "After the client clicks 'Looks right — send it' on the first edit, the existing single-edit submit is replaced by a STAGE action that pushes the built payload into sessionStorage['mar_feedback_staged_v1'] — sessionStorage ONLY per D-08 (stages survive iframe navigation and reload, clear on browser close; explicitly NOT localStorage and NOT server-side draft issues — those are v1.2 candidates) — and returns the state machine to IDLE (STAGE-01 / D-08)."
    - "A corner chip element `#mar-fb-chip` appears after the first stage, reading `N edits staged · Submit batch · View list`; clicking the chip opens a panel listing each staged edit in plain language with a per-item ❌ delete (irrevocable, D-12) and a 'Clear all' button that uses window.confirm() before clearing (D-11) (STAGE-02 / STAGE-03)."
    - "The chip rehydrates on iframe navigation (pageshow / visibilitychange listener calls loadStaged()) so cross-page batching works (D-06)."
    - "Clicking 'Submit batch' produces exactly ONE POST whose JSON body is `{schemaVersion:2, batch:true, edits:[...]}` and whose Content-Type is application/json; on 200 response the chip + panel disappear and sessionStorage is cleared (STAGE-04)."
    - "Photo Files are NOT serialised into sessionStorage; only a descriptor `{name,type,size}` is persisted. Actual File objects live in an in-memory Map keyed by stageId. Base64 encoding happens ONLY at submit-batch time (STAGE-05 / D-09)."
    - "Client enforces the 10-edit cap (STAGE-06): when staging would exceed 10, an inline panel message reads 'This batch is full — submit it before staging more' AND the 'Confirm' button on the open edit is disabled (D-04 / STAGE-07)."
    - "Client enforces the 30MB total-photo-size cap (STAGE-06): the same panel message + Confirm disable triggers when summed photo bytes would exceed 30 * 1024 * 1024."
    - "On a 422 per-edit-errors response (API-03 shape `{errors:[{index,error}]}`), the panel highlights which staged items failed but does NOT clear sessionStorage; the chip stays visible."
    - "Per-edit ❌ delete is irrevocable - no confirm (D-12)."
    - "FEEDBACK_INJECT_VER bump (Plan 06) is required to invalidate the cached v1 inject; this plan ships the new client BUT cannot be observed by users until the version constant bumps in the same PR (D-14 / OPS-01 - cross-plan dependency)."
  artifacts:
    - path: "public/feedback-inject.js"
      provides: "v2 state machine with STAGED state; corner chip + panel DOM; sessionStorage staging; submit-batch path"
      contains: "STAGED_KEY"
      min_lines: 950
    - path: "public/feedback-inject.js"
      provides: "SCHEMA_VERSION_V2, MAX_BATCH_EDITS, MAX_BATCH_BYTES, STAGED_KEY constants mirroring submit.ts (KEEP-IN-SYNC convention)"
      contains: "MAX_BATCH_EDITS = 10"
  key_links:
    - from: "STAGE button in renderConfirm()"
      to: "stagedPush() → sessionStorage.setItem(STAGED_KEY, ...)"
      via: "click handler"
      pattern: "STAGED_KEY"
    - from: "chip 'Submit batch' button"
      to: "window.parent.postMessage({type:'mar-feedback-submit', payload:{schemaVersion:2, batch:true, edits:[...]}})"
      via: "click handler builds payload from staged[] + in-memory File map"
      pattern: "mar-feedback-submit"
    - from: "pageshow / visibilitychange listeners"
      to: "loadStaged() → renderChip()"
      via: "iframe-navigation rehydrate"
      pattern: "pageshow"
---

<objective>
Implement the client-side v2 batch state machine in `public/feedback-inject.js`: extend the existing `IDLE → SELECTED → FIELDS → CONFIRM → DONE` flow into `... → STAGED → (loop or Submit) → DONE`; add the corner chip + panel UI; mirror server-side caps (D-01 / D-02) client-side as STAGE-06 with the STAGE-07 limit-reached UX (D-04); persist staged descriptors in sessionStorage (D-08 / D-09); rehydrate on iframe navigation for cross-page batching (D-06); base64-encode photos only at submit time (STAGE-05).

This is the largest behavioural change in the phase (~350 LOC added to feedback-inject.js). The file's existing IIFE structure, ES5-style `var` usage, the `validateFields()` validator, the existing `mar-feedback-submit` postMessage contract, and the `?feedback=1` iframe-only guard are all PRESERVED.

Purpose: Deliver the client half of the batch pipeline. After this plan + Plan 03 ship, a 3-edit batch round-trip works end-to-end against the new server path.

Output: Modified feedback-inject.js implementing the full STAGE flow; ESxx style preserved; admin-facing chip styling consistent with the existing `#mar-fb-banner` / `#mar-fb-panel` palette (`var ACCENT = '#FF6B2B'`, `#0f172a`, `#e2e8f0`). Per Claude's Discretion (CONTEXT.md), chip uses native window.confirm() for "Clear all" (lightest pattern, no new UI deps); chip text is admin-facing and skips i18n entirely.

**Emergent requirements declared in this plan (per D-05): STAGE-06 (client cap enforcement) and STAGE-07 (chip 'limit reached' UX). Surface in 04-04-SUMMARY.md for ROADMAP.md / REQUIREMENTS.md traceability-table update.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@.planning/phases/04-batch-pipeline-implementation/04-03-PLAN.md
@CLAUDE.md
@public/feedback-inject.js
@src/pages/feedback.astro
@src/pages/api/feedback/submit.ts

<interfaces>
<!-- v2 client-side state machine extends the existing one in feedback-inject.js:90-91: -->
<!--                                                                              -->
<!-- var STATE = {                                                                -->
<!--   IDLE: 'idle',          // existing                                         -->
<!--   SELECTED: 'selected',  // existing                                         -->
<!--   FIELDS: 'fields',      // existing                                         -->
<!--   CONFIRM: 'confirm',    // existing                                         -->
<!--   STAGED: 'staged',      // NEW (post-stage, panel open showing chip)        -->
<!--   SUBMITTING: 'submitting',  // NEW (between Submit batch click and 200)     -->
<!--   DONE: 'done',          // existing                                         -->
<!--   AUTH: 'auth'           // existing                                         -->
<!-- };                                                                           -->
<!--                                                                              -->
<!-- // sessionStorage shape (STAGED_KEY = 'mar_feedback_staged_v1'):             -->
<!-- // [                                                                          -->
<!-- //   {                                                                        -->
<!-- //     stageId: 's_<timestamp>_<rand>',  // for in-memory File map keying     -->
<!-- //     intent: 'change-wording' | ...,                                        -->
<!-- //     pageRoute: '/',                                                        -->
<!-- //     confirmationAccepted: true,                                            -->
<!-- //     intentDetail: { ... },                                                 -->
<!-- //     locator: { i18nKey, i18nAttr, imageRef, galleryAttrRaw, ... },         -->
<!-- //     imageDescriptor: { name, type, size } | null,  // NOT the File / NOT base64 -->
<!-- //     stagedAt: <ts>                                                         -->
<!-- //   },                                                                       -->
<!-- //   ...                                                                      -->
<!-- // ]                                                                          -->
<!--                                                                              -->
<!-- // In-memory File map (NOT in sessionStorage):                                -->
<!-- // var fileMap = {};  // stageId → File object                                -->
<!-- // Reading a File: fileMap[stageId]                                           -->
<!-- // When sessionStorage rehydrates on iframe nav but fileMap is empty (new doc), -->
<!-- // the staged entry's imageDescriptor remains; the panel shows the file name  -->
<!-- // and size but the user must re-attach the photo before that edit can submit. -->
<!-- // (Cross-page batching with photos: photo-less edits round-trip cleanly;     -->
<!-- // photo-bearing edits attached on one page work if submitted before nav.)    -->

<!-- v2 POST body shape (mirrors Plan 03's server contract): -->
<!--   {                                                    -->
<!--     schemaVersion: 2,                                  -->
<!--     batch: true,                                       -->
<!--     edits: [                                           -->
<!--       {                                                -->
<!--         intent, pageRoute, confirmationAccepted,       -->
<!--         intentDetail,                                  -->
<!--         image: { present, mime, dataBase64, bytes, originalFilename }, -->
<!--         ... locator fields                             -->
<!--       },                                               -->
<!--       ...                                              -->
<!--     ]                                                  -->
<!--   }                                                    -->

<!-- The existing parent (feedback.astro) postMessage receiver does NOT change: -->
<!-- it accepts `{type:'mar-feedback-submit', payload: ...}` and POSTs payload  -->
<!-- as JSON to /api/feedback/submit. v2 plugs in by setting payload to the   -->
<!-- batch shape. Confirmed via reading src/pages/feedback.astro.             -->

<!-- Chip DOM (admin-facing, no i18n - per CONTEXT.md Claude's Discretion):    -->
<!--   <div id="mar-fb-chip">                                                  -->
<!--     <span class="mar-fb-chip__count">N</span>                             -->
<!--     <span class="mar-fb-chip__sep"> edits staged · </span>                -->
<!--     <button class="mar-fb-chip__submit">Submit batch</button>             -->
<!--     <span class="mar-fb-chip__sep"> · </span>                             -->
<!--     <button class="mar-fb-chip__view">View list</button>                  -->
<!--   </div>                                                                  -->
<!-- Styling: same #mar-fb- block prefix as existing banner/panel; ACCENT      -->
<!-- '#FF6B2B', text '#0f172a' on '#e2e8f0' background; z-index 2147483645     -->
<!-- (one lower than panel, one higher than banner per PATTERNS.md item 8).    -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v2 constants, sessionStorage helpers, in-memory File map, STAGE state, and rehydrate-on-load</name>
  <files>public/feedback-inject.js</files>
  <read_first>
    - public/feedback-inject.js (full file - the file being modified)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"public/feedback-inject.js" items 1, 2, 3, 4, 12 (IIFE wrapper, constants, state enum, sessionStorage helpers, restore-on-load)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-06, D-08, D-09 (cross-page batching, sessionStorage only, File-in-memory + descriptor-in-storage)
    - src/pages/api/feedback/submit.ts (post-Plan-03 - the cap constants this plan mirrors)
  </read_first>
  <behavior>
    - Test: After load, `sessionStorage.getItem('mar_feedback_staged_v1')` is honored on read (loadStaged returns the parsed array or [] on parse error / private-mode throw).
    - Test: saveStaged([{stageId:'s_1', intent:'change-wording', ...}]) writes the JSON-stringified array; clearStaged() removes the key.
    - Test: On `pageshow` event, loadStaged() runs; if staged.length > 0 the chip element exists in DOM (`document.getElementById('mar-fb-chip')` is non-null).
    - Test: window.STATE includes STAGED and SUBMITTING values.
    - Test: All sessionStorage calls are wrapped in try/catch (no throws from private-mode).
    - Test: MAX_BATCH_EDITS, MAX_BATCH_BYTES, SCHEMA_VERSION_V2 constants are defined inside the IIFE with values 10, 31457280, 2 respectively.
  </behavior>
  <action>
    Modify public/feedback-inject.js inside the existing IIFE (lines 17-710 approximately). Preserve the iframe-only guard and the `?feedback=1` URLSearchParams check.

    1. ADD constants in the existing shared-contract block at the top (around line 21-43). The block already has a `// ---- shared contract (KEEP IN SYNC with src/pages/api/feedback/submit.ts) --` comment. UPDATE the comment to: `// ---- shared contract (KEEP IN SYNC with src/pages/api/feedback/submit.ts + src/pages/api/feedback/validate.ts) --`. ADD the v2 constants:
       ```
       var SCHEMA_VERSION_V2 = 2;
       var MAX_BATCH_EDITS = 10;              // D-01 / STAGE-06 - mirror submit.ts MAX_BATCH_EDITS
       var MAX_BATCH_BYTES = 30 * 1024 * 1024;  // D-02 / STAGE-06 - mirror submit.ts MAX_BATCH_BYTES
       var STAGED_KEY = 'mar_feedback_staged_v1';  // sessionStorage, not localStorage (D-08)
       ```
       Use `var` (not `let`/`const`) per the existing ES5 style. Single quotes. 2-space indent. Trailing semicolons.

    2. EXTEND the STATE object (currently at line ~90):
       ```
       var STATE = { IDLE: 'idle', SELECTED: 'selected', FIELDS: 'fields', CONFIRM: 'confirm',
                     STAGED: 'staged', SUBMITTING: 'submitting', DONE: 'done', AUTH: 'auth' };
       ```

    3. ADD sessionStorage helpers (mirror the existing `saveDraft` / `clearDraft` shape at lines 281-289):
       ```
       function loadStaged() {
         try { var raw = sessionStorage.getItem(STAGED_KEY); return raw ? JSON.parse(raw) : []; }
         catch (e) { return []; }
       }
       function saveStaged(arr) { try { sessionStorage.setItem(STAGED_KEY, JSON.stringify(arr)); } catch (e) {} }
       function clearStaged() { try { sessionStorage.removeItem(STAGED_KEY); } catch (e) {} }
       ```

    4. ADD an in-memory File map sibling to the existing module-level vars (around line 89, near `var draft = ...`):
       ```
       var fileMap = {};  // stageId → File object; ephemeral, NOT serialised to storage (D-09 / STAGE-05)
       function nextStageId() { return 's_' + Date.now() + '_' + Math.floor(Math.random() * 1e9).toString(36); }
       ```

    5. ADD a rehydrate-on-load block as a sibling to the existing `(function restore() { ... })()` IIFE at lines 670-703. Different shape - staged edits don't need a 'Resume?' prompt because they're already committed:
       ```
       (function rehydrateStaged() {
         var staged = loadStaged();
         if (staged.length > 0) renderChip(staged.length);
       })();
       ```
       (`renderChip` is defined in Task 2.)

    6. ADD `pageshow` and `visibilitychange` listeners for cross-page rehydration (D-06):
       ```
       window.addEventListener('pageshow', function () {
         var staged = loadStaged();
         if (staged.length > 0 && !document.getElementById('mar-fb-chip')) {
           renderChip(staged.length);
         }
       });
       document.addEventListener('visibilitychange', function () {
         if (document.visibilityState === 'visible') {
           var staged = loadStaged();
           if (staged.length > 0 && !document.getElementById('mar-fb-chip')) {
             renderChip(staged.length);
           }
         }
       });
       ```

    7. DO NOT touch the existing localStorage `DRAFT_KEY` / `saveDraft` / `clearDraft` logic - the in-progress-edit draft remains a separate concern (per CONTEXT.md, staged edits are about COMMITTED edits; the draft is the in-progress unsaved edit).

    8. The file must still be a single IIFE. No new `<script>` elements. No new event-listener leaks (always-added top-level listeners are OK).

    9. After this task: constants exist, helpers exist, the file LOADS without errors, but the chip is not yet defined (Task 2 defines it). The rehydrate IIFE will call `renderChip` which is undefined at load time - DEFER the rehydrate call to the bottom of the IIFE (after Task 2 declares renderChip) by either (a) placing it at the very END of the IIFE, or (b) wrapping in `setTimeout(rehydrateStaged, 0)` to defer past hoisting. Recommended: place at the end of the IIFE after all function declarations.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('public/feedback-inject.js','utf8'); const checks = [['var SCHEMA_VERSION_V2 = 2', true], ['var MAX_BATCH_EDITS = 10', true], ['var MAX_BATCH_BYTES = 30 \\* 1024 \\* 1024', true], ['var STAGED_KEY = .mar_feedback_staged_v1.', true], ['STAGED: .staged.', true], ['SUBMITTING: .submitting.', true], ['function loadStaged', true], ['function saveStaged', true], ['function clearStaged', true], ['function nextStageId', true], ['addEventListener\\(.pageshow', true], ['addEventListener\\(.visibilitychange', true]]; let bad = []; checks.forEach(([pat, want]) => { const has = new RegExp(pat).test(txt); if (has !== want) bad.push(pat); }); if (bad.length) { console.error('FAILED:', bad); process.exit(1); } console.log('OK Task 1'); "</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "var SCHEMA_VERSION_V2 = 2" public/feedback-inject.js` returns 1.
    - Source: `grep -c "var MAX_BATCH_EDITS = 10" public/feedback-inject.js` returns 1.
    - Source: `grep -c "var MAX_BATCH_BYTES = 30 \\* 1024 \\* 1024" public/feedback-inject.js` returns 1.
    - Source: `grep -c "STAGED_KEY = 'mar_feedback_staged_v1'" public/feedback-inject.js` returns 1.
    - Source: `grep -c "STAGED: 'staged'" public/feedback-inject.js` returns 1.
    - Source: `grep -c "SUBMITTING: 'submitting'" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function loadStaged" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function saveStaged" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function clearStaged" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function nextStageId" public/feedback-inject.js` returns 1.
    - Source: `grep -c "addEventListener('pageshow'" public/feedback-inject.js` returns 1.
    - Source: `grep -c "addEventListener('visibilitychange'" public/feedback-inject.js` returns 1.
    - Source: `grep -c "KEEP IN SYNC" public/feedback-inject.js` returns at least 1 (the KEEP-IN-SYNC comment is updated, not removed).
    - Source: `grep -c "fileMap" public/feedback-inject.js` returns at least 2 (declaration + at least one mention).
    - Behavior: The file LOADS in a browser (or via `node --check public/feedback-inject.js`) without syntax errors. `node --check public/feedback-inject.js` returns exit 0.
    - Behavior: Manually opening the file in a browser tab via `?feedback=1` (mock-loaded) does not throw in the console; an iframe outside `?feedback=1` does not initialise anything (existing guard preserved).
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 fence).
  </acceptance_criteria>
  <done>Constants, sessionStorage helpers, in-memory File map, STATE extension, and pageshow/visibilitychange listeners land. File parses with `node --check`. Chip rendering itself comes in Task 2; rehydrate IIFE references renderChip which is forward-declared by being placed at the end of the IIFE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement renderChip(), renderPanel(), stagedPush/Delete/ClearAll, and replace the v1 submit on Confirm with STAGE</name>
  <files>public/feedback-inject.js</files>
  <read_first>
    - public/feedback-inject.js (current state after Task 1)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"public/feedback-inject.js" items 3, 5, 6, 7, 8, 10, 11 (state enum extension, validateFields stays, el() helper, escapeHtml, style block, result-receiver, reset)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-04, D-11, D-12 (cap UX, clear-all confirm dialog, per-item ❌ irrevocable)
    - src/pages/api/feedback/submit.ts (Plan 03 v2 contract - the POST body shape and response shapes the client builds against)
    - src/pages/feedback.astro (the parent that receives `mar-feedback-submit` postMessage and POSTs to /api/feedback/submit - confirm message contract unchanged)
  </read_first>
  <behavior>
    - Test: After confirming the first edit, `document.getElementById('mar-fb-chip')` exists, its text content contains 'edits staged', 'Submit batch', and 'View list', and sessionStorage 'mar_feedback_staged_v1' contains a single-element JSON array.
    - Test: After two confirmed stages, the chip text begins with `2 edits staged`.
    - Test: Clicking the chip's 'View list' button creates `document.getElementById('mar-fb-panel-staged')` (or similar identifiable container) showing each staged item with the page route, intent, and (for change-wording) the new EN text.
    - Test: Each panel item has a clickable ❌ delete element with no confirm dialog (D-12 - irrevocable). Clicking it removes the item from sessionStorage, removes it from the in-memory fileMap, decrements the chip count.
    - Test: Panel includes a 'Clear all' button. Clicking it calls `window.confirm('Clear all staged edits? This cannot be undone.')` (or equivalent string). If confirmed: sessionStorage cleared, fileMap cleared, chip removed, panel closed.
    - Test: After staging 10 edits and attempting to stage an 11th, the renderConfirm()'s 'Looks right — send it' button is disabled AND a panel-level inline message reads `This batch is full — submit it before staging more` (D-04 / STAGE-07).
    - Test: When the SUM of all staged photo bytes (from descriptors + the about-to-stage entry) would exceed 30 * 1024 * 1024, the same disable + message triggers (STAGE-06 byte cap).
    - Test: When the user removes a staged item via ❌ that drops the totals back under the caps, the next renderConfirm()'s Confirm button is re-enabled (no stale disabled state).
    - Test: Staging a `change-wording` edit pushes an entry into staged[] with `imageDescriptor:null` (no image); staging a `replace-photo` edit pushes `imageDescriptor:{name,type,size}` and stores the File in `fileMap[stageId]`. The dataBase64 field is NOT in sessionStorage at any point (assert by reading sessionStorage and checking the value contains neither 'dataBase64' nor base64 strings >100 chars).
    - Test: Panel text uses escapeHtml on every client-supplied string (new EN text, page route, file name). Inject test: a staged edit with newTextEn `<script>alert(1)</script>` renders in the panel as escaped `&lt;script&gt;alert(1)&lt;/script&gt;` - no script tag in the DOM.
  </behavior>
  <action>
    Add new top-level functions inside the IIFE (place AFTER existing render functions like `renderConfirm`, BEFORE the rehydrate IIFE at the end). Use ES5 `var`/`function` style throughout.

    1. ADD CSS rules to the EXISTING `style.textContent` concatenation at lines 57-87 (do NOT create a new `<style>` element). Append to the existing concat string:
       ```
       '#mar-fb-chip{position:fixed;right:16px;bottom:16px;z-index:2147483645;padding:8px 12px;border-radius:999px;background:#0f172a;color:#e2e8f0;font-family:system-ui,sans-serif;font-size:13px;line-height:1.2;cursor:default;box-shadow:0 2px 8px rgba(0,0,0,.25);display:flex;gap:6px;align-items:center;}' +
       '#mar-fb-chip .mar-fb-chip__count{background:' + ACCENT + ';color:#fff;border-radius:999px;padding:2px 8px;font-weight:600;}' +
       '#mar-fb-chip button{background:transparent;color:#e2e8f0;border:none;cursor:pointer;text-decoration:underline;font:inherit;padding:0;}' +
       '#mar-fb-chip button:hover{color:#fff;}' +
       '#mar-fb-panel-staged{position:fixed;right:16px;bottom:60px;z-index:2147483647;width:360px;max-height:60vh;overflow-y:auto;background:#0f172a;color:#e2e8f0;font-family:system-ui,sans-serif;font-size:13px;padding:12px;border-radius:8px;box-shadow:0 4px 16px rgba(0,0,0,.4);}' +
       '#mar-fb-panel-staged .mar-fb-staged-item{padding:8px;border:1px solid #1e293b;border-radius:6px;margin-bottom:8px;display:flex;justify-content:space-between;align-items:flex-start;gap:8px;}' +
       '#mar-fb-panel-staged .mar-fb-staged-item.is-error{border-color:' + ACCENT + ';}' +
       '#mar-fb-panel-staged .mar-fb-staged-item__body{flex:1;overflow:hidden;}' +
       '#mar-fb-panel-staged .mar-fb-staged-item__delete{background:transparent;color:#e2e8f0;border:none;cursor:pointer;font-size:16px;line-height:1;padding:0 4px;}' +
       '#mar-fb-panel-staged .mar-fb-staged-item__delete:hover{color:' + ACCENT + ';}' +
       '#mar-fb-panel-staged .mar-fb-staged-cap-msg{color:' + ACCENT + ';font-weight:600;padding:8px;border:1px dashed ' + ACCENT + ';border-radius:6px;margin-bottom:8px;}' +
       '#mar-fb-panel-staged .mar-fb-staged-actions{display:flex;justify-content:space-between;gap:8px;margin-top:8px;}' +
       '#mar-fb-panel-staged .mar-fb-staged-actions button{flex:1;padding:8px;border-radius:6px;border:none;font-weight:600;cursor:pointer;}' +
       '#mar-fb-panel-staged .mar-fb-staged-submit{background:' + ACCENT + ';color:#fff;}' +
       '#mar-fb-panel-staged .mar-fb-staged-clear{background:#1e293b;color:#e2e8f0;}' +
       ```
       BEM-style (`block__element--modifier`) per CLAUDE.md naming.

    2. ADD `function renderChip(count) { ... }`:
       - If `count === 0`, removes any existing `#mar-fb-chip` and returns.
       - Otherwise: if chip exists, update only the count span; if not, create it via the `el()` helper. Children: `<span class="mar-fb-chip__count">N</span>`, then a literal space + 'edits staged · ', then a `<button>Submit batch</button>`, then ' · ', then a `<button>View list</button>`.
       - Submit batch button click: call `submitBatch()` (defined in Task 3).
       - View list button click: call `togglePanel()` which opens / closes `#mar-fb-panel-staged`.

    3. ADD `function renderPanel() { ... }`:
       - Removes any existing `#mar-fb-panel-staged` and rebuilds from `loadStaged()`.
       - For each staged item: a `.mar-fb-staged-item` div containing a `.mar-fb-staged-item__body` with the page route, intent, and a one-line plain-language summary (e.g. for change-wording: `On ${escapeHtml(route)}: change to "${escapeHtml(newTextEn.slice(0, 80))}…"`; for replace-photo: `On ${escapeHtml(route)}: replace photo with ${escapeHtml(imageDescriptor.name)}`).
       - Each item gets a ❌ delete button (no confirm per D-12) that on click calls `stagedDelete(stageId)`.
       - At the bottom, two buttons: 'Submit batch' (calls submitBatch) and 'Clear all' (calls stagedClearAll which uses window.confirm per D-11).
       - If the panel includes any error highlights (from a previous failed submit per Plan 03's `errors:[{index,error}]` shape), add `.is-error` to the corresponding item and prepend the error message inside the `.mar-fb-staged-item__body`.
       - If the panel was opened because a cap was hit, prepend a `.mar-fb-staged-cap-msg` div with the text `This batch is full — submit it before staging more`.
       - All string interpolation MUST go through `escapeHtml()` (PATTERNS.md item 7).

    4. ADD `function togglePanel() { ... }`:
       - If `#mar-fb-panel-staged` exists, remove it. Otherwise call `renderPanel()`.

    5. ADD `function stagedPush(entry) { ... }`:
       - entry shape: `{stageId, intent, pageRoute, confirmationAccepted, intentDetail, locator, imageDescriptor, stagedAt}` (no File - File lives in fileMap[stageId] separately, no dataBase64).
       - Reads loadStaged(), pushes entry, saveStaged(arr), `renderChip(arr.length)`.
       - If the panel is currently open, re-render it.

    6. ADD `function stagedDelete(stageId) { ... }`:
       - Reads loadStaged(), filters out matching stageId, saveStaged, also `delete fileMap[stageId]`.
       - Updates chip count: `renderChip(loadStaged().length)`. If 0, also closes the panel.
       - If the panel is currently open, re-render it.
       - No confirm (D-12 - irrevocable).

    7. ADD `function stagedClearAll() { ... }`:
       - Calls `if (!window.confirm('Clear all staged edits? This cannot be undone.')) return;` (D-11).
       - On confirm: clearStaged(), fileMap = {}, remove chip, close panel.

    8. ADD `function batchTotals() { ... }`: returns `{count, totalBytes}` summing loadStaged(). Helper for cap checks.

    9. ADD `function exceedsCaps(extraBytes) { ... }`: returns `{exceeds:boolean, reason:string|null}`. Used by renderConfirm to decide whether to disable the Confirm button:
       ```
       var totals = batchTotals();
       if (totals.count + 1 > MAX_BATCH_EDITS) return {exceeds:true, reason:'This batch is full — submit it before staging more'};
       if (totals.totalBytes + (extraBytes || 0) > MAX_BATCH_BYTES) return {exceeds:true, reason:'This batch is full — submit it before staging more'};
       return {exceeds:false, reason:null};
       ```

    10. MODIFY the existing `renderConfirm()` function. Specifically the 'Looks right — send it' button handler (currently calls `submit()` directly, around line 590-ish - find by searching for the validateFields then submit pattern):
        - Before: button calls `validateFields()` then `submit()`.
        - After: button is RENAMED to 'Confirm and stage' (or similar) and now calls `validateFields()` then, if caps allow, builds a staged entry from `locator`, `draft.intent`, `draft.detail`, `draft.image` (File extracted to fileMap, descriptor saved to staged[]) and calls `stagedPush(entry)`. After pushing, return to STATE.IDLE (reset the per-edit state but NOT the staged[]).
        - At the top of renderConfirm, call `exceedsCaps(draft.image?.size || 0)`. If `exceeds === true`: disable the Confirm button, show the inline message above the button via `el('div', ..., escapeHtml(reason))` (D-04 / STAGE-07). The chip continues to show; the user can Submit batch from the chip to clear room.

    11. MODIFY the existing `mar-feedback-result` postMessage receiver (currently around lines 588-610). Extend the success branch: when `m.ok === true`, ALSO call `clearStaged()`, clear `fileMap = {}`, remove the chip, close the panel. Extend the error branch: if `m.errors` is an Array (Plan 03's API-03 shape), call `renderPanel({errors: m.errors})` to highlight the failing items by index. DO NOT clear staged on error (the edits represent real client work; let the user fix or remove the failing ones).

    12. PRESERVE everything else: the iframe-only guard, the existing draft/saveDraft/clearDraft localStorage logic, validateFields (now reused for pre-stage validation), the existing banner DOM, the existing reset() for per-edit state.

    13. The submit-batch wiring (POST building + postMessage) is Task 3. After this task, staging works end-to-end client-side, the chip + panel render correctly, but clicking Submit batch is wired to an undefined `submitBatch()` (Task 3 declares it).

    14. Place `function submitBatch() { /* placeholder - Task 3 implements */ }` as an empty stub at the bottom of the IIFE for now to avoid runtime errors during partial testing.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('public/feedback-inject.js','utf8'); const checks = [['function renderChip', true], ['function renderPanel', true], ['function togglePanel', true], ['function stagedPush', true], ['function stagedDelete', true], ['function stagedClearAll', true], ['function batchTotals', true], ['function exceedsCaps', true], ['mar-fb-chip', true], ['mar-fb-panel-staged', true], ['mar-fb-staged-item', true], ['Submit batch', true], ['View list', true], ['edits staged', true], ['This batch is full', true], ['Clear all staged edits', true], ['escapeHtml\\(', true]]; let bad = []; checks.forEach(([pat, want]) => { const has = new RegExp(pat).test(txt); if (has !== want) bad.push(pat); }); if (bad.length) { console.error('FAILED:', bad); process.exit(1); } console.log('OK Task 2'); " && node --check public/feedback-inject.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "function renderChip" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function renderPanel" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function togglePanel" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function stagedPush" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function stagedDelete" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function stagedClearAll" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function batchTotals" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function exceedsCaps" public/feedback-inject.js` returns 1.
    - Source: `grep -c "mar-fb-chip" public/feedback-inject.js` returns at least 5 (style rule + DOM IDs + class refs).
    - Source: `grep -c "mar-fb-panel-staged" public/feedback-inject.js` returns at least 3.
    - Source: `grep -c "This batch is full" public/feedback-inject.js` returns 1.
    - Source: `grep -c "Clear all staged edits?" public/feedback-inject.js` returns 1 (the window.confirm string).
    - Source: `grep -c "window.confirm(" public/feedback-inject.js` returns at least 1 (D-11 confirm).
    - Source: `grep -c "delete fileMap" public/feedback-inject.js` returns at least 1 (per-item delete clears the File map entry).
    - Source: `grep -c "dataBase64" public/feedback-inject.js` returns 0 (proves Tasks 1+2 did not leak base64 into persistence; the only dataBase64 references arrive in Task 3 at submit-time encoding).
    - Behavior: `node --check public/feedback-inject.js` returns exit 0.
    - Behavior: In a headless browser test (or jsdom), simulating two successful confirms followed by reading sessionStorage shows a JSON array of 2 entries, neither containing a `dataBase64` field and neither containing a value >5000 chars long.
    - Behavior: Simulating 10 successful stages followed by an 11th attempt produces `exceedsCaps()` returning `{exceeds:true}` and the renderConfirm's Confirm button has `disabled` attribute.
    - Behavior: Simulating one stage with `intentDetail.newTextEn = '<script>alert(1)</script>'` and opening the panel: the panel's innerHTML contains `&lt;script&gt;` but NOT a literal `<script>` tag (escapeHtml applied).
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 fence).
  </acceptance_criteria>
  <done>Chip + panel render correctly. Staging works. Cap UX (STAGE-06 + STAGE-07) triggers when next stage would breach 10-edit or 30MB cap. Per-item delete is irrevocable; clear-all uses window.confirm. XSS-safe via escapeHtml. dataBase64 never persisted to sessionStorage. submitBatch() is the stub from Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement submitBatch() — base64-encode files, build v2 payload, postMessage to parent</name>
  <files>public/feedback-inject.js</files>
  <read_first>
    - public/feedback-inject.js (current state after Task 2)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"public/feedback-inject.js" items 9, 10 (postMessage to parent, result receiver)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-09 (encode at submit time only)
    - src/pages/feedback.astro (lines around 167 - the parent's `mar-feedback-submit` receiver)
    - src/pages/api/feedback/submit.ts (post-Plan-03 - the v2 contract the POST body must match)
  </read_first>
  <behavior>
    - Test: Calling submitBatch() with 2 staged edits (one change-wording, one replace-photo) reads loadStaged(), reads fileMap for the photo entry, base64-encodes the File via FileReader, builds payload `{schemaVersion:2, batch:true, edits:[{intent:'change-wording', ..., image:{present:false}}, {intent:'replace-photo', ..., image:{present:true, mime:'image/jpeg', dataBase64:'...', bytes:N, originalFilename:'photo.jpg'}}]}`, and calls `window.parent.postMessage({type:'mar-feedback-submit', payload:...}, '*')` exactly ONCE.
    - Test: Before posting, state transitions to STATE.SUBMITTING; the chip's Submit batch button is disabled and shows 'Submitting…'.
    - Test: On `mar-feedback-result` with `ok:true`: state returns to STATE.DONE briefly then IDLE; chip + panel removed; sessionStorage 'mar_feedback_staged_v1' cleared; fileMap emptied.
    - Test: On `mar-feedback-result` with `ok:false, errors:[{index, error}]`: state returns to STATE.STAGED; chip + panel re-rendered with per-item error highlights; sessionStorage UNCHANGED; user can edit/delete and retry.
    - Test: On `mar-feedback-result` with `ok:false, cap:'edits'` or `cap:'bytes'`: state returns to STATE.STAGED; cap-msg banner shown in panel with the server's error message; chip stays.
    - Test: On `mar-feedback-result` with `auth: true` (existing 401 recovery): existing auth-prompt UI fires (preserved from current behavior).
    - Test: A staged edit whose photo File is missing from fileMap (e.g., page was navigated and File was lost) is REPLACED in the payload with `image:{present:false}` and the edit is flagged in a `__notice` field; the rest of the batch still submits.
    - Test: submitBatch with 0 staged edits is a no-op (no postMessage sent).
    - Test: Only ONE postMessage of type 'mar-feedback-submit' is dispatched per Submit batch click (no duplicate submissions).
  </behavior>
  <action>
    Implement the `submitBatch()` function (replacing the empty stub from Task 2). Place near the other v2 functions inside the IIFE.

    1. Guard: if loadStaged().length === 0, return early (no-op).

    2. State transition: `state = STATE.SUBMITTING;`. Disable the chip's Submit batch button and change its text to 'Submitting…' (find the chip element, query the Submit button, set `disabled=true` and `textContent='Submitting…'`).

    3. Build the `edits[]` array by mapping over loadStaged(). For each staged entry:
       - Build a plain object containing: intent, pageRoute, confirmationAccepted, intentDetail, and all locator fields (spread from entry.locator).
       - If `entry.imageDescriptor && fileMap[entry.stageId]`: base64-encode the File using a FileReader-as-Data-URL pattern wrapped in a Promise. Extract the base64 (strip the `data:.../base64,` prefix). Build `image: { present: true, mime: file.type, dataBase64: <b64>, bytes: file.size, originalFilename: file.name }`.
       - If `entry.imageDescriptor && !fileMap[entry.stageId]` (File was lost across navigation): build `image: { present: false }` and add a `__notice: 'photo file was lost across navigation; please re-attach if this edit needs the photo'` field. (Server will validate; for change-wording-only edits this is fine; for replace-photo intent this WILL fail per-edit validation → user gets a 422 with errors[index] highlighted, can re-add the photo, retry.)
       - If `!entry.imageDescriptor`: build `image: { present: false }` (no image needed for this intent).

    4. FileReader Promise wrapper (place inside submitBatch or as a helper):
       ```
       function readAsBase64(file) {
         return new Promise(function (resolve, reject) {
           var r = new FileReader();
           r.onload = function () {
             try {
               var s = String(r.result || '');
               var comma = s.indexOf(',');
               resolve(comma >= 0 ? s.slice(comma + 1) : '');
             } catch (e) { reject(e); }
           };
           r.onerror = function () { reject(new Error('FileReader error')); };
           r.readAsDataURL(file);
         });
       }
       ```
       Use `Promise.all()` to encode all photos in parallel before building the payload (this is in-memory encoding, no rate limits to worry about; the server commits sequentially per Plan 03).

    5. Build the final payload:
       ```
       var payload = {
         schemaVersion: SCHEMA_VERSION_V2,
         batch: true,
         edits: builtEdits,
       };
       ```
       Optional: if a global `__feedback_testMode` flag is desired for canary work, allow `if (window.__feedback_testMode === true) payload.testMode = true;`.

    6. Send: `window.parent.postMessage({type:'mar-feedback-submit', payload:payload}, '*');` — EXACTLY one call.

    7. The existing `message` event listener (around line 588-610) handles the `mar-feedback-result` response. EXTEND it (already touched in Task 2) with the cap-violation branch:
       - If `m.ok === true`: clear chip/panel/storage/fileMap, show success banner (use existing showBanner / equivalent), state → STATE.IDLE.
       - If `m.ok === false && Array.isArray(m.errors)`: state → STATE.STAGED; re-render the chip and panel with the error highlights (renderPanel({errors:m.errors})); re-enable the Submit batch button.
       - If `m.ok === false && m.cap`: state → STATE.STAGED; re-render the panel with the cap message at the top (renderPanel({capMessage: m.error})); re-enable Submit batch.
       - If `m.auth === true`: existing 401 recovery (untouched).
       - Else (generic error): re-enable Submit batch; show a generic error banner; state → STATE.STAGED.

    8. Catch errors during base64 encoding (FileReader.onerror): re-enable the Submit batch button, show an error banner like 'Could not read a staged photo. Try removing and re-adding it.', state → STATE.STAGED.

    9. Concurrent-submission protection: if `state === STATE.SUBMITTING`, the Submit batch button should be disabled (Task 2 disables it on click, but also guard inside submitBatch with `if (state === STATE.SUBMITTING) return;` as a defence in depth).

    10. After this task, the full v2 client flow is functional. The implementation is observable as a STAGED → SUBMITTING → DONE flow that produces ONE POST per Submit batch click with the correct v2 payload shape (STAGE-04).
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('public/feedback-inject.js','utf8'); const checks = [['function submitBatch', true], ['function readAsBase64', true], ['FileReader', true], ['readAsDataURL', true], ['schemaVersion: SCHEMA_VERSION_V2', true], ['batch: true', true], ['type: .mar-feedback-submit.', true], ['state = STATE.SUBMITTING', true], ['Submitting', true], ['Promise.all', true], ['m\\.cap', true], ['m\\.errors', true]]; let bad = []; checks.forEach(([pat, want]) => { const has = new RegExp(pat).test(txt); if (has !== want) bad.push(pat); }); if (bad.length) { console.error('FAILED:', bad); process.exit(1); } console.log('OK Task 3'); " && node --check public/feedback-inject.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "function submitBatch" public/feedback-inject.js` returns 1.
    - Source: `grep -c "function readAsBase64" public/feedback-inject.js` returns 1.
    - Source: `grep -c "FileReader" public/feedback-inject.js` returns at least 1.
    - Source: `grep -c "readAsDataURL" public/feedback-inject.js` returns 1.
    - Source: `grep -c "schemaVersion: SCHEMA_VERSION_V2" public/feedback-inject.js` returns 1.
    - Source: `grep -c "batch: true" public/feedback-inject.js` returns 1.
    - Source: `grep -c "type: 'mar-feedback-submit'" public/feedback-inject.js` returns at least 1 (v1 had this; v2 reuses the same message type per PATTERNS.md item 9).
    - Source: `grep -c "state = STATE.SUBMITTING" public/feedback-inject.js` returns 1.
    - Source: `grep -c "Submitting" public/feedback-inject.js` returns at least 1.
    - Source: `grep -c "m.cap" public/feedback-inject.js` returns at least 1 (cap-violation response branch).
    - Source: `grep -c "m.errors" public/feedback-inject.js` returns at least 1 (per-edit-errors response branch).
    - Source: `grep -c "Promise.all" public/feedback-inject.js` returns 1.
    - Source: `grep -c "dataBase64" public/feedback-inject.js` returns at most 2 (only at submit-time encoding in this task — NEVER persisted; sessionStorage writes must remain free of dataBase64 — moved from Task 2 per checker W1).
    - Behavior: `node --check public/feedback-inject.js` returns exit 0.
    - Behavior: In a jsdom test, staging 2 edits then calling submitBatch() results in exactly one `window.parent.postMessage` call whose first arg has `type === 'mar-feedback-submit'` and `payload.schemaVersion === 2 && payload.batch === true && payload.edits.length === 2`.
    - Behavior: For a staged replace-photo edit, the posted payload's edit object has `image.present === true && image.mime === <file.type> && typeof image.dataBase64 === 'string' && image.bytes === <file.size>`.
    - Behavior: After receiving `{type:'mar-feedback-result', ok:true}`, sessionStorage 'mar_feedback_staged_v1' is null (cleared), `document.getElementById('mar-fb-chip')` is null, `document.getElementById('mar-fb-panel-staged')` is null.
    - Behavior: After receiving `{type:'mar-feedback-result', ok:false, errors:[{index:1, error:'...'}]}`, sessionStorage is UNCHANGED, the chip stays visible, the panel re-renders with item index 1 having `is-error` class.
    - Behavior: Calling submitBatch() while state === STATE.SUBMITTING is a no-op (no second postMessage).
    - Behavior: submitBatch() with 0 staged edits is a no-op (no postMessage).
    - Source: `head -3 public/feedback-inject.js` does NOT include `"use strict";` directives outside the IIFE (preserve file shape).
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 fence).
  </acceptance_criteria>
  <done>submitBatch() is fully implemented. v2 POST round-trips through the existing parent (feedback.astro) postMessage receiver. Success clears staged state; per-edit errors keep staged; cap violations keep staged with cap message; auth + generic errors handled. Concurrent submissions blocked. node --check passes. OPS-02 fence respected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page DOM (user-edited content) → inject.js panel | Client-supplied strings (newTextEn, page route, file names) flow into the panel innerHTML; escapeHtml MUST run on every such interpolation. |
| sessionStorage ↔ inject.js | sessionStorage is per-origin and isolated; reading on rehydrate already wraps in try/catch (private-mode safety). |
| iframe ↔ parent (feedback.astro) → /api/feedback/submit | The existing `mar-feedback-submit` postMessage type and JSON payload shape is the contract; this plan only changes payload.shape from v1 to v2. |
| In-memory fileMap → sessionStorage | Files are NEVER serialised to sessionStorage. The descriptor (name/type/size) is — that's safe (no binary content). Files lost on navigation are degraded gracefully (server validation 422 prompts re-attach). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering / XSS | Client-supplied strings (e.g., newTextEn) rendered in the panel innerHTML without escaping → XSS in the admin's feedback iframe context | mitigate | Acceptance criterion in Task 2 explicitly tests that a `<script>` injection in newTextEn renders escaped. PATTERNS.md item 7 (escapeHtml helper) is the canonical mitigation. Every interpolation in renderPanel uses escapeHtml. |
| T-04-18 | DoS / Memory | Photo Files (up to 12 MB each, up to 10 in a batch = 120 MB worst case) held in fileMap consuming browser memory | accept | Files are held only while staged; the in-memory map is cleared on success, on per-item delete, and on browser close (sessionStorage too). 30MB total cap (STAGE-06) is the hard limit; 120MB worst case is unreachable. |
| T-04-19 | Tampering | A user manually editing sessionStorage to spoof staged entries (e.g., adding entries for routes they shouldn't be able to stage on) | accept | The server re-validates EVERY edit through validateEdit; client-side staging is convenience UX, not a trust boundary. Server is the authority. |
| T-04-20 | Information Disclosure | Photo files leaked into sessionStorage (which is per-origin and survives iframe navigation but could be inspected by browser DevTools) | mitigate | STAGE-05 + D-09: ONLY descriptors `{name,type,size}` go into sessionStorage; the File itself lives in an in-memory Map and is encoded to base64 only at submit time. Acceptance criterion checks sessionStorage value never contains 'dataBase64' or a base64 string >5000 chars. |
| T-04-21 | Repudiation | A user clicking 'Clear all' by accident loses their staged work | mitigate | D-11: window.confirm dialog before clearing. Acceptance criterion asserts the confirm call. (D-12 says per-item ❌ is irrevocable; that's a deliberate UX choice for the lighter-weight action.) |
| T-04-22 | Denial of Service / silent regression | Cached v1 inject script in browsers (the new v2 client never loads) → users see the old single-edit flow forever | mitigate | OPS-01 / Plan 06 bumps FEEDBACK_INJECT_VER from '1' to '2' which invalidates the CDN cache key. This plan SHIPS the new client; Plan 06 is the activation step. Phase 5's OPS-05 canary verifies the cache-bust took effect post-deploy. |
| T-04-23 | Tampering | postMessage from a malicious sibling iframe spoofing `mar-feedback-result` to clear the user's staged batch | mitigate | The existing receiver checks `if (ev.origin !== location.origin) return;` (preserved per PATTERNS.md item 10). Only same-origin messages are honored. |
| T-04-24 | DoS | Rapid double-clicks on 'Submit batch' producing duplicate POSTs | mitigate | Submit button is disabled on click + text changes to 'Submitting…'; submitBatch() also returns early if state === STATE.SUBMITTING. Two layers of guard. |

</threat_model>

<verification>
- `node --check public/feedback-inject.js` exits 0.
- Headless / jsdom: stage 3 edits, click Submit batch, observe exactly ONE `mar-feedback-submit` postMessage with payload `{schemaVersion:2, batch:true, edits:[3 items]}`.
- Headless: stage 11th edit, observe Confirm disabled + 'This batch is full' message in panel.
- Headless: stage edit, navigate iframe to another page, observe chip rehydrates on pageshow.
- Headless: receive `{ok:true}` result, observe chip + sessionStorage cleared.
- Headless: receive `{ok:false, errors:[...]}`, observe sessionStorage UNCHANGED + per-item error highlight.
- Headless: stage `<script>` in newTextEn, observe panel renders escaped (no script tag).
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0.
</verification>

<success_criteria>
- v2 client state machine is implemented; STAGE → loop → Submit batch flow works.
- Chip + panel UI render correctly; per-item ❌ delete (no confirm); Clear all uses window.confirm.
- Cap enforcement (STAGE-06) with limit-reached UX (STAGE-07).
- sessionStorage persists descriptors only (D-09 / STAGE-05); base64 encoded at submit time only.
- Cross-page rehydration (D-06) via pageshow/visibilitychange.
- Single POST per Submit batch click; result-receiver handles ok/errors/cap/auth/generic branches.
- XSS-safe via escapeHtml.
- node --check passes.
- OPS-02 scope fence respected.
- Emergent STAGE-06 and STAGE-07 declared and to be surfaced in 04-04-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-04-SUMMARY.md` per template. In the summary, EXPLICITLY surface the NEW requirements **STAGE-06 (client cap enforcement)** and **STAGE-07 (chip 'limit reached' UX)** for ROADMAP.md / REQUIREMENTS.md traceability-table update.
</output>
