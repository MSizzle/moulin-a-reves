---
phase: 04-batch-pipeline-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/api/feedback/validate.ts
autonomous: true
requirements:
  - API-04
user_setup: []
tags:
  - feedback
  - validator
  - extraction
  - shared-helper

must_haves:
  truths:
    - "A new module src/pages/api/feedback/validate.ts exists and exports validateEdit, signalCount, isVague, clamp, INTENTS, MAX_IMAGE_BYTES, MIN_VAGUE_LEN, MOVE_RESIZE_OPTIONS, VAGUE_STOPLIST, VAGUE_MESSAGE (D-15)."
    - "validateEdit(p) returns null on pass and an error-message string on fail; it never constructs Response objects (caller wraps responses)."
    - "Every intent-specific rule from submit.ts:148-182 is preserved byte-equivalent in validate.ts (the shared rules can never drift between v1 and v2 - D-15 / API-04)."
    - "No code outside validate.ts changes in this plan; submit.ts is untouched here so the regression surface is zero."
  artifacts:
    - path: "src/pages/api/feedback/validate.ts"
      provides: "Shared per-edit validator + helpers for v1 (existing) and v2 (next plan) code paths"
      exports: ["validateEdit", "signalCount", "isVague", "clamp", "INTENTS", "MAX_IMAGE_BYTES", "MIN_VAGUE_LEN", "MOVE_RESIZE_OPTIONS", "VAGUE_STOPLIST", "VAGUE_MESSAGE", "Intent"]
      min_lines: 80
  key_links:
    - from: "src/pages/api/feedback/validate.ts"
      to: "src/pages/api/feedback/submit.ts (current state)"
      via: "verbatim extraction of submit.ts lines 16-47, 110-130, 148-182"
      pattern: "every constant and function preserves identical behavior"
---

<objective>
Step 1 of the phase per D-15: extract per-edit validation logic into a shared helper module so both the v1 single-edit path (Plan 02) and the v2 batch path (Plan 03) consume the same rules and can never drift (API-04).

Purpose: Eliminate the structural risk of v1 and v2 validation drift before either path is touched. PATTERNS.md §"src/pages/api/feedback/validate.ts" mandates this be the first concrete code change.

Output: A new module `src/pages/api/feedback/validate.ts` exporting validation functions + shared contract constants. No existing files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@CLAUDE.md
@src/pages/api/feedback/submit.ts
@src/pages/api/feedback/clarify.ts

<interfaces>
<!-- The new module mirrors helper conventions established in submit.ts. -->
<!-- Functions to extract from submit.ts (current line ranges noted in PATTERNS.md §"submit.ts"). -->

From src/pages/api/feedback/submit.ts (current state - to be split):
- Lines 16-35: shared contract constants (SCHEMA_VERSION, INTENTS, MAX_IMAGE_BYTES, MIN_VAGUE_LEN, MOVE_RESIZE_OPTIONS, VAGUE_STOPLIST, VAGUE_MESSAGE)
- Lines 37-47: isVague(raw: unknown): boolean
- Lines 110-115: fail(message, status) — DO NOT extract (it returns Response; stays in submit.ts)
- Lines 117-119: clamp(s, max): string
- Lines 123-130: signalCount(p: any): number
- Lines 148-182: intent-specific validation block — extract to validateEdit(p): string | null

Recommended exported surface (per PATTERNS.md §"validate.ts"):
```typescript
export const INTENTS: readonly Intent[];
export type Intent = ...;
export const MAX_IMAGE_BYTES: number;
export const MIN_VAGUE_LEN: number;
export const MOVE_RESIZE_OPTIONS: readonly string[];
export const VAGUE_STOPLIST: readonly string[];
export const VAGUE_MESSAGE: string;
export function isVague(raw: unknown): boolean;
export function clamp(s: unknown, max: number): string;
export function signalCount(p: any): number;
export function validateEdit(p: any): string | null;  // null on pass; error message on fail
```

<!-- Note: SCHEMA_VERSION stays in submit.ts (it's the dispatch key, not a validation rule). -->
<!-- Note: validateEdit MUST NOT return Response objects - caller wraps with fail() or accumulates into batch errors[]. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create validate.ts with extracted helpers and constants</name>
  <files>src/pages/api/feedback/validate.ts</files>
  <read_first>
    - src/pages/api/feedback/submit.ts (full file — source for verbatim extraction)
    - src/pages/api/feedback/clarify.ts (sibling non-route helper module pattern; shows that a file under api/feedback/ may exist without `export const prerender = false;` when it has no APIRoute export)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"src/pages/api/feedback/validate.ts" (the extraction recipe)
    - CLAUDE.md §"Conventions" → "Naming Patterns" / "Code Style" (kebab-case files, camelCase functions, SCREAMING_SNAKE_CASE constants, single quotes, 2-space indent, named exports only)
  </read_first>
  <behavior>
    - Test: importing validate.ts must yield named exports `validateEdit`, `signalCount`, `isVague`, `clamp`, `INTENTS`, `MAX_IMAGE_BYTES`, `MIN_VAGUE_LEN`, `MOVE_RESIZE_OPTIONS`, `VAGUE_STOPLIST`, `VAGUE_MESSAGE` and a type export `Intent`.
    - Test: validateEdit({intent:'change-wording', pageRoute:'/', confirmationAccepted:true, intentDetail:{newTextEn:'hi', okToTranslate:true}, image:{present:false}}) returns null.
    - Test: validateEdit({intent:'replace-photo', pageRoute:'/', confirmationAccepted:true, intentDetail:{}, image:{present:true, mime:'image/png', dataBase64:'abc', bytes: 13 * 1024 * 1024}}) returns the literal string 'Replacement photo must be 12 MB or smaller'.
    - Test: validateEdit({intent:'bogus', pageRoute:'/', confirmationAccepted:true}) returns 'Unknown intent'.
    - Test: validateEdit({intent:'change-wording', pageRoute:'/', confirmationAccepted:true, intentDetail:{newTextEn:'hi'}}) returns the bilingual-required message (no okToTranslate, no fr).
    - Test: validateEdit({intent:'change-wording'}) returns 'Confirmation was not accepted' (since confirmationAccepted is not true).
    - Test: validateEdit() with missing pageRoute returns 'Missing page route'.
    - Test: signalCount({i18nKey:'a', i18nAttr:'b', imageRef:'r', galleryAttrRaw:'g', galleryIndex:0, nearbyText:'x'.repeat(30), nearestHeading:'H'}) returns 4.
    - Test: isVague('this is a long, specific sentence about something') returns false; isVague('ok') returns true; isVague('?') returns true.
  </behavior>
  <action>
    Create the new file src/pages/api/feedback/validate.ts. The file:
    1. MUST NOT contain `export const prerender = false;` (this is a helper module, not an APIRoute). NOTE (per checker W4 — disambiguation): `src/pages/api/feedback/clarify.ts` is cited in `<read_first>` as a MODULE-STRUCTURE reference (it shows the convention of helper-style code living alongside a route in the same directory), NOT as a file to copy `export const prerender = false;` from. `clarify.ts` IS a route and therefore HAS that line; `validate.ts` is NOT a route and MUST NOT have it. The acceptance criterion `grep -c "export const prerender" src/pages/api/feedback/validate.ts` returns 0 enforces this.
    2. Opens with a doc-comment block stating: "Shared per-edit validator - consumed by BOTH the v1 single-edit path AND the v2 batch path in ./submit.ts. The two paths cannot drift because the rules live here (D-15 / API-04). Mirrored client-side in public/feedback-inject.js::validateFields(). When you change a rule here, change it there too."
    3. Exports the following CONSTANTS verbatim from current submit.ts:16-35 (DO NOT include SCHEMA_VERSION - it stays in submit.ts as the dispatch key):
       - `export const INTENTS = ['change-wording', 'replace-photo', 'move-resize', 'remove', 'something-else'] as const;`
       - `export type Intent = (typeof INTENTS)[number];`
       - `export const MAX_IMAGE_BYTES = 12 * 1024 * 1024;`
       - `export const MIN_VAGUE_LEN = 25;`
       - `export const MOVE_RESIZE_OPTIONS = [...] as const;` (copy the array members verbatim from submit.ts)
       - `export const VAGUE_STOPLIST = [...];` (copy verbatim from submit.ts)
       - `export const VAGUE_MESSAGE = '...';` (copy verbatim from submit.ts - the string that begins with "Can you be more specific?")
    4. Exports `isVague(raw: unknown): boolean` verbatim from submit.ts:37-47.
    5. Exports `clamp(s: unknown, max: number): string` verbatim from submit.ts:117-119.
    6. Exports `signalCount(p: any): number` verbatim from submit.ts:123-130.
    7. Exports `validateEdit(p: any): string | null` - extracted from submit.ts:148-182. Signature returns `null` on pass, `string` error message on fail. NEVER constructs Response objects. The function body MUST include all of:
       - INTENTS.includes(intent) check returning 'Unknown intent'
       - pageRoute check returning 'Missing page route'
       - confirmationAccepted check returning 'Confirmation was not accepted'
       - 'change-wording' branch with newTextEn check and bilingual rule (okToTranslate OR newTextFr required) returning the exact strings used at submit.ts:158-167
       - 'replace-photo' branch with present, mime, dataBase64, bytes>0, bytes<=MAX_IMAGE_BYTES checks returning the same exact strings as submit.ts:168-174
       - 'move-resize' branch checking MOVE_RESIZE_OPTIONS membership and isVague(detail.detail)
       - 'remove' branch checking detail.confirmed === true returning 'Tick the confirmation box to remove this element'
       - 'something-else' branch with isVague(detail.detail)
    8. Uses 2-space indent, single quotes, trailing semicolons, named exports only. No class. No default export. No logging.
    9. DOES NOT modify submit.ts in this task - that is Plan 02's job. After this task, the new file exists in isolation; existing submit.ts continues to define its own copies and Astro continues to type-check.

    The acceptance criteria below assume the file is created and TypeScript happily compiles a unit-test consumer; the actual rewiring of submit.ts is Plan 02.
  </action>
  <verify>
    <automated>node -e "import('./src/pages/api/feedback/validate.ts').then(m => { const required = ['validateEdit','signalCount','isVague','clamp','INTENTS','MAX_IMAGE_BYTES','MIN_VAGUE_LEN','MOVE_RESIZE_OPTIONS','VAGUE_STOPLIST','VAGUE_MESSAGE']; const missing = required.filter(k => !(k in m)); if (missing.length) { console.error('MISSING EXPORTS:', missing); process.exit(1); } console.log('OK exports present'); })" 2>&1 || npx tsx -e "import * as m from './src/pages/api/feedback/validate.ts'; const required = ['validateEdit','signalCount','isVague','clamp','INTENTS','MAX_IMAGE_BYTES','MIN_VAGUE_LEN','MOVE_RESIZE_OPTIONS','VAGUE_STOPLIST','VAGUE_MESSAGE']; const missing = required.filter(k => !(k in m)); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f src/pages/api/feedback/validate.ts` exits 0.
    - Source: `grep -c "export const prerender" src/pages/api/feedback/validate.ts` returns 0 (this is NOT a route).
    - Source: `grep -c "export function validateEdit" src/pages/api/feedback/validate.ts` returns 1.
    - Source: `grep -c "export function signalCount" src/pages/api/feedback/validate.ts` returns 1.
    - Source: `grep -c "export function isVague" src/pages/api/feedback/validate.ts` returns 1.
    - Source: `grep -c "export function clamp" src/pages/api/feedback/validate.ts` returns 1.
    - Source: `grep -c "export const INTENTS" src/pages/api/feedback/validate.ts` returns 1.
    - Source: `grep -c "export const MAX_IMAGE_BYTES" src/pages/api/feedback/validate.ts` returns 1.
    - Source: `grep -E "^export (const|function|type)" src/pages/api/feedback/validate.ts | wc -l` returns at least 10 (constants + helpers + type).
    - Source: `grep -c "12 \* 1024 \* 1024" src/pages/api/feedback/validate.ts` returns 1 (the byte cap is the exact same numeric expression as submit.ts).
    - Source: `git diff main -- src/pages/api/feedback/submit.ts public/feedback-inject.js | wc -l` returns 0 (no other file is touched in this plan).
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (D-13 / OPS-02 scope fence respected).
    - Behavior: `npx astro check` (or `npm run build` in dry mode) reports no new TypeScript errors attributable to validate.ts.
    - Behavior: The doc-comment header in validate.ts contains the strings "D-15", "API-04", and "MIRROR" (case-insensitive grep).
  </acceptance_criteria>
  <done>validate.ts exists, exports the full helper surface verbatim from submit.ts:16-47, 117-130, 148-182; submit.ts is untouched; no other repo file is modified; TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New helper module ↔ existing API route | Pure-function refactor; helpers will be consumed by submit.ts in Plan 02. No trust boundary crossed in this plan (no new I/O, no new network call, no new persistence). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | validate.ts extraction drift from current submit.ts rules | mitigate | Acceptance criteria assert the byte cap numeric expression "12 * 1024 * 1024" matches and the function surface includes every required export. Plan 02's regression test (curl with v1 payload) catches any behavior drift end-to-end. |
| T-04-02 | Information Disclosure | Helper module accidentally exporting secrets or env-bound values | accept | The current submit.ts validator code reads NO env vars and references NO secrets; verbatim extraction preserves that property. validate.ts only re-exports pure-function rules. |
| T-04-03 | Repudiation | Removing the KEEP-IN-SYNC comments that bind client and server rules together | mitigate | Acceptance criteria assert the doc comment contains "MIRROR" - explicitly forces the executor to add the binding header. PATTERNS.md §"KEEP-IN-SYNC comment convention" is the canonical source. |
| T-04-04 | Denial of Service | (silent regression) Plan 01 itself shipping without Plan 02 wiring would leave the codebase with dead exports and v1 path still in submit.ts | accept | Phase-level OPS-02 verification (Plan 08) and Plan 02 wiring task close this loop. Plan 01 in isolation does not change runtime behavior so the risk is structural, not user-facing. |

</threat_model>

<verification>
- `test -f src/pages/api/feedback/validate.ts` exits 0
- `git diff main -- src/pages/api/feedback/submit.ts public/feedback-inject.js public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0
- `grep -E "^export" src/pages/api/feedback/validate.ts | wc -l` ≥ 10
- `npx astro check` reports no new errors
</verification>

<success_criteria>
- New module validate.ts exists with the complete helper surface enumerated in `<interfaces>`.
- submit.ts unchanged; no other file modified.
- OPS-02 scope fence respected (zero lines in editor-flow diff).
- Doc comment establishes the D-15 / API-04 mirror binding to feedback-inject.js.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-01-SUMMARY.md` per template.
</output>
