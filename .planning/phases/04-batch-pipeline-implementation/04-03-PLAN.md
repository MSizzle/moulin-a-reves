---
phase: 04-batch-pipeline-implementation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/pages/api/feedback/submit.ts
autonomous: true
requirements:
  - API-01
  - API-03
  - API-05
  - API-06
  - ISSUE-01
  - ISSUE-02
  - ISSUE-03
  - ISSUE-04
user_setup: []
tags:
  - feedback
  - submit
  - v2-batch
  - issue-construction
  - server-cap
  - d-03-vercel-limit

must_haves:
  truths:
    - "D-03 reconciled: the Vercel project's request-body limit has been verified (or conservatively defaulted) and `MAX_BATCH_BYTES` is set so that base64-inflated (~33%) batch JSON does not exceed the Vercel tier's limit; a `D-03` comment above the constant documents tier + chosen value."
    - "POST /api/feedback/submit with `{schemaVersion: 2, batch: true, edits: [...]}` is accepted by submit.ts (API-01)."
    - "Per-edit validation runs through the shared validateEdit() for every edit in edits[]; ANY single-edit failure rejects the whole batch with a structured per-edit-errors response of shape `{ok:false, error:'One or more edits failed validation', errors:[{index, error}]}` (API-03 / D-05)."
    - "Server re-enforces the 10-edit cap and total-photo-bytes cap (value depends on D-03 reconciliation; default 30MB if tier allows) and returns a structured cap-violation response with shape `{ok:false, error:..., cap:'edits'|'bytes', limit, actual}` (D-01 / D-02 / API-06 / D-05)."
    - "A v2 batch creates exactly ONE GitHub issue (ISSUE-01) titled `[Feedback] batch of N edits — <comma-separated unique pageRoutes, truncated to 60 chars>`; the comma-separated-unique-pageRoutes pattern naturally accommodates multi-route batches per D-07 (cross-page batching → title lists every route touched); testMode payloads prefix `[TEST]` and use the `client-feedback-test` label."
    - "The issue body contains N per-edit renderHuman() summaries separated by `---` (ISSUE-02) PLUS exactly one fenced ```json``` block holding the full {schemaVersion:2, batch:true, edits:[...]} payload (ISSUE-03)."
    - "The autonomy hint embedded in the issue body says AUTO-ELIGIBLE iff EVERY edit individually passes the per-edit autonomy gate; otherwise NEEDS-REVIEW with a failure-reason string listing which edit indices failed (ISSUE-04 / D-10)."
    - "All photos in a batch land under the same `feedback-incoming/issue-<n>/` directory (with per-edit-index filename prefix to prevent collisions, e.g. `edit-1-photo.jpg`); the issue body receives a SINGLE patchIssueBody() call after every photo has been committed (API-05 - 'NOT N PATCH calls' per PATTERNS.md item 14)."
    - "The v1 single-edit path remains byte-equivalent in observable behavior (API-02 - cached browsers keep working; D-16 - one endpoint, two shapes)."
  artifacts:
    - path: "src/pages/api/feedback/submit.ts"
      provides: "Fully-implemented v2 batch handler (handleV2Batch) replacing the Plan 02 stub"
      contains: "handleV2Batch"
    - path: "src/pages/api/feedback/submit.ts"
      provides: "MAX_BATCH_EDITS and MAX_BATCH_BYTES constants with KEEP-IN-SYNC comment binding them to feedback-inject.js, AND a D-03 comment block documenting the Vercel tier and chosen byte cap"
      contains: "MAX_BATCH_EDITS"
    - path: "src/pages/api/feedback/submit.ts"
      provides: "failBatch() helper returning structured per-edit-errors response (API-03 / D-05)"
      contains: "failBatch"
  key_links:
    - from: "handleV2Batch"
      to: "validateEdit (from ./validate)"
      via: "edits.map((e, i) => validateEdit(e)) error accumulation loop"
      pattern: "validateEdit"
    - from: "handleV2Batch"
      to: "createIssue / commitBase64File / patchIssueBody"
      via: "issue-first → photos-sequential → ONE final patchIssueBody sequencing"
      pattern: "patchIssueBody"
    - from: "handleV2Batch"
      to: "signalCount (from ./validate)"
      via: "per-edit autonomy roll-up: batchAutoEligible = every(edit pass) (D-10)"
      pattern: "signalCount"
    - from: "MAX_BATCH_BYTES (submit.ts)"
      to: "Vercel project request-body limit (verified per D-03)"
      via: "comment block above constant naming tier + chosen value"
      pattern: "D-03"
---

<objective>
Implement the v2 batch handler body in submit.ts to fulfill API-01, API-03, API-05, ISSUE-01..04, plus the emergent API-06 (server-side cap re-validation, per D-05). Additionally, reconcile D-03 (the Vercel request-body limit constraint) by verifying or conservatively defaulting `MAX_BATCH_BYTES` against the project tier.

Builds directly on Plan 02's dispatch skeleton: only the body of `handleV2Batch()` changes (plus new constants, one helper pair, and a D-03 verification step); the dispatch wiring, v1 path, and auth gate are untouched.

Purpose: Land the server half of the batch pipeline. This is the largest set of changes in the phase (~150 LOC inside submit.ts plus a D-03 verification preamble); after this plan the server understands v2 payloads and emits a valid GitHub issue, ready for Plan 04's client to call.

Output: A modified submit.ts where (1) `MAX_BATCH_BYTES` is reconciled against the Vercel project's actual request-body limit per D-03, (2) POSTing a v2 batch produces exactly one GitHub issue per the ISSUE-01..04 contract, (3) per-edit validation flows through the shared validateEdit(), (4) the edit-count and byte caps are re-enforced server-side (API-06), and (5) structured per-edit-errors responses are returned on validation failure (API-03).

**Emergent requirements declared in this plan (per D-05): API-06 (server re-validates both caps with structured per-cap error). Added to ROADMAP.md / REQUIREMENTS.md traceability table during planning iteration 1.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@.planning/phases/04-batch-pipeline-implementation/04-02-PLAN.md
@CLAUDE.md
@src/pages/api/feedback/submit.ts
@src/pages/api/feedback/validate.ts
@vercel.json
@.vercel/project.json

<interfaces>
<!-- v2 payload shape (D-16 + API-01 contract this server enforces): -->
<!--                                                                  -->
<!-- {                                                                -->
<!--   schemaVersion: 2,                                              -->
<!--   batch: true,                                                   -->
<!--   testMode?: boolean,                                            -->
<!--   edits: [                                                       -->
<!--     {                                                            -->
<!--       intent: 'change-wording' | 'replace-photo' | 'move-resize' | 'remove' | 'something-else', -->
<!--       pageRoute: string,                                         -->
<!--       confirmationAccepted: true,                                -->
<!--       intentDetail: { ... },                                     -->
<!--       image?: { present: boolean, mime?: string, dataBase64?: string, bytes?: number, originalFilename?: string }, -->
<!--       // ... all locator fields (i18nKey, i18nAttr, imageRef, galleryAttrRaw, galleryIndex, nearbyText, nearestHeading, etc.) -->
<!--     },                                                           -->
<!--     ...                                                          -->
<!--   ]                                                              -->
<!-- }                                                                -->

<!-- v2 response shapes: -->
<!--   200: { ok: true, issueNumber: number, issueUrl: string }       -->
<!--   400: { ok: false, error: 'Invalid request body' }              -->
<!--   401: { ok: false, error: 'Unauthorized' }                      -->
<!--   422 (cap violation): { ok: false, error: 'Batch exceeds N-edit limit', cap: 'edits', limit: 10, actual: <n> } -->
<!--   422 (cap violation bytes): { ok: false, error: 'Batch exceeds <X> MB total photo size', cap: 'bytes', limit: <bytes>, actual: <n> } -->
<!--   422 (per-edit validation): { ok: false, error: 'One or more edits failed validation', errors: [{index, error}, ...] } -->
<!--   500: { ok: false, error: ...message... }                       -->
<!--   502: { ok: false, error: 'Issue creation failed' | upstream }  -->

<!-- v2 issue contract (ISSUE-01..04): -->
<!--   Title: `${testMode?'[TEST] ':''}[Feedback] batch of ${N} edits — ${uniqueRoutesCsv.slice(0,60)}` then overall slice(0,79)+'…' if >80 chars -->
<!--   Labels: testMode ? ['client-feedback-test'] : ['client-feedback'] -->
<!--   Body:                                                          -->
<!--     `**A client left feedback on the live site (${N} edits).**`   -->
<!--     '' '---' ''                                                  -->
<!--     `### Edit 1 of N` + renderHuman(e1, e1.intent) (joined by '\n\n---\n\n') -->
<!--     '' '---' ''                                                  -->
<!--     '<!-- machine-readable feedback payload — do not edit by hand -->' -->
<!--     '```json' + JSON.stringify({schemaVersion:2, batch:true, edits:machineEdits}, null, 2) + '```' -->
<!--     '' + autonomy hint (AUTO-ELIGIBLE iff EVERY edit auto-eligible; NEEDS-REVIEW with failedIndexes otherwise) -->

<!-- Photo commit shape (API-05): -->
<!--   Sequential commitBase64File calls, paths = `feedback-incoming/issue-${N}/edit-${i+1}-${safeName}` -->
<!--   After ALL photos committed: ONE patchIssueBody() call with the final body (paths populated). -->
<!--   NOT N PATCH calls - PATTERNS.md item 14 is explicit. -->

<!-- D-03 reconciliation matrix (Task 1 outcome): -->
<!--   Vercel Hobby tier: request-body limit ~4.5 MB → MAX_BATCH_BYTES = 3 * 1024 * 1024 (3 MB raw → ~4 MB base64) -->
<!--   Vercel Pro tier:   request-body limit ~4.5 MB by default, configurable up to ~50 MB via maxDuration tuning -->
<!--   Vercel Enterprise: ≥40 MB allowed → MAX_BATCH_BYTES = 30 * 1024 * 1024 (D-02 default) -->
<!--   Unverifiable in session: default to Hobby-safe 3 MB AND mark task autonomous:false for operator confirm -->
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: D-03 reconciliation — verify Vercel function request-body limit and lock MAX_BATCH_BYTES</name>
  <files>src/pages/api/feedback/submit.ts</files>
  <read_first>
    - vercel.json (project root — current config; expected to have NO explicit body-size override)
    - .vercel/project.json (project ID `prj_RIo7DcBIYysCuz9zhnI71u1Ifrb4` and team `team_mYKN6qZrDebsHJGJaiN7XqrY`)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-03 (the mandate for this task)
    - CLAUDE.md §"Platform Requirements" → Vercel deploy bullet
    - Vercel docs reference URL: https://vercel.com/docs/functions/runtimes#request-body-size
  </read_first>
  <behavior>
    - Test: `submit.ts` defines `const MAX_BATCH_BYTES = <N> * 1024 * 1024;` where N is one of {3, 4, 12, 30} based on the reconciliation outcome.
    - Test: Immediately above the constant, a comment block of the form `// D-03: Vercel <tier> tier request-body limit is <L> MB; MAX_BATCH_BYTES set to <X> MB so base64-inflated wire size (~<X*1.33> MB) stays under the limit.` exists.
    - Test: `grep -c "D-03" src/pages/api/feedback/submit.ts` returns at least 1.
    - Test: The chosen value MUST match the value Plan 04 (client) mirrors in `public/feedback-inject.js` (KEEP-IN-SYNC convention preserved). If this task changes the default from 30 MB, Plan 04's MAX_BATCH_BYTES must be updated to match in the same iteration — surface this requirement in 04-03-SUMMARY.md.
  </behavior>
  <action>
    Perform the D-03 verification + reconciliation step BEFORE writing the v2 batch handler. The outcome of this task locks the cap value used by Tasks 2 and 3 (and by Plan 04's client mirror).

    1. **Read vercel.json:** Confirm there is NO explicit `maxDuration`, `memory`, or body-size override that would alter the default limit. Current vercel.json (per pre-task read) contains only `rewrites` + `redirects` — no function-config keys.

    2. **Read .vercel/project.json:** Confirm the project ID is `prj_RIo7DcBIYysCuz9zhnI71u1Ifrb4` and team `team_mYKN6qZrDebsHJGJaiN7XqrY`. These match `MSizzle/moulin-a-reves`.

    3. **Determine the Vercel project tier.** Three sub-paths in priority order:
       a. **Preferred — `vercel inspect` CLI** (if authenticated locally): run `vercel inspect prj_RIo7DcBIYysCuz9zhnI71u1Ifrb4 --scope team_mYKN6qZrDebsHJGJaiN7XqrY` or `vercel teams list` + `vercel project ls` to surface the tier. Capture the tier in the comment block.
       b. **Fallback — dashboard URL:** if CLI is unavailable in this session, document in the comment block that the operator must confirm at `https://vercel.com/MSizzle/moulin-a-reves/settings/general` (or the team-scoped equivalent) before merge, and DEFAULT to the conservative Hobby value (3 MB raw → ~4 MB on the wire) so the constant ships safe.
       c. **Operator-marked branch:** if (a) and (b) both fail, mark this task `autonomous: false` (change the task-level attribute, document inline why) and STOP — the operator must run `vercel inspect` and tell Claude the tier before continuing to Task 2.

    4. **Reconcile MAX_BATCH_BYTES against the tier:**

       | Vercel tier | Request-body limit (default) | MAX_BATCH_BYTES (raw photo total) |
       |-------------|-------------------------------|------------------------------------|
       | Hobby       | ~4.5 MB                       | `3 * 1024 * 1024` (~3 MB raw → ~4 MB base64) |
       | Pro (default) | ~4.5 MB                     | `3 * 1024 * 1024` (same as Hobby unless function override is set) |
       | Pro (tuned via fluid/compute settings) | up to ~50 MB | `30 * 1024 * 1024` (D-02 default) only if explicitly tuned |
       | Enterprise  | ≥40 MB                        | `30 * 1024 * 1024` (D-02 default) |
       | Unverifiable | (treat as Hobby)             | `3 * 1024 * 1024` (Hobby-safe default) |

       Pick the row that matches the verified tier. If the chosen value differs from the 30 MB default declared in CONTEXT.md D-02, surface the divergence in 04-03-SUMMARY.md so Plan 04's mirror constant is updated in the same iteration.

    5. **Insert the comment + constant into `submit.ts`.** The placement is the same KEEP-IN-SYNC block where Plan 02 added `SCHEMA_VERSION_V2` (right after that constant). Use this exact format (substitute `<tier>`, `<L>`, `<X>`):

       ```
       // D-03: Vercel <tier> tier request-body limit is <L> MB.
       // MAX_BATCH_BYTES set to <X> MB so base64-inflated wire size (~<X*1.33> MB) stays under the limit.
       // KEEP IN SYNC with MAX_BATCH_BYTES in public/feedback-inject.js (STAGE-06 client mirror).
       const MAX_BATCH_BYTES = <X> * 1024 * 1024;
       ```

       (Do NOT declare `MAX_BATCH_EDITS = 10` in this task — that lands in Task 2 with the rest of the v2 batch input gates. Task 1 declares ONLY `MAX_BATCH_BYTES` plus its D-03 comment so the value is locked before downstream tasks reference it.)

    6. **Do NOT modify any other part of submit.ts in this task.** Tasks 2 and 3 add the rest of the v2 handler; Task 1 only locks the byte cap and its provenance.

    7. After this task, surface the chosen value + tier in the 04-03-SUMMARY.md preamble so:
       - Plan 04 (client) knows whether to keep 30 MB or lower to match.
       - Plan 08's integration smoke knows the actual byte-cap value to use in cap-violation tests.
       - The operator has a one-line audit trail for D-03 in the PR description.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('src/pages/api/feedback/submit.ts','utf8'); if (!/D-03/.test(txt)) { console.error('FAIL: no D-03 comment in submit.ts'); process.exit(1); } if (!/const MAX_BATCH_BYTES = \d+ \* 1024 \* 1024/.test(txt)) { console.error('FAIL: MAX_BATCH_BYTES constant not declared with expected pattern'); process.exit(1); } const m = txt.match(/const MAX_BATCH_BYTES = (\d+) \* 1024 \* 1024/); if (!m) { console.error('FAIL: cannot parse MAX_BATCH_BYTES value'); process.exit(1); } const v = parseInt(m[1], 10); if (![3, 4, 12, 30].includes(v)) { console.error('FAIL: MAX_BATCH_BYTES value ' + v + ' is not in the D-03 reconciliation matrix {3,4,12,30}'); process.exit(1); } console.log('OK D-03: MAX_BATCH_BYTES = ' + v + ' MB'); "</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "D-03" src/pages/api/feedback/submit.ts` returns at least 1.
    - Source: `grep -cE "const MAX_BATCH_BYTES = (3|4|12|30) \\* 1024 \\* 1024" src/pages/api/feedback/submit.ts` returns 1.
    - Source: The comment block above `MAX_BATCH_BYTES` contains the literal string "Vercel" and the literal string "tier".
    - Source: The comment block contains "KEEP IN SYNC" (matching the existing KEEP-IN-SYNC convention; Plan 04 client mirror must match this exact value).
    - Behavior: `npx astro check` reports no new errors (constant + comment are valid TypeScript).
    - Behavior: If the chosen value is NOT 30 MB (the D-02 default), 04-03-SUMMARY.md MUST flag this to Plan 04 so the client constant updates in the same iteration.
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 fence).
  </acceptance_criteria>
  <done>D-03 reconciled: the Vercel project tier is verified (or conservatively defaulted with operator follow-up flagged), MAX_BATCH_BYTES is locked with a documenting comment, and Plan 04's mirror value is known. Downstream tasks (2, 3) and Plan 04 reference the chosen value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add v2 constants (MAX_BATCH_EDITS, fail helpers), input gates, AND issue construction</name>
  <files>src/pages/api/feedback/submit.ts</files>
  <read_first>
    - src/pages/api/feedback/submit.ts (current state after Task 1 — MAX_BATCH_BYTES is locked with its D-03 comment)
    - src/pages/api/feedback/validate.ts (for the imports)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"submit.ts" items 3, 6, 8, 10, 11, 12, 13, 14 (cap constants, failBatch shape, validation loop, signalCount autonomy roll-up, GitHub helpers, body construction, title construction, issue-first sequencing)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-01, D-05, D-07 (multi-route title), D-10 (per-edit autonomy thresholds, no batch redesign), D-17 (bilingual per-edit)
    - .planning/REQUIREMENTS.md ISSUE-01, ISSUE-02, ISSUE-03, ISSUE-04
  </read_first>
  <behavior>
    - Test: POSTing a v2 payload with 11 edits returns HTTP 422 with body containing `cap:"edits"`, `limit:10`, `actual:11`, and the literal string 'Batch exceeds 10-edit limit'.
    - Test: POSTing a v2 payload with edits whose summed `image.bytes` exceeds MAX_BATCH_BYTES returns HTTP 422 with body containing `cap:"bytes"`, `limit:<MAX_BATCH_BYTES>`, `actual:<sum>`, and 'Batch exceeds <N> MB total photo size' (where N is MAX_BATCH_BYTES / (1024*1024)).
    - Test: POSTing a v2 payload where `edits` is not an array returns HTTP 422 with body containing 'edits must be a non-empty array'.
    - Test: POSTing a v2 payload with 0 edits returns HTTP 422 with the same 'edits must be a non-empty array' message.
    - Test: POSTing a v2 payload with `[{intent:'change-wording', pageRoute:'/', confirmationAccepted:true, intentDetail:{}}, {intent:'replace-photo', pageRoute:'/', confirmationAccepted:true, intentDetail:{}, image:{present:false}}]` returns HTTP 422 with body shape `{ok:false, error:'One or more edits failed validation', errors:[{index:0, error:'New wording (English) is required'},{index:1, error:'A replacement photo is required'}]}`.
    - Test: Caps are checked BEFORE per-edit validation (so an oversize batch fails fast without enumerating every per-edit error).
    - Test: POSTing a v2 payload of 3 valid edits returns HTTP 200 with body `{ok:true, issueNumber:<n>, issueUrl:'https://...'}` and the created issue's title matches `/^\\[Feedback\\] batch of 3 edits — /`.
    - Test: With `testMode:true` the title is prefixed `[TEST]` and the label is `client-feedback-test`.
    - Test: A multi-route batch with edits on `/` and `/homes/le-moulin/` produces a title containing `/, /homes/le-moulin/` (or the truncated form if longer than 60 chars).
    - Test: Title overall length is ≤ 80 chars (truncated with `…` if exceeded).
    - Test: Issue body contains exactly N renderHuman() blocks separated by `\n\n---\n\n`, each prefixed with `### Edit X of N`.
    - Test: Issue body contains exactly one fenced ```json``` block whose parsed content equals `{schemaVersion:2, batch:true, edits:[...]}` (verified by extracting the block, JSON.parse, and asserting shape).
    - Test: For a batch where EVERY edit passes signalCount-based autonomy (intent change-wording/replace-photo AND signalCount≥2), the autonomy hint string starts with `Autonomy hint: AUTO-ELIGIBLE`.
    - Test: For a batch where ANY edit fails, the autonomy hint string starts with `Autonomy hint: NEEDS-REVIEW` and contains a `#N` reference for each failing index (e.g., `#2, #4`).
  </behavior>
  <action>
    Extend submit.ts (do NOT touch handleV1 or any helper other than as listed). This task merges what were previously Task 1 (constants + gates) and Task 2 (issue construction) of the pre-revision plan, per Warning W3 — fewer broken-state surfaces during execution.

    **Sub-step A: Constants + failure helpers**

    1. ADD a single new constant immediately after the `MAX_BATCH_BYTES` block from Task 1, within the same KEEP-IN-SYNC commented block. Use a doc comment that reads "Batch edit-count cap (D-01 / API-06). Mirror this EXACTLY in public/feedback-inject.js (STAGE-06).":
       `const MAX_BATCH_EDITS = 10;            // D-01`
       (MAX_BATCH_BYTES was added in Task 1 with the D-03 comment block; do NOT redeclare it.)

    2. ADD a new helper function right after the existing `fail()`:
       `function failBatch(errors: Array<{ index: number; error: string }>, status = 422): Response { ... }`
       Returns `new Response(JSON.stringify({ ok: false, error: 'One or more edits failed validation', errors }), { status, headers: { 'Content-Type': 'application/json' } })`.

    3. ADD a second helper for cap violations right after failBatch():
       `function failCap(cap: 'edits' | 'bytes', limit: number, actual: number, message: string, status = 422): Response { ... }`
       Returns `new Response(JSON.stringify({ ok: false, error: message, cap, limit, actual }), { status, headers: { 'Content-Type': 'application/json' } })`.

    **Sub-step B: handleV2Batch input gates**

    4. REPLACE the body of `handleV2Batch(p)` (currently the Plan 02 stub) with the input-gate stage. Photo commit lands in Task 3; the issue construction (sub-step C) lands inside the same try/catch in this task. Specifically:
       - If `!Array.isArray(p.edits) || p.edits.length === 0` → `return fail('edits must be a non-empty array')`.
       - If `p.edits.length > MAX_BATCH_EDITS` → `return failCap('edits', MAX_BATCH_EDITS, p.edits.length, 'Batch exceeds 10-edit limit')`.
       - Compute `totalBytes = p.edits.reduce((sum, e) => sum + (e?.image?.bytes ?? 0), 0)`. If `totalBytes > MAX_BATCH_BYTES` → `return failCap('bytes', MAX_BATCH_BYTES, totalBytes, 'Batch exceeds ' + (MAX_BATCH_BYTES / (1024*1024)) + ' MB total photo size')`.
       - Per-edit validation loop: `const errors = []; p.edits.forEach((e, index) => { const err = validateEdit(e); if (err) errors.push({ index, error: err }); }); if (errors.length > 0) return failBatch(errors);`.

    5. Cap checks MUST run BEFORE per-edit validation (so the response order is: shape → caps → per-edit). This matches D-04 UX ("batch is full" is a fast signal).

    **Sub-step C: Issue construction (title, labels, body, autonomy hint, createIssue call)**

    6. NORMALISE each edit through the existing locator-normalise logic (use the SAME field clamps + lengths the v1 path uses; inline the same clamp() calls per edit and produce a `machineEdit` object with the same shape v1 produces at submit.ts:184-216 area). Each machineEdit retains: `intent`, `pageRoute` (clamped 200), all the locator fields (clamped per the v1 path), `intentDetail` (with okToTranslate, newTextEn/Fr clamped per the v1 path), `image: imageMeta(null, null)` placeholder (paths populated AFTER commit in Task 3).

    7. COMPUTE per-edit autonomy roll-up:
       ```
       const perEdit = p.edits.map((e, i) => {
         const sigs = signalCount(e);
         const auto = (e.intent === 'change-wording' || e.intent === 'replace-photo') && sigs >= 2;
         return { sigs, auto };
       });
       const batchAuto = perEdit.every(x => x.auto);
       const failedIndexes = perEdit.map((x, i) => x.auto ? null : i + 1).filter(x => x !== null);
       ```

    8. BUILD title (ISSUE-01):
       ```
       const uniqueRoutes = Array.from(new Set(p.edits.map(e => String(e.pageRoute))));
       const routeList = uniqueRoutes.join(', ').slice(0, 60);
       let title = `${p.testMode ? '[TEST] ' : ''}[Feedback] batch of ${p.edits.length} edits — ${routeList}`;
       if (title.length > 80) title = title.slice(0, 79) + '…';
       const labels = p.testMode ? ['client-feedback-test'] : ['client-feedback'];
       ```

    9. BUILD body factory `buildBatchBody(machineEdits)` (closure over `failedIndexes`, `batchAuto`, `p.edits.length`):
       - Header line: `**A client left feedback on the live site (${N} edits).**` (literal, including the period).
       - Then `''`, `'---'`, `''`.
       - Then for each edit i: `### Edit ${i+1} of ${N}\n\n${renderHuman(p.edits[i], p.edits[i].intent)}`, joined by `'\n\n---\n\n'`.
       - Then `''`, `'---'`, `''`.
       - Then `'<!-- machine-readable feedback payload — do not edit by hand -->'`.
       - Then `'```json'` on its own line.
       - Then `JSON.stringify({schemaVersion: 2, batch: true, edits: machineEdits}, null, 2)`.
       - Then ```` '```' ```` on its own line (closing fence).
       - Then `''`.
       - Then the autonomy hint string:
         - `batchAuto`: `Autonomy hint: AUTO-ELIGIBLE (all ${N} edits pass per-edit gate). Verify against the autonomy gate in .github/CLAUDE_FEEDBACK.md before labelling 'auto-approved'.`
         - otherwise: `Autonomy hint: NEEDS-REVIEW (${failedIndexes.length} of ${N} edits failed the per-edit gate: ${failedIndexes.map(i => '#' + i).join(', ')}). Open a PR, do not auto-merge.`
       - Join everything with `'\n'`.

    10. WRAP in the existing top-level try/catch shape (mirror handleV1's pattern):
        ```
        try {
          const placeholderMachineEdits = p.edits.map((e, i) => ({ /* normalised */, image: imageMeta(null, null, e) }));
          const initialBody = buildBatchBody(placeholderMachineEdits);
          const issue = await createIssue(title, initialBody, labels);
          const issueNumber: number = issue.number;
          const issueUrl: string = issue.html_url;
          // ... Task 3 inserts the photo loop + final patchIssueBody here.
          return new Response(JSON.stringify({ ok: true, issueNumber, issueUrl }), { status: 200, headers: { 'Content-Type': 'application/json' } });
        } catch (err: any) {
          return new Response(JSON.stringify({ ok: false, error: err?.message || 'Server error' }), { status: 500, headers: { 'Content-Type': 'application/json' } });
        }
        ```

    11. Use the EXISTING `createIssue`, `renderHuman`, `imageMeta`, `signalCount` (imported from validate.ts), and `clamp` (imported from validate.ts) helpers. DO NOT add new GitHub helpers (PATTERNS.md item 11 - no batch-commit helper).

    12. After this task, the v2 path returns 200 with an issue created. Photos are NOT yet committed (image paths in the JSON block are placeholder nulls). Task 3 adds the photo loop + final PATCH.

    13. Preserve `export const prerender = false;` first line; preserve auth gate; preserve v1 path unchanged.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('src/pages/api/feedback/submit.ts','utf8'); const checks = [['const MAX_BATCH_EDITS = 10', true], ['const MAX_BATCH_BYTES = (3|4|12|30) \\* 1024 \\* 1024', true], ['function failBatch', true], ['function failCap', true], ['edits must be a non-empty array', true], ['Batch exceeds 10-edit limit', true], ['Batch exceeds .* MB total photo size', true], ['validateEdit\\(e\\)', true], ['Autonomy hint: AUTO-ELIGIBLE', true], ['Autonomy hint: NEEDS-REVIEW', true], ['\\[Feedback\\] batch of', true], ['createIssue\\(title, ', true], ['client-feedback-test', true], ['machine-readable feedback payload', true], ['signalCount\\(e\\)', true]]; let bad = []; checks.forEach(([pat, want]) => { const has = new RegExp(pat).test(txt); if (has !== want) bad.push(pat); }); if (bad.length) { console.error('FAILED:', bad); process.exit(1); } console.log('OK Task 2'); "</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "const MAX_BATCH_EDITS = 10" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -cE "const MAX_BATCH_BYTES = (3|4|12|30) \\* 1024 \\* 1024" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "function failBatch" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "function failCap" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "edits must be a non-empty array" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "Batch exceeds 10-edit limit" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -cE "Batch exceeds .* MB total photo size" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "validateEdit(e)" src/pages/api/feedback/submit.ts` returns at least 1.
    - Source: `grep -c "Autonomy hint: AUTO-ELIGIBLE" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "Autonomy hint: NEEDS-REVIEW" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "\\[Feedback\\] batch of" src/pages/api/feedback/submit.ts` returns 1.
    - Source: `grep -c "machine-readable feedback payload" src/pages/api/feedback/submit.ts` returns at least 1 (handleV1 has one; handleV2Batch has one).
    - Source: `grep -c "client-feedback-test" src/pages/api/feedback/submit.ts` returns at least 1.
    - Source: `grep -c "signalCount" src/pages/api/feedback/submit.ts` returns at least 1.
    - Source: `grep -c "createIssue(title" src/pages/api/feedback/submit.ts` returns at least 2 (v1 + v2).
    - Behavior: POST v2 with 11 edits returns HTTP 422 + body containing `"cap":"edits"`.
    - Behavior: POST v2 with totalBytes > MAX_BATCH_BYTES returns HTTP 422 + body containing `"cap":"bytes"`.
    - Behavior: POST v2 with 2 edits where edit #2 fails validation returns HTTP 422 + body containing `"errors":[{"index":1,"error":...}]`.
    - Behavior: POST v2 with 3 valid edits, mocked GitHub → response HTTP 200 with `{ok:true, issueNumber:N, issueUrl:'https://...'}`.
    - Behavior: Issue title (captured from the mocked createIssue call) matches `^[Feedback] batch of 3 edits — `.
    - Behavior: Issue body string contains exactly N occurrences of `### Edit ` (where N = edit count) and exactly one occurrence of `\n\`\`\`json\n` (the opening fence).
    - Behavior: Extracting the JSON block from the body and JSON.parse-ing it yields `{schemaVersion:2, batch:true, edits:[...]}` with `edits.length === N`.
    - Behavior: For a batch where all edits have intent change-wording + signalCount≥2, body contains `Autonomy hint: AUTO-ELIGIBLE`. For a batch with one failing edit, body contains `Autonomy hint: NEEDS-REVIEW` and `#<index>` for the failing index.
    - Behavior: For testMode=true, captured title starts `[TEST] [Feedback] batch of`; labels arg is `['client-feedback-test']`.
    - Behavior: v1 happy-path POST still returns its pre-existing response shape (regression check).
  </acceptance_criteria>
  <done>Constants, helpers, input gates, AND issue construction land in handleV2Batch. Caps + per-edit validation flow correctly. The v2 path returns 200 with issueNumber/issueUrl. Photo commit (Task 3) follows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sequential photo commits to feedback-incoming/issue-N/ with single final patchIssueBody</name>
  <files>src/pages/api/feedback/submit.ts</files>
  <read_first>
    - src/pages/api/feedback/submit.ts (current state after Task 2)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"submit.ts" items 14, 15, 16 (issue-first sequencing, SHA256 hash, safe filename)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"code_context" (`feedback-incoming/issue-<n>/<file>` rule, repo root, .vercelignore)
    - CLAUDE.md §"Architectural Constraints" → "Feedback mode" → "feedback-incoming/ rule"
    - .planning/REQUIREMENTS.md API-05 (same directory, single PATCH after all photos committed)
  </read_first>
  <behavior>
    - Test: POSTing a v2 payload with edits #1 and #3 carrying `image.present === true` (edits #2 and #4 photo-less) commits exactly 2 files to GitHub, both under `feedback-incoming/issue-<N>/`, with filenames `edit-1-<safeName>` and `edit-3-<safeName>`. No file is committed for photo-less edits.
    - Test: Two edits in the batch with identical `image.originalFilename` of `photo.jpg` produce distinct paths `feedback-incoming/issue-<N>/edit-1-photo.jpg` and `feedback-incoming/issue-<N>/edit-3-photo.jpg` (no clobber per the per-edit-index prefix).
    - Test: After all photos commit, EXACTLY ONE patchIssueBody call updates the issue with the final body where `edits[i].image.committedPath` and `sha256` are populated (no longer null) for the photo-bearing edits.
    - Test: If commitBase64File fails for one photo, the handler still completes — the final body keeps that edit's `committedPath:null` and a `commitError` note (best-effort, mirroring v1's graceful fallback at submit.ts:300-310 area).
    - Test: A safe-filename with weird chars (e.g. `My Photo!.JPG`) is sanitised to `My-Photo-.JPG` (or similar - same regex as v1: `/[^a-zA-Z0-9._-]+/g → '-'`, trim leading/trailing dashes).
    - Test: SHA256 hash failure (e.g., crypto unavailable) is caught and the body's `sha256: null` continues; the issue still completes successfully.
  </behavior>
  <action>
    Add the photo-commit loop + final body PATCH inside the try block of handleV2Batch, between `const issue = await createIssue(...)` and `return new Response({ok:true, ...})`. Specifically:

    1. After the issue is created and `issueNumber` is known, BUILD the per-edit photo descriptors and commit them SEQUENTIALLY (NOT Promise.all per PATTERNS.md §"Shared Patterns" → "GitHub Contents API: get-SHA-then-PUT"):
       ```
       const finalMachineEdits = [];
       for (let i = 0; i < p.edits.length; i++) {
         const e = p.edits[i];
         const img = e.image || { present: false };
         let committedPath: string | null = null;
         let sha256: string | null = null;
         let commitError: string | null = null;
         if (img.present && img.dataBase64) {
           // sanitise filename, mirror v1 submit.ts:288-292
           const safeName = (clamp(img.originalFilename, 120) || 'upload')
             .replace(/[^a-zA-Z0-9._-]+/g, '-')
             .replace(/^-+|-+$/g, '') || 'upload';
           // per-edit collision-avoidance prefix
           const candidatePath = `feedback-incoming/issue-${issueNumber}/edit-${i + 1}-${safeName}`;
           // SHA256 best-effort
           try {
             const b64 = String(img.dataBase64);
             const bytes = Uint8Array.from(atob(b64), c => c.charCodeAt(0));
             const digest = await crypto.subtle.digest('SHA-256', bytes);
             sha256 = Array.from(new Uint8Array(digest)).map(x => x.toString(16).padStart(2, '0')).join('');
           } catch { sha256 = null; }
           // Sequential commit
           const ok = await commitBase64File(candidatePath, String(img.dataBase64), `chore(feedback): add photo for issue #${issueNumber} edit ${i + 1}`);
           if (ok) committedPath = candidatePath;
           else commitError = 'photo commit failed';
         }
         finalMachineEdits.push({
           ...placeholderMachineEdits[i],
           image: imageMeta(committedPath, sha256, e),
           ...(commitError ? { commitError } : {}),
         });
       }
       ```

    2. After the loop, REBUILD the final body via `buildBatchBody(finalMachineEdits)` and PATCH the issue ONCE:
       ```
       await patchIssueBody(issueNumber, buildBatchBody(finalMachineEdits));
       ```
       **EXACTLY ONE patchIssueBody call per batch — API-05 is explicit. NOT N calls.**

    3. Return `new Response(JSON.stringify({ ok: true, issueNumber, issueUrl }), { status: 200, ... })`.

    4. The top-level try/catch (from Task 2) wraps the whole thing; any GitHub fetch failure surfaces as a 500 with the error message.

    5. NO Promise.all on commits (rate-limit + shared-SHA risk per PATTERNS.md). Sequential await loop only.

    6. NO new GitHub helper. Use existing commitBase64File and patchIssueBody as-is.

    7. The placeholder body created in Task 2 is OVERWRITTEN by the final body in this PATCH; only the final issue state (with photo paths and sha256s populated) is observed by the Action.

    8. Preserve `export const prerender = false;` first line; preserve auth gate; preserve v1 path unchanged.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('src/pages/api/feedback/submit.ts','utf8'); const checks = [['feedback-incoming/issue-\\$\\{issueNumber\\}/edit-', true], ['commitBase64File\\(candidatePath', true], ['patchIssueBody\\(issueNumber, buildBatchBody', true], ['for \\(let i = 0; i < p\\.edits\\.length', true], ['\\.replace\\(\\/\\[\\^a-zA-Z0-9._-\\]\\+\\/g, ', true], ['crypto\\.subtle\\.digest', true]]; let bad = []; checks.forEach(([pat, want]) => { const has = new RegExp(pat).test(txt); if (has !== want) bad.push(pat); }); if (bad.length) { console.error('FAILED:', bad); process.exit(1); } console.log('OK Task 3'); "</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "feedback-incoming/issue-\\\${issueNumber}/edit-" src/pages/api/feedback/submit.ts` returns 1 (per-edit-index prefix lives in v2 only).
    - Source: `grep -c "patchIssueBody(issueNumber, buildBatchBody" src/pages/api/feedback/submit.ts` returns 1 (EXACTLY ONE patch call in v2 - API-05).
    - Source: `grep -c "for (let i = 0; i < p.edits.length" src/pages/api/feedback/submit.ts` returns 1 (sequential loop, not Promise.all).
    - Source: `grep -c "Promise.all" src/pages/api/feedback/submit.ts` returns 0 (NO concurrent photo commits in v2; sequential mandated by PATTERNS.md §"Shared Patterns").
    - Source: `grep -c "crypto.subtle.digest" src/pages/api/feedback/submit.ts` returns at least 1 (v1 already has this; v2 also uses SHA256).
    - Behavior: POST v2 with 3 edits, edits #1 and #3 photo-bearing, mocked GitHub → exactly 2 commitBase64File calls made, paths `feedback-incoming/issue-<N>/edit-1-<...>` and `feedback-incoming/issue-<N>/edit-3-<...>`.
    - Behavior: Exactly 1 patchIssueBody call made (count GitHub PATCH requests).
    - Behavior: Final body string in the patchIssueBody call, when its JSON block is parsed, has `edits[0].image.committedPath === 'feedback-incoming/issue-<N>/edit-1-<safeName>'` and `edits[2].image.committedPath === 'feedback-incoming/issue-<N>/edit-3-<safeName>'`, and `edits[1].image.committedPath === null` (no photo on edit #2).
    - Behavior: Two edits with `originalFilename:'photo.jpg'` at indexes 0 and 2 produce paths ending `/edit-1-photo.jpg` and `/edit-3-photo.jpg` (no clobber).
    - Behavior: POST v2 where commitBase64File fails for one photo still returns 200; the failed edit's `commitError` field is populated and `committedPath` stays null.
    - Behavior: POST v2 with 0 photo-bearing edits makes 0 commitBase64File calls and still patches the body exactly once (with all `committedPath:null`).
    - Behavior: v1 happy-path POST is still byte-equivalent (regression check).
    - Source: `head -1 src/pages/api/feedback/submit.ts` equals `export const prerender = false;`.
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 scope fence).
    - Behavior: `npx astro check` reports no new TypeScript errors.
  </acceptance_criteria>
  <done>Photos for a v2 batch land in feedback-incoming/issue-N/ with per-edit-index prefix; exactly ONE patchIssueBody finalises the body with all paths and hashes; sequential commits; v1 unchanged; OPS-02 fence respected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted client (browser) → POST /api/feedback/submit (v2 path) | Hostile JSON payloads can carry oversized batches, malformed schemaVersion/batch combinations, crafted edit fields aiming for prompt injection, or filenames designed for path traversal. |
| Submit handler → GitHub Issues API | Server controls the integer issue number, title (truncated), body (server-constructed). The body INCLUDES client-supplied JSON inside a fenced ```json``` block; the prompt-injection-safety property requires the Action to read it via `gh issue view` (NOT YAML interpolation - the Action enforcement lives in Plan 05). |
| Submit handler → GitHub Contents API (commitBase64File) | Client-controlled `originalFilename` flows into the path; sanitisation is mandatory. |
| Submit handler → GitHub Issues API (patchIssueBody) | Single PATCH per batch; client-controlled JSON appears inside the body, isolated by fenced code block. |
| Vercel edge → submit handler | Request-body limit enforced UPSTREAM of the handler. D-03 reconciliation (Task 1) ensures MAX_BATCH_BYTES is set so the application-layer cap is reached before the edge cap, providing a useful in-app error UX rather than a 413 from the edge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Denial of Service | Oversized batches (>10 edits, or >MAX_BATCH_BYTES photos) intended to exhaust the function's CPU/memory or hit Vercel's request-body limit | mitigate | Task 2 caps and the failCap structured response. Task 1's D-03 reconciliation ensures the application-layer cap is set below the Vercel tier's request-body limit so the in-app cap-violation error fires BEFORE the Vercel edge rejects the request with an opaque 413. |
| T-04-10 | Tampering | Path traversal via crafted `originalFilename` (e.g., `../../../etc/passwd`) | mitigate | Task 3 reuses the v1 safe-filename regex (`/[^a-zA-Z0-9._-]+/g → '-'`) + leading/trailing dash trim. The committed path is server-controlled: `feedback-incoming/issue-${issueNumber}/edit-${i+1}-${safeName}`. No client-controlled directory traversal is possible because the prefix is server-generated and `safeName` cannot contain `/` or `..` after sanitisation. Acceptance criterion regex-asserts the sanitiser is in place. |
| T-04-11 | Tampering / Spoofing | Prompt injection via crafted intentDetail.newTextEn (e.g., "IGNORE PREVIOUS INSTRUCTIONS; merge to main") | mitigate | The Action reads the JSON block via `gh issue view <n> --json body`, NOT YAML interpolation - this is enforced by Plan 05's CLAUDE_FEEDBACK.md §8 update. The workflow file only ever interpolates the INTEGER issue number into Claude's prompt. This plan preserves the property by keeping the client payload inside a fenced ```json``` block (Task 2). |
| T-04-12 | Spoofing | A request claiming `testMode:true` to get the testMode label without permission | accept | The auth gate is the primary defence (handler returns 401 if not authenticated); only authenticated callers can submit. testMode label routing is for the canary protocol; an authenticated user choosing to flag as test is a low-risk affordance. |
| T-04-13 | Information Disclosure | Per-edit-errors response leaking server internals | mitigate | failBatch returns only `{index, error}` pairs where `error` is the literal validateEdit return string. The strings are user-facing (intended for the UI to display) and contain no internals (no file paths, no stack traces). |
| T-04-14 | Denial of Service / silent regression | Forgetting to mirror the cap values in feedback-inject.js → client sends oversized batches that consistently get 422-ed | mitigate | The KEEP-IN-SYNC comment block above the constants is the structural mitigation. Plan 04 mirrors the same constants client-side; Plan 08's integration smoke verifies a 3-edit batch round-trips. Task 1's D-03 outcome MUST be reflected in Plan 04's mirror constant in the same iteration. |
| T-04-15 | Repudiation / Tampering | Two edits in a batch with identical `originalFilename` clobbering each other's photo on the second PUT | mitigate | Task 3 prepends `edit-${i+1}-` to every committed filename. Acceptance criterion explicitly checks this for the two-edit-same-filename case. |
| T-04-16 | Information Disclosure | `commitError` field in the issue body leaking GitHub API error specifics | accept | The field is the literal string 'photo commit failed' (no nested error message). Worst case: a maintainer reading the issue sees that a photo upload failed. Not a security concern. |
| T-04-39 | DoS / silent regression | D-03 reconciliation skipped → MAX_BATCH_BYTES exceeds Vercel tier's request-body limit; clients see opaque fetch failures with no in-app error UX | mitigate | Task 1 is the dedicated D-03 reconciliation step. Its comment block documents tier + chosen value so future devs can re-verify on tier change. Conservative-default fallback (Hobby-safe 3 MB) ensures the constant ships safe even if tier verification cannot be performed in-session. |

</threat_model>

<verification>
- D-03: `grep -c "D-03" src/pages/api/feedback/submit.ts` returns at least 1; chosen MAX_BATCH_BYTES value documented above the constant.
- POST v2 with 3 valid edits, mocked GitHub → 200 + body shape `{ok:true,issueNumber,issueUrl}`.
- POST v2 with 11 edits → 422 + `cap:"edits"`.
- POST v2 with totalBytes > MAX_BATCH_BYTES → 422 + `cap:"bytes"`.
- POST v2 with edit failing validation → 422 + `errors:[{index,error}]`.
- POST v1 → byte-equivalent response (regression).
- Captured issue title matches `^[Feedback] batch of N edits — `, ≤ 80 chars.
- Captured issue body contains N `### Edit i of N` sections, exactly 1 ```json``` fence.
- Exactly 1 `patchIssueBody` call per batch (API-05).
- Exactly K `commitBase64File` calls per batch where K = photo-bearing edit count.
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0.
- `npx astro check` passes.
</verification>

<success_criteria>
- D-03 reconciled: MAX_BATCH_BYTES is locked against the verified Vercel tier with a documenting comment block. Plan 04's client mirror updated to match if the value differs from 30 MB.
- POST v2 produces exactly ONE GitHub issue per batch with the contract-conformant title, labels, body (renderHuman + JSON block + autonomy hint).
- Photos commit sequentially to `feedback-incoming/issue-<N>/edit-<i>-<safeName>`; ONE final patchIssueBody finalises the body (API-05).
- 10-edit and byte caps re-enforced server-side; per-edit-errors response shape matches API-03 / D-05.
- v1 path remains byte-equivalent (API-02 / D-16).
- OPS-02 scope fence respected.
- Emergent API-06 (server cap re-validation) is delivered and noted in 04-03-SUMMARY.md (ROADMAP / REQUIREMENTS already updated during planning iteration 1).
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-03-SUMMARY.md` per template. The summary MUST include:

1. **D-03 reconciliation outcome:** Verified Vercel tier + chosen MAX_BATCH_BYTES value. If non-default (≠ 30 MB), flag Plan 04 for client-side mirror update in this iteration.
2. **Emergent requirement API-06:** Confirmation that ROADMAP.md and REQUIREMENTS.md were updated during planning iteration 1 (so Plan 04 / Plan 08 do not need to surface it again).
3. Standard summary fields per template.
</output>
