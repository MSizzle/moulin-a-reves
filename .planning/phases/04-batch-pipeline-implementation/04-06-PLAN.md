---
phase: 04-batch-pipeline-implementation
plan: 06
type: execute
wave: 5
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - src/lib/feedback-version.ts
autonomous: true
requirements:
  - OPS-01
user_setup: []
tags:
  - feedback
  - cache-bust
  - version-bump

must_haves:
  truths:
    - "`src/lib/feedback-version.ts` exports `FEEDBACK_INJECT_VER = '2'` (bumped from `'1'`) - the single source of truth for the cache-bust query string (D-14 / OPS-01)."
    - "BaseLayout.astro's loader `?v=` and feedback.astro's iframe `src` (both import FEEDBACK_INJECT_VER) automatically pick up the new value at build time - no changes needed in either file."
    - "This bump invalidates the Vercel CDN cache key for public/feedback-inject.js, forcing browsers to fetch the new v2 inject script (shipped in Plan 04) on next visit."
    - "Bumping BEFORE Plans 03 and 04 land would cause cached browsers to fetch a non-existent v2 inject script (404) or to load an inject script whose server contract doesn't exist yet (500s); bumping AFTER Plans 03 and 04 land is the only safe ordering — this is why this plan is Wave 5 and explicitly depends on 04-03 (v2 server), 04-04 (v2 client), and 04-05 (Action manual)."
  artifacts:
    - path: "src/lib/feedback-version.ts"
      provides: "FEEDBACK_INJECT_VER constant = '2' (post-bump)"
      contains: "FEEDBACK_INJECT_VER = '2'"
  key_links:
    - from: "src/lib/feedback-version.ts"
      to: "src/layouts/BaseLayout.astro (importer of FEEDBACK_INJECT_VER)"
      via: "compile-time import; loader emits `<script src=/feedback-inject.js?v=2>`"
      pattern: "FEEDBACK_INJECT_VER"
    - from: "src/lib/feedback-version.ts"
      to: "src/pages/feedback.astro (importer of FEEDBACK_INJECT_VER)"
      via: "compile-time import; iframe src includes `?v=2`"
      pattern: "FEEDBACK_INJECT_VER"
---

<objective>
Bump `FEEDBACK_INJECT_VER` from `'1'` to `'2'` in `src/lib/feedback-version.ts` to invalidate Vercel's CDN cache key for `public/feedback-inject.js` (D-14 / OPS-01).

This is the single highest-impact one-line change in the phase. Without it, cached browsers continue running the v1 inject script indefinitely; the new state machine never loads; the chip never appears; Phase 5's OPS-05 canary fails.

This plan deliberately ships AFTER Plan 03 (v2 server), Plan 04 (the new client), and Plan 05 (the Action manual) land — bumping the version before the v2 client/server pair exists would cause cached browsers to fetch a 404 or to load a v2 client whose server contract isn't there yet. The explicit `depends_on: [04-03, 04-04, 04-05]` enforces this ordering directly (the transitive dep via 04-04→04-03 exists, but checker B1 required the direct edge).

Purpose: Activate the v2 inject script for all browsers, cached or not, on the next post-deploy visit.

Output: One-line edit to `src/lib/feedback-version.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md
@CLAUDE.md
@src/lib/feedback-version.ts

<interfaces>
<!-- Before: -->
<!--   export const FEEDBACK_INJECT_VER = '1'; -->
<!-- After: -->
<!--   export const FEEDBACK_INJECT_VER = '2'; -->
<!-- (Single string literal change. Imports in BaseLayout.astro and feedback.astro are untouched - they pick up the new value at build time.) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump FEEDBACK_INJECT_VER from '1' to '2'</name>
  <files>src/lib/feedback-version.ts</files>
  <read_first>
    - src/lib/feedback-version.ts (full file - 12 lines; the file being modified)
    - .planning/phases/04-batch-pipeline-implementation/04-PATTERNS.md §"`src/lib/feedback-version.ts`" (the cache-bust rationale)
    - .planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md §"decisions" D-14 (cache-bust mandate)
    - CLAUDE.md §"Architectural Constraints" → "Feedback mode" → "Cache-bust constant" bullet
  </read_first>
  <behavior>
    - Test: `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns 1.
    - Test: `grep -c "FEEDBACK_INJECT_VER = '1'" src/lib/feedback-version.ts` returns 0.
    - Test: After build, the generated `<script>` tag in any page's HTML head includes `?v=2` (verifiable via `npm run build` then grep on the dist/ output - though full build verification is for Plan 08's smoke task, not this one).
  </behavior>
  <action>
    Edit `src/lib/feedback-version.ts`. Change the single line:
    - From: `export const FEEDBACK_INJECT_VER = '1';`
    - To:   `export const FEEDBACK_INJECT_VER = '2';`

    Preserve all surrounding comments and imports. Do NOT add new exports, do NOT rename the constant, do NOT change the file's structure or whitespace beyond the one-character version string change. The comment block at the top of the file (which already explains the cache-bust rationale) does NOT need updating.

    Verify the change by running `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` and confirming it returns 1.
  </action>
  <verify>
    <automated>node -e "const txt = require('fs').readFileSync('src/lib/feedback-version.ts','utf8'); if (!/FEEDBACK_INJECT_VER = '2'/.test(txt)) { console.error('MISSING: FEEDBACK_INJECT_VER = \\'2\\''); process.exit(1); } if (/FEEDBACK_INJECT_VER = '1'/.test(txt)) { console.error('STILL PRESENT: FEEDBACK_INJECT_VER = \\'1\\''); process.exit(1); } console.log('OK'); "</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns 1.
    - Source: `grep -c "FEEDBACK_INJECT_VER = '1'" src/lib/feedback-version.ts` returns 0.
    - Source: `wc -l src/lib/feedback-version.ts` shows the same line count as before (no lines added or removed - one-character literal change only).
    - Source: `git diff main -- src/lib/feedback-version.ts | grep "^+" | grep -v "^+++" | wc -l` returns 1 (exactly one added line; the diff is the single version-string change).
    - Source: `git diff main -- src/lib/feedback-version.ts | grep "^-" | grep -v "^---" | wc -l` returns 1 (exactly one removed line).
    - Source: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0 (OPS-02 fence).
    - Behavior: `npx astro check` reports no new errors.
    - Behavior: `npm run build` (in a dry-run / CI sense — at minimum it must not fail) completes; the generated HTML for any route that imports BaseLayout includes `feedback-inject.js?v=2` somewhere in the page head's `<script>` for the feedback loader block (verifiable by inspecting dist/ output or rendering a single page).
  </acceptance_criteria>
  <done>FEEDBACK_INJECT_VER bumped to '2'. The diff is one line. Build succeeds. OPS-02 fence respected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache layer (Vercel CDN) → browser | The CDN serves `public/feedback-inject.js` and caches based on the URL + query string. Bumping `?v=` is the cache-bust mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-29 | Denial of Service / silent regression | Forgetting to bump → cached browsers run v1 inject forever; v2 batch UI never reaches users | mitigate | This entire plan exists to mitigate. Acceptance criteria assert `'2'` is present and `'1'` is absent. Plan 08's smoke task verifies a fresh-deploy HTML response includes `?v=2`. Phase 5 OPS-05 canary confirms end-to-end (browser network tab). |
| T-04-30 | Tampering / version skew | Bumping the version BEFORE Plans 03 and 04 land in the same PR → browsers fetch the new v2 inject script but the server still 422s any v2 payload | mitigate | Plan dependencies: this plan's `depends_on: [04-03, 04-04, 04-05]` enforces wave ordering with an explicit direct edge to 04-03 (the v2 server). Plan 08 is the final wave and verifies the full PR diff is coherent before merge. Single-PR scope (D-18) means the bump ships with the rest of the change, not as a separate deploy. |
| T-04-31 | Denial of Service | Mistyping the version (e.g., typo `'2x'` or `''`) breaking the script URL | mitigate | Acceptance criterion `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns exactly 1 (strict match). |

</threat_model>

<verification>
- `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns 1.
- `grep -c "FEEDBACK_INJECT_VER = '1'" src/lib/feedback-version.ts` returns 0.
- `npx astro check` reports no errors.
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0.
- Build succeeds; rendered page head includes `feedback-inject.js?v=2`.
</verification>

<success_criteria>
- One-line version bump landed in src/lib/feedback-version.ts.
- v2 client and v2 server are now coupled via the new cache-bust key.
- OPS-02 fence respected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-06-SUMMARY.md` per template.
</output>
</content>
</invoke>