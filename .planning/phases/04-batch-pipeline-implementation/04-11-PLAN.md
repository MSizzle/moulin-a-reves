---
phase: 04-batch-pipeline-implementation
plan: 11
type: execute
wave: 3
gap_closure: true
depends_on: [04-10]
files_modified:
  - src/lib/feedback-version.ts
autonomous: true
requirements: [OPS-01]
gaps_closed:
  - "OPS-01 cache-bust constraint: `public/feedback-inject.js` was modified in 04-10; without bumping `FEEDBACK_INJECT_VER`, cached browsers continue running the v2 inject without the CR-03 warning-success branch and the gap-closure is invisible to clients."
must_haves:
  truths:
    - "`FEEDBACK_INJECT_VER` equals `'3'`; the value `'2'` no longer appears in `src/lib/feedback-version.ts`."
    - "The loader (`src/layouts/BaseLayout.astro`) and iframe `src` (`src/pages/feedback.astro`) automatically pick up the new version via the existing import — no manual touch needed in either consumer."
  artifacts:
    - path: "src/lib/feedback-version.ts"
      provides: "Bumped cache-bust constant"
      contains: "FEEDBACK_INJECT_VER = '3'"
  key_links:
    - from: "src/lib/feedback-version.ts"
      to: "BaseLayout.astro `?feedback=1` loader + feedback.astro iframe `src`"
      via: "named import — both consumers stay in lockstep via the single-constant pattern"
      pattern: "FEEDBACK_INJECT_VER"
---

<objective>
Bump the feedback-inject cache-bust constant so cached browsers fetch the new `public/feedback-inject.js` shipped in 04-10. Single-line change.

Purpose: 04-10 modified `public/feedback-inject.js` (added the CR-03 success-with-warning branch). Without bumping `FEEDBACK_INJECT_VER`, the Vercel CDN keeps serving the old `?v=2` script to every cached browser and the user-visible half of the gap-closure is invisible until cache TTL expires. This is the OPS-01 / D-14 cache-bust convention established by Plan 04-06 in the original phase.

Output: One file changed (`src/lib/feedback-version.ts`); one constant value flips from `'2'` to `'3'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md
@.planning/phases/04-batch-pipeline-implementation/04-10-SUMMARY.md
@.planning/phases/04-batch-pipeline-implementation/04-06-SUMMARY.md
@CLAUDE.md
@src/lib/feedback-version.ts

<interfaces>
<!-- The file is 12 lines; the change is a single string literal. -->

src/lib/feedback-version.ts (full file):
  - Line 12: `export const FEEDBACK_INJECT_VER = '2';` — the source of truth.
  - Imported by: `src/layouts/BaseLayout.astro` (the inline script loader for `?feedback=1`), `src/pages/feedback.astro` (the iframe src query string at line 29: `const firstSrc = '/?feedback=1&v=' + VER;`).
  - The original Plan 04-06 bumped this constant from `'1'` to `'2'` as the cache-bust for the v2 batch-pipeline inject. This plan bumps it `'2'` to `'3'` for the gap-closure's inject change.
  - No other file needs to be touched. The two consumers automatically pick up the new value via the named import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump FEEDBACK_INJECT_VER from '2' to '3'</name>
  <files>src/lib/feedback-version.ts</files>
  <read_first>
    - src/lib/feedback-version.ts (entire file, 12 lines)
    - .planning/phases/04-batch-pipeline-implementation/04-10-SUMMARY.md (confirms `public/feedback-inject.js` was modified in 04-10 and explicitly directs this bump)
    - .planning/phases/04-batch-pipeline-implementation/04-06-SUMMARY.md (the original `'1'` to `'2'` bump precedent — same single-line change shape)
  </read_first>
  <action>
    Edit `src/lib/feedback-version.ts` line 12 to change the string literal from `'2'` to `'3'`. The full new line is `export const FEEDBACK_INJECT_VER = '3';`. Preserve the existing comment block at lines 1-11 verbatim. Do not change the export name, do not change quote style (single quotes match the project style per CLAUDE.md "Single quotes for JS/TS strings"), do not add or remove any other line, do not touch any consumer (BaseLayout.astro, feedback.astro). The consumers re-resolve the new value automatically via the named import on next build.
  </action>
  <verify>
    <automated>grep -c "FEEDBACK_INJECT_VER = '3'" src/lib/feedback-version.ts &amp;&amp; test "$(grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts)" = "0" &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "FEEDBACK_INJECT_VER = '3'" src/lib/feedback-version.ts` returns `1`.
    - Source assertion: `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns `0`.
    - Source assertion: total line count of the file is unchanged. `wc -l src/lib/feedback-version.ts` returns `12` (matches today's count).
    - TypeScript: `npx tsc --noEmit -p tsconfig.json` exits 0.
    - Consumer parity: `grep -n "FEEDBACK_INJECT_VER" src/layouts/BaseLayout.astro src/pages/feedback.astro` shows the named import in both consumers and confirms neither was modified directly.
    - OPS-02 fence (final fence check across all three gap-closure plans): `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0`.
    - Smoke harness final pass: `npx tsx scripts/smoke-feedback-v2.mjs` prints `5 passed, 0 failed` (re-confirm 04-10's gate after the bump).
  </acceptance_criteria>
  <done>The constant is `'3'`; TypeScript compiles; consumers pick up the new value automatically; OPS-02 fence is clean; the gap-closure is now visible to cached browsers on next deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel CDN cache → client browser | The cache-bust query string `?v=<FEEDBACK_INJECT_VER>` is the boundary that forces clients off the stale script. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04g-09 | Tampering (stale cached script) | Vercel CDN-cached `feedback-inject.js` | mitigate | Bumping `FEEDBACK_INJECT_VER` flips the `?v=` query string, forcing all CDN edges to fetch the new file. Same pattern the editor flow uses for `editor-inject.js` per CLAUDE.md "Cache-bust the inject script after editing it." |
| T-04g-10 | Repudiation (silent rollout) | Operator forgetting to bump | accept | This plan IS the bump; the dependency declaration `depends_on: [04-10]` and the explicit OPS-01 requirement in the frontmatter make the omission impossible if the gap-closure execution order is honoured. Future regressions are caught by the OPS-01 audit step in any future verification of this phase. |
</threat_model>

<verification>
After this task:
- `grep -c "FEEDBACK_INJECT_VER = '3'" src/lib/feedback-version.ts` returns `1`.
- `grep -c "FEEDBACK_INJECT_VER = '2'" src/lib/feedback-version.ts` returns `0`.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npx tsx scripts/smoke-feedback-v2.mjs` returns `5 passed, 0 failed` (final merge gate; re-runs because environment is unchanged from 04-10).
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0` (OPS-02 fence — final assertion across the gap-closure trio).
- `node --check public/feedback-inject.js` exits 0 (sanity).
- Cross-plan final audit (executor SHOULD record in the SUMMARY):
  - All three plans 04-09, 04-10, 04-11 are committed.
  - `git log --oneline main..HEAD` shows the gap-closure commits with `CR-01` / `CR-02` / `CR-03` traceability tokens or REQ IDs.
  - `grep -rn "CR-01\|CR-02\|CR-03" src/pages/feedback.astro src/pages/api/feedback/submit.ts public/feedback-inject.js scripts/smoke-feedback-v2.mjs` returns at least one match per token per file as appropriate (CR-01 in feedback.astro, CR-02 in submit.ts + smoke, CR-03 in submit.ts + feedback-inject.js).
</verification>

<success_criteria>
- Cache-bust constant equals `'3'`.
- Consumers (`BaseLayout.astro`, `feedback.astro`) automatically pick up the new value on next build.
- Smoke harness still passes (no regression introduced by the bump itself — it cannot regress; the constant is not load-bearing in the test harness).
- OPS-02 fence is clean.
- Cached browsers will fetch the new `?v=3` inject on first navigation after deploy and immediately see the CR-01/CR-03 UI wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-11-SUMMARY.md` per the standard summary template. In the summary, record:
1. The new value `'3'` and the previous value `'2'`.
2. Confirm the smoke harness still prints `5 passed, 0 failed`.
3. Confirm the OPS-02 fence grep returns `0`.
4. Note that the gap-closure trio (04-09 + 04-10 + 04-11) collectively closes CR-01, CR-02, CR-03 and that Success Criterion #6 / API-06 and ISSUE-04 atomicity are now deliverable end-to-end on the deployed pipeline.
5. Recommend a re-verification run of the phase via `/gsd-verify-phase 04` to update `04-VERIFICATION.md` with `gaps_closed: [...]` and flip `status` to `verified`.
</output>
