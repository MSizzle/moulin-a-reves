---
phase: 04-batch-pipeline-implementation
plan: 10
type: execute
wave: 2
gap_closure: true
depends_on: [04-09]
files_modified:
  - src/pages/feedback.astro
  - public/feedback-inject.js
  - scripts/smoke-feedback-v2.mjs
autonomous: true
requirements: [API-03, API-05, API-06, ISSUE-04, STAGE-07]
gaps_closed:
  - "CR-01: parent forwarder in `src/pages/feedback.astro:202-208` drops structured cap/error fields → inject's `m.cap` and `m.errors` branches are unreachable"
  - "Smoke scenario 4 in `scripts/smoke-feedback-v2.mjs` ships a 3-byte `dataBase64: 'AAAA'` stub with spoofed `bytes` descriptor — once 04-09 lands, the stub no longer trips the cap, so the scenario must ship an actually-oversize base64 string."
  - "Inject success branch in `public/feedback-inject.js:1109-1123` clears staged state unconditionally on `m.ok`; partial-failure `warning` is not surfaced to the user."
must_haves:
  truths:
    - "When the server returns `{ ok: false, cap, limit, actual, error }`, the user sees the cap-specific message ('This batch exceeds X MB total photo size') in the panel, not the generic 'Something went wrong' fallback."
    - "When the server returns `{ ok: false, errors: [{ index, error }, ...] }`, the user sees per-edit `.is-error` highlights on the failing staged items."
    - "When the server returns `{ ok: true, warning, commitErrors }`, the user sees the warning ('N of M photos failed to upload — re-attach and resubmit those edits') and the staged list is NOT cleared, so the user can fix and resubmit."
    - "Smoke harness scenario 4 ships a base64 string whose decoded length exceeds `MAX_BATCH_BYTES`, exercising the real cap defence; all 5 scenarios pass."
  artifacts:
    - path: "src/pages/feedback.astro"
      provides: "Parent forwarder that spreads server `errors` / `cap` / `limit` / `actual` / `error` / `warning` / `commitErrors` onto the `mar-feedback-result` postMessage to the iframe."
      contains: "data.cap"
    - path: "public/feedback-inject.js"
      provides: "Success-with-warning branch in the `mar-feedback-result` listener that surfaces `m.warning` without clearing staged state."
      contains: "m.warning"
    - path: "scripts/smoke-feedback-v2.mjs"
      provides: "Scenario 4 that ships a real oversize base64 string (length > capBytes * 4/3 so decoded length > capBytes)."
      contains: "'A'.repeat"
  key_links:
    - from: "feedback.astro message listener (line ~202-208) result branch"
      to: "iframe postMessage `mar-feedback-result`"
      via: "spread of `data.errors`, `data.cap`, `data.limit`, `data.actual`, `data.error`, `data.warning`, `data.commitErrors`"
      pattern: "data\\.cap"
    - from: "feedback-inject.js `mar-feedback-result` listener (STATE.SUBMITTING branch, lines 1108-1158)"
      to: "warning-success rendering"
      via: "new conditional `if (m.warning)` branch before the unconditional `clearStaged()` path"
      pattern: "m\\.warning"
    - from: "scripts/smoke-feedback-v2.mjs scenario 4 (lines 307-344)"
      to: "decoded-bytes cap path in submit.ts (post-04-09)"
      via: "construct base64 string whose `.length * 3/4` > capBytes"
      pattern: "'A'\\.repeat"
---

<objective>
Close the UI-plumbing BLOCKER (CR-01) and update the smoke harness so the v2 cap defence is exercised end-to-end. After this plan, Success Criterion #6 is fully deliverable: server re-validates (04-09) + UI surfaces (this plan), and the smoke harness proves both halves work together.

Purpose: 04-09 fixed the server but the inject's `m.cap` / `m.errors` branches at `public/feedback-inject.js:1134, 1143` are still unreachable because the parent forwarder in `src/pages/feedback.astro:202-208` only posts `{ ok: false }` to the iframe. After 04-09, the smoke harness scenario 4 also breaks because the cap now reads decoded bytes from a 3-byte stub. This plan wires the result-message round-trip end-to-end and updates the smoke scenario to ship a real oversize payload.

Output: Three files changed (`feedback.astro` parent forwarder, `public/feedback-inject.js` success-with-warning branch, `scripts/smoke-feedback-v2.mjs` scenario 4). After this plan, `FEEDBACK_INJECT_VER` MUST bump in 04-11 because `public/feedback-inject.js` was modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md
@.planning/phases/04-batch-pipeline-implementation/04-REVIEW.md
@.planning/phases/04-batch-pipeline-implementation/04-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-09-SUMMARY.md
@CLAUDE.md
@src/pages/feedback.astro
@public/feedback-inject.js
@scripts/smoke-feedback-v2.mjs

<interfaces>
<!-- Key landmarks in the three files. Read each file (or the cited line range) before editing. -->

src/pages/feedback.astro:
  - Lines 164-217: the `window.addEventListener('message', async function (ev) ...)` listener that receives `mar-feedback-submit` from the inject and POSTs to `/api/feedback/submit`.
  - Lines 191-200: success branch — posts `{ type: 'mar-feedback-result', ok: true }` to the iframe and shows the green toast.
  - Lines 202-208: failure branch — posts `{ type: 'mar-feedback-result', ok: false }` only. THIS IS THE CR-01 SITE. Server-provided structured fields (`errors`, `cap`, `limit`, `actual`, `error`, `warning`, `commitErrors`) are dropped.
  - Lines 179-188: auth-fail branch — posts `{ ok: false, auth: true }`. KEEP this shape; the inject's `m.auth` branch depends on it.
  - The file is NOT in the OPS-02 fence (confirmed in 04-VERIFICATION.md line 116, line 138-139).

public/feedback-inject.js:
  - Lines 1103-1184: the `mar-feedback-result` listener. Origin guard at line 1104; type guard at line 1106.
  - Lines 1109-1158: STATE.SUBMITTING branch (v2 batch result handler).
  - Lines 1110-1123: success branch (`m.ok`). Unconditionally clears staged state + chip + panel + drafts and renders a "Your batch was sent" panel. THIS IS THE SITE for the new `m.warning` branch.
  - Lines 1125-1132: auth branch (`m.auth`). Keeps staged state, re-renders chip.
  - Lines 1134-1141: errors branch (`Array.isArray(m.errors)`). Calls `renderPanel({ errors: m.errors })`. Currently DEAD CODE — becomes live once feedback.astro spreads `data.errors`.
  - Lines 1143-1151: cap branch (`m.cap`). Sets `lastCapMessage` from `m.error` and re-renders the panel. Currently DEAD CODE — becomes live once feedback.astro spreads `data.cap` + `data.error`.
  - Lines 1153-1157: generic-failure fallback.

scripts/smoke-feedback-v2.mjs:
  - Lines 307-344: scenario 4 "v2 cap violation: photo bytes". Reads `MAX_BATCH_BYTES` from submit.ts source via regex (line 312). After 04-09 the assertion target moves from descriptor to decoded bytes, so the constructed `dataBase64` must be a base64 string whose `.length * 3/4 - padding > capBytes` per edit (or summed across 2 edits, matching today's two-edit structure).
  - The fetch stub at line 91-… handles GitHub Contents API GET/PUT/POST/PATCH. PUTs return 201 with `{ content: { sha } }`. The cap gate fires BEFORE any PUT in handleV2Batch, so scenario 4 never touches the photo loop — it only needs the request body to make the cap reducer trip.

Key contract from 04-09: the partial-failure response shape is `{ ok: true, issueNumber, issueUrl, warning: '<N> of <M> photo(s) failed to upload', commitErrors: [{ index, error }] }` and the cap-violation response shape is `{ ok: false, cap: 'edits' | 'bytes', limit, actual, error }` (already shipped — confirmed in 04-VERIFICATION.md line 86). Both shapes must round-trip from `feedback.astro` to the iframe via the `mar-feedback-result` postMessage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Spread structured server fields onto the `mar-feedback-result` postMessage in `feedback.astro`</name>
  <files>src/pages/feedback.astro</files>
  <read_first>
    - src/pages/feedback.astro lines 134-219 (the entire message listener IIFE)
    - .planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md (gap A truth + artifact "src/pages/feedback.astro:202-208" — the CR-01 site)
    - .planning/phases/04-batch-pipeline-implementation/04-REVIEW.md lines 62-107 (CR-01 with the exact spread shape)
    - .planning/phases/04-batch-pipeline-implementation/04-09-SUMMARY.md (the partial-failure response shape `{ ok: true, …, warning, commitErrors }` 04-09 introduced)
  </read_first>
  <action>
    Modify the parent forwarder's two paths (lines 191-208) so the iframe postMessage carries the full structured server response:

    1. Failure branch at line 202-208 (the CR-01 site): change the posted message from `{ type: 'mar-feedback-result', ok: false }` to ALSO spread, when present on `data`, the following fields verbatim from the server response: `errors` (the per-edit `[{index, error}, ...]` array — pass through as `errors: data.errors` ONLY when `Array.isArray(data.errors)`), `cap` (the string `'edits' | 'bytes'`), `limit` (number), `actual` (number), `error` (the server's user-facing error string). Use conditional-spread syntax matching the existing `...(data && data.auth ? { auth: true } : {})` precedent at line 183. The base shape stays `{ type: 'mar-feedback-result', ok: false, ...conditionalFields }`. The toast call at lines 205-207 keeps its current shape (it already reads `data.error` for the toast body — no change there).

    2. Success branch at line 191-201: extend the posted message to ALSO spread `warning` (string) and `commitErrors` (array) when present on `data`. The current `{ type: 'mar-feedback-result', ok: true }` becomes `{ type: 'mar-feedback-result', ok: true, ...(data.warning ? { warning: data.warning } : {}), ...(Array.isArray(data.commitErrors) ? { commitErrors: data.commitErrors } : {}) }`. The green toast at lines 194-201 also gets a small adjustment: when `data.warning` is truthy, show the warning string in the toast body instead of the default "We've logged your request…" success copy. The `Done` button stays. Toast still uses `'ok'` kind — the issue WAS created; partial photo failure is a soft warning.

    3. Network/error catch branch at line 209-216: leave the posted message at `{ ok: false }` (no server response data to forward); no change.

    4. Auth 401 branch at line 179-188: leave unchanged (already spreads `auth: true`).

    Use the same `location.origin` target as the existing posts. Use the existing `var iframe = ...; iframe.contentWindow && iframe.contentWindow.postMessage(...)` idiom — do not refactor the IIFE.

    Reference the gap by writing the literal token `CR-01` in at least one new comment in the modified region for grep-traceability.

    This file is NOT in the OPS-02 fence. Confirm by checking that the path `src/pages/feedback.astro` is NOT among `public/editor-inject.js`, `public/editor`, `public/guardrails.js`, `src/pages/api/site`, `middleware.ts`.
  </action>
  <verify>
    <automated>grep -n "data\\.cap" src/pages/feedback.astro | head -3 &amp;&amp; grep -n "data\\.errors" src/pages/feedback.astro | head -3 &amp;&amp; grep -n "data\\.warning" src/pages/feedback.astro | head -3 &amp;&amp; grep -n "data\\.commitErrors" src/pages/feedback.astro | head -3 &amp;&amp; grep -c "CR-01" src/pages/feedback.astro &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "data\.cap" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: `grep -c "data\.errors" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: `grep -c "data\.limit" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: `grep -c "data\.actual" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: `grep -c "data\.warning" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: `grep -c "data\.commitErrors" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: the literal token `CR-01` appears in at least one comment in the modified region. `grep -c "CR-01" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: the existing auth branch still spreads `auth: true` (regression guard). `grep -c "auth: true" src/pages/feedback.astro` returns at least `1`.
    - Source assertion: there is NO unconditional `errors:`, `cap:`, `limit:`, `actual:`, `warning:`, `commitErrors:` field in the posted message — all six MUST be guarded by a conditional spread that checks the server provided the field. Manual grep verification: each new `data.X` reference appears inside a `?` ternary or `&&`/`Array.isArray()` guard.
    - Astro file type-checks via the project's TS: `npx tsc --noEmit -p tsconfig.json` exits 0 (the inline `<script is:inline>` is not TS-checked, but the frontmatter is).
    - OPS-02 fence: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0` (`feedback.astro` is NOT in the fence; this assertion guards against an accidental fence file edit).
  </acceptance_criteria>
  <done>Parent forwarder spreads `errors`/`cap`/`limit`/`actual`/`error` on failure and `warning`/`commitErrors` on success; auth path unchanged; inject's previously-dead `m.cap` and `m.errors` branches are now reachable; partial-failure warning is visible in the parent toast.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add warning-success branch to `public/feedback-inject.js` so partial commit failure does NOT clear staged state</name>
  <files>public/feedback-inject.js</files>
  <read_first>
    - public/feedback-inject.js lines 1090-1160 (the `mar-feedback-result` listener, STATE.SUBMITTING branch in particular)
    - public/feedback-inject.js lines 130-140 (STATE enum definition for STATE.SUBMITTING / STATE.STAGED / STATE.DONE labels)
    - public/feedback-inject.js lines 819-905 (renderPanel / renderChip / stagedDelete — the rendering helpers the new branch will call)
    - .planning/phases/04-batch-pipeline-implementation/04-09-SUMMARY.md (the partial-failure response shape — `{ ok: true, warning: '<N> of <M> photo(s) failed to upload', commitErrors: [{ index, error }] }`)
    - .planning/phases/04-batch-pipeline-implementation/04-REVIEW.md lines 160-205 (CR-03 — the v1 `warning` precedent that v2 inject must now mirror)
  </read_first>
  <action>
    Inside the `STATE.SUBMITTING` branch of the result listener (lines 1109-1158), add a NEW conditional ahead of the existing unconditional success branch at line 1110. Order of branches inside `if (state === STATE.SUBMITTING)` becomes:

      1. NEW: `if (m.ok && m.warning) { … partial-failure-warning-success … return; }`
      2. Existing: `if (m.ok) { … clear-everything success … return; }` (unchanged at lines 1110-1123)
      3. Existing: `if (m.auth) { … }` (unchanged)
      4. Existing: `if (Array.isArray(m.errors)) { … }` (unchanged — now reachable post-Task 1)
      5. Existing: `if (m.cap) { … }` (unchanged — now reachable post-Task 1)
      6. Existing generic failure fallback (unchanged)

    The NEW branch (1) does the following:
      - Does NOT call `clearStaged()`, `removeChip()`, `destroyStagedPanel()`, or `clearDraft()`.
      - Sets `state = STATE.STAGED` (so the chip + delete buttons re-enable; the user can edit and resubmit).
      - Calls `renderChip(loadStaged().length)` to refresh the count (which is unchanged — staged list survived).
      - Renders the panel with a warning message that includes `m.warning` (the server-provided summary string, e.g. "2 of 3 photo(s) failed to upload"). Pass the warning via the `capMessage` field of `renderPanel({ capMessage })` — that field is already plumbed through `renderPanel` as the inline non-fatal banner per existing usage at lines 1148-1150. The wording in the panel should append guidance such as "Re-attach the photo on the affected edits and resubmit." after the server's summary. Concatenate, do not replace, the server's `m.warning` text. Be defensive against missing fields with a fallback string.
      - Optionally (if `Array.isArray(m.commitErrors)`), pass the array through to `renderPanel({ errors: m.commitErrors })` so the failing indexes get `.is-error` highlights via the same mechanism the `m.errors` branch uses. This re-uses existing rendering — do NOT add a new highlight code path.

    Do NOT modify the v1 single-edit path at lines 1160-1184. Do NOT modify any other listener (`mar-feedback-submit` listener etc.). Do NOT touch the rest of the file.

    Reference the gap by writing the literal token `CR-03` in at least one new comment in the modified region for grep-traceability (the inject side of CR-03 is the success-with-warning surfacing).

    NOTE: This file IS subject to the FEEDBACK_INJECT_VER cache-bust rule. Plan 04-11 will bump `'2'` → `'3'` in `src/lib/feedback-version.ts`. Do not bump it here.
  </action>
  <verify>
    <automated>grep -n "m\\.warning" public/feedback-inject.js | head -3 &amp;&amp; node --check public/feedback-inject.js &amp;&amp; grep -c "CR-03" public/feedback-inject.js</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "m\.warning" public/feedback-inject.js` returns at least `1`.
    - Source assertion: the new `if (m.ok && m.warning)` (or equivalent guard) branch appears BEFORE the existing unconditional `if (m.ok)` branch inside `if (state === STATE.SUBMITTING) { ... }`. Manual ordering check: grep for the line numbers of both conditionals; the warning conditional must appear first.
    - Source assertion: within the new branch, `clearStaged(`, `removeChip(`, `destroyStagedPanel(`, `clearDraft(` MUST NOT appear (staged state is preserved). Verify by reading the branch body.
    - Source assertion: the literal token `CR-03` appears in at least one comment in the modified region. `grep -c "CR-03" public/feedback-inject.js` returns at least `1`.
    - Parse: `node --check public/feedback-inject.js` exits 0.
    - Behaviour assertion (manual, in 04-11 verification step): when a v2 batch returns 200 with `warning` + `commitErrors`, the chip count stays the same, the panel shows the warning message including the server's `<N> of <M>` summary, and the user can click ✕ to remove the failing edits and resubmit.
    - OPS-02 fence: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The inject distinguishes "clean success" from "success with partial photo-commit failure"; staged state survives partial failures; CR-03's UI half is now wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update smoke harness scenario 4 to ship an actually-oversize base64 string</name>
  <files>scripts/smoke-feedback-v2.mjs</files>
  <read_first>
    - scripts/smoke-feedback-v2.mjs lines 307-344 (scenario 4 today)
    - scripts/smoke-feedback-v2.mjs lines 1-90 (harness setup, fetch stub framework — to understand what the cap gate sees)
    - .planning/phases/04-batch-pipeline-implementation/04-09-SUMMARY.md (confirms the cap now reads `approxDecodedBytes(dataBase64)`)
    - .planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md lines 35-40 (the gap A missing item: "Smoke scenario 4 updated to send an actually-oversize base64 string")
  </read_first>
  <action>
    Rewrite the body of scenario 4 (lines 308-344) so that, with the decoded-bytes cap from 04-09 in effect, the cap gate trips on the REAL decoded length of `dataBase64` — not on a spoofed `bytes` descriptor.

    Concrete construction:
      - Keep the existing prelude that reads `MAX_BATCH_BYTES` from `src/pages/api/feedback/submit.ts` via the regex at lines 311-314 (capMB / capBytes derivation). Do not duplicate this constant in the test.
      - Build a base64 string `bigB64` long enough that, decoded, its byte count exceeds `capBytes` when summed across the two edits. Per-edit, ship a `dataBase64` of length `Math.ceil((capBytes + 1024) * 4 / 3 / 2) + 4` (the `+ 4` accounts for `=` padding rounding; the `+ 1024` provides a small margin above capBytes). Use `'A'.repeat(N)` (no padding `=` chars — pure `A` decodes cleanly as binary `\x00\x00\x00…` and is the simplest oversize stub). Use the same per-edit `dataBase64` value for both edits.
      - Drop the spoofed `bytes` descriptor entirely (the cap no longer reads it). Either remove `bytes` from the `image` shape OR keep it at a value that does NOT itself exceed the per-photo `MAX_IMAGE_BYTES = 12 MB` cap — keep it for shape-parity with what the client actually ships, but its value is no longer load-bearing. Recommended: keep it at a realistic value matching the decoded payload size for documentation, e.g. `bytes: Math.floor(bigB64.length * 3 / 4)`.
      - Keep the two-edit shape so the assertion still proves the SUM-across-edits reducer works (not just a single-photo gate).
      - Keep the existing assertions at lines 336-343 verbatim: status 422, `body.cap === 'bytes'`, `body.limit === capBytes`, `typeof body.actual === 'number'`, `body.actual > capBytes`. These all still hold because the cap response shape did not change in 04-09; only the data source did.

    Update the inline comment block above the construction (currently lines 316-319 documenting "we don't need real base64 of that size") to explain the new approach: "The cap reads `approxDecodedBytes(dataBase64)` per CR-02 fix; ship a real oversize base64 string so the sum-of-decoded-bytes reducer trips."

    Also fix the scenario's misleading inline note in the comment header (today line 25 of the harness reads "MAX_BATCH_BYTES read directly from submit.ts (scales with D-03)" — this stays; but add a one-line cross-reference to CR-02 next to scenario 4's docstring at lines 307 so a future reader sees the gap-closure breadcrumb).

    Do not modify scenarios 1, 2, 3, 5 or the fetch stub or the env setup.

    Reference the gap by writing the literal token `CR-02` in at least one comment line within scenario 4 for grep-traceability.
  </action>
  <verify>
    <automated>grep -n "'A'\\.repeat" scripts/smoke-feedback-v2.mjs | head -2 &amp;&amp; ! grep -n "dataBase64: 'AAAA'" scripts/smoke-feedback-v2.mjs &amp;&amp; grep -c "CR-02" scripts/smoke-feedback-v2.mjs &amp;&amp; npx tsx scripts/smoke-feedback-v2.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: scenario 4 constructs a base64 string with `'A'.repeat(…)`. `grep -c "'A'\.repeat" scripts/smoke-feedback-v2.mjs` returns at least `1`.
    - Source assertion: the old 4-byte stub is GONE. `grep -c "dataBase64: 'AAAA'" scripts/smoke-feedback-v2.mjs` returns `0`.
    - Source assertion: the literal token `CR-02` appears in at least one comment within or above scenario 4. `grep -c "CR-02" scripts/smoke-feedback-v2.mjs` returns at least `1`.
    - Behaviour assertion (the full proof): `npx tsx scripts/smoke-feedback-v2.mjs` prints `5 passed, 0 failed` and exits 0. This is the merge-gate command.
    - Cap-shape assertion (unchanged from today): scenario 4 still asserts `res.status === 422`, `body.cap === 'bytes'`, `body.limit === capBytes`, `body.actual > capBytes`.
    - Cross-plan invariant: at the point Task 3 runs, 04-09 Tasks 1 and 2 are committed, so the cap gate reads decoded bytes. If Task 3 is somehow run before 04-09 lands, scenario 4 will trivially pass with the old stub — that's why this plan declares `depends_on: [04-09]` in the frontmatter.
    - OPS-02 fence: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0`.
  </acceptance_criteria>
  <done>Scenario 4 ships a base64 string whose decoded length exceeds `MAX_BATCH_BYTES`; `npx tsx scripts/smoke-feedback-v2.mjs` returns `5 passed, 0 failed`; the OPS-02 fence is clean; the CR-02 fix is exercised by an honest test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/feedback/submit response → parent feedback.astro listener | Trusted (same-origin server). The forwarder spreads structured fields but the consumer (inject) treats them defensively (origin guard + type guard + per-field conditionals). |
| parent feedback.astro → iframe postMessage | Same-origin (`location.origin`). The inject's origin guard at line 1104 already gates this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04g-05 | Tampering / Information Disclosure | Parent forwarder spreading server fields onto iframe postMessage | mitigate | Conditional-spread guards (`Array.isArray`, `data.cap ? …`) ensure no `undefined` field reaches the iframe; the existing origin-guard at `feedback-inject.js:1104` still gates the receiver. Closes CR-01. |
| T-04g-06 | Repudiation | Partial-failure response semantics | mitigate | Inject preserves staged state on `m.warning` so the user can re-attach + resubmit failing photos. The user cannot accidentally close the tab thinking a partially-failed batch shipped clean. Closes CR-03's UI half. |
| T-04g-07 | Denial of Service | Smoke harness shipping a multi-MB base64 string in-memory | accept | The harness runs locally / in CI; the base64 string sits in process memory only and is reset between scenarios via `resetFetchLog()`. Bounded by capBytes (3 MB) × 4/3 ≈ 4 MB per scenario run. Acceptable. |
| T-04g-08 | Spoofing | Same-origin postMessage receiver in inject (WR-06 in 04-REVIEW) | accept | Pre-existing project posture; flagged by WR-06 as out of scope per the gap-closure brief. The origin guard at line 1104 is sufficient for the current threat model. |
</threat_model>

<verification>
After all three tasks:
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `node --check public/feedback-inject.js` exits 0.
- `npx tsx scripts/smoke-feedback-v2.mjs` prints `5 passed, 0 failed` and exits 0. **This is the master merge-gate for the gap-closure.**
- `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns `0` (OPS-02 fence).
- Grep traceability: `grep -c "CR-01" src/pages/feedback.astro` ≥ 1, `grep -c "CR-03" public/feedback-inject.js` ≥ 1, `grep -c "CR-02" scripts/smoke-feedback-v2.mjs` ≥ 1.

Manual end-to-end smoke (optional, post-merge): in `astro dev` with a fake authenticated session, simulate a 422 cap response by patching the server to return `{ ok: false, cap: 'bytes', limit: 3000000, actual: 4000000, error: 'Batch exceeds 3 MB total photo size' }` and confirm the inject panel shows "Batch exceeds 3 MB total photo size" (not the generic "Something went wrong"). Verify the same for `errors: [{index: 1, error: 'wording required'}]`. Verify success-with-warning by patching the server to return `{ ok: true, issueNumber: 123, issueUrl: '#', warning: '1 of 2 photo(s) failed to upload', commitErrors: [{ index: 0, error: 'photo commit failed' }] }` and confirm the chip count stays at 2 and the panel shows the warning.
</verification>

<success_criteria>
- The inject's `m.cap` and `m.errors` branches are now reachable (CR-01 closed).
- The inject distinguishes clean success from partial-failure success and preserves staged state in the latter (CR-03's UI half closed).
- The smoke harness exercises the real decoded-bytes cap path; all 5 scenarios pass (CR-02 fully verified end-to-end).
- OPS-02 fence is clean.
- Plan 04-11 follow-up: `FEEDBACK_INJECT_VER` bump is now required because `public/feedback-inject.js` changed behaviour.
</success_criteria>

<output>
After completion, create `.planning/phases/04-batch-pipeline-implementation/04-10-SUMMARY.md` per the standard summary template. In the summary, explicitly:
1. Confirm `5 passed, 0 failed` from the smoke harness.
2. Confirm the OPS-02 fence grep returned `0`.
3. State that `public/feedback-inject.js` was modified and that 04-11 MUST bump `FEEDBACK_INJECT_VER` from `'2'` to `'3'` per OPS-01.
4. List the new parent-forwarder spread fields (`errors`, `cap`, `limit`, `actual`, `error`, `warning`, `commitErrors`) so future maintainers can grep the SUMMARY for the contract.
</output>
