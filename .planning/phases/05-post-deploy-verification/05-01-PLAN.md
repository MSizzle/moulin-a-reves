---
phase: 05-post-deploy-verification
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke-feedback-v2.mjs
  - scripts/canary.sh
  - package.json
autonomous: true
gap_closure: false
requirements:
  - OPS-04
  - OPS-05
user_setup:
  - service: github-cli
    why: "Canaries (in Plans 05-02 and 05-03) shell out to `gh issue view`, `gh pr list`, `gh pr view`, `gh pr close`, `gh issue close`, `gh variable get DRY_RUN`, `gh api`. This plan only builds the tooling — `gh` is not invoked from inside the harness yet, but the operator must have `gh` authenticated for the keychain when 05-02 / 05-03 run."
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Operator can invoke `npm run canary:v1` from the project root and the harness launches in canary mode (real fetch, not stubbed)."
    - "Operator can invoke `npm run canary:v2` from the project root and the harness launches in v2-only canary mode."
    - "When `TARGET_URL` is unset, the smoke harness still runs all 5 unit-mode scenarios as before (no regression to existing 5/5 pass)."
    - "When `TARGET_URL` is set, the harness skips the dynamic import of `submit.ts`, skips the `globalThis.fetch` stub, and skips scenarios 3-5 (cap + validation) with a `SKIP: <reason>` line."
    - "The bash wrapper `scripts/canary.sh` defaults `DEPLOY_URL` to `https://www.moulinareves.com` and is overridable via env var."
  artifacts:
    - path: scripts/smoke-feedback-v2.mjs
      provides: "Dual-mode harness (unit + canary). Reads `TARGET_URL` env var and `--canary v1|v2` argv to branch."
      contains: "TARGET_URL"
    - path: scripts/canary.sh
      provides: "Thin bash wrapper exec'ing the harness against DEPLOY_URL with --canary v1|v2."
      min_lines: 25
    - path: package.json
      provides: "Three new entries in `scripts` section: `canary:v1`, `canary:v2`, `canary`."
      contains: "canary:v1"
  key_links:
    - from: scripts/canary.sh
      to: scripts/smoke-feedback-v2.mjs
      via: "exec via npx tsx with TARGET_URL=$DEPLOY_URL"
      pattern: "TARGET_URL=.*npx tsx scripts/smoke-feedback-v2.mjs"
    - from: package.json scripts.canary:v1
      to: scripts/canary.sh
      via: "shell invocation"
      pattern: "scripts/canary.sh v1"
---

<objective>
Build the canary tooling that Plans 05-02 (v1 canary) and 05-03 (v2 canary) will execute against the live deployment at `https://www.moulinareves.com`.

This is a pure-tooling plan: no live deploy is hit, no GitHub issues are created. Plans 05-02 and 05-03 use the artifacts built here.

Purpose: Close the audit gap surfaced in `.planning/v1.1-MILESTONE-AUDIT.md` — "Smoke harness has no TARGET_URL retarget seam" — by extending `scripts/smoke-feedback-v2.mjs` with a dual-mode (unit vs canary) shape per CONTEXT.md D-01. Add the bash wrapper (D-02) and npm scripts (D-04) so the operator's surface is one line: `npm run canary:v1` or `npm run canary:v2`.

Output: Three modified files. Unit-mode regression: the existing 5-scenario harness still returns `5 passed, 0 failed`. Canary mode lays the seams that 05-02 and 05-03 invoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-post-deploy-verification/05-CONTEXT.md
@.planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md
@.planning/v1.1-MILESTONE-AUDIT.md

<interfaces>
Read these line ranges from scripts/smoke-feedback-v2.mjs before modifying. Executor preserves these shapes verbatim.

Module-level constants in scripts/smoke-feedback-v2.mjs:
- process.env.GITHUB_TOKEN, process.env.GITHUB_REPO, process.env.DASHBOARD_PASSWORD — set with ||= before dynamic import (lines 50-52)
- registerHooks() text-rewrite of import.meta.env to process.env (lines 67-82) — only matters in unit mode (importing submit.ts)
- globalThis.fetch stub installed at line 91 — only matters in unit mode
- fetchLog array + resetFetchLog() — only matters in unit mode
- Dynamic import of ../src/pages/api/feedback/submit.ts at line 146 and ../src/lib/auth.ts at line 148 — only matters in unit mode (canary calls real endpoint, not local POST)
- makeRequest(payload) at line 178 builds a Request with cookie maison_session=ENCODED_TOKEN from createSession() — canary mode reuses this signature pattern but POSTs to TARGET_URL with fetch, not local POST()
- The 5 scenarios at lines 211, 241, 292, 310, 355 — unit-only; canary mode SKIPs them with a SKIP: line each

POST endpoint contract (from src/pages/api/feedback/submit.ts):
- Auth: requires maison_session cookie HMAC-signed with DASHBOARD_PASSWORD (checkAuth)
- v1 success shape (status 200): an object with ok: true, issueNumber as number, issueUrl as string
- v2 success shape (status 200): same plus optional warning and commitErrors fields when per-edit photo commits fail
- Error response (status 4xx): ok: false plus error string, or for cap violations: cap, limit, actual fields

createSession() from src/lib/auth.ts:
- Pure function, no args, returns the HMAC-signed timestamp cookie value as a string
- Uses DASHBOARD_PASSWORD env var as HMAC secret
- For canary mode against live deploy: the local DASHBOARD_PASSWORD must match the Vercel project's DASHBOARD_PASSWORD env var (otherwise the deployed endpoint returns 401). Document this requirement in canary.sh header.

DEPLOY_URL contract (canary.sh):
- Default: https://www.moulinareves.com
- Override: DEPLOY_URL=https://example.com npm run canary:v1 — passed through to harness as TARGET_URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/smoke-feedback-v2.mjs with TARGET_URL + --canary v1|v2 dual-mode branching</name>
  <files>scripts/smoke-feedback-v2.mjs</files>
  <read_first>
    - scripts/smoke-feedback-v2.mjs (current 386 LOC — preserve all unit-mode behavior verbatim)
    - src/pages/api/feedback/submit.ts (first 60 lines for response shapes, lines 580-610 for v2 response shape, lines 240-260 for v1 title pattern, lines 500-505 for v2 title pattern)
    - src/lib/auth.ts (createSession signature — needed to mint a valid maison_session cookie for canary fetches)
    - .planning/phases/05-post-deploy-verification/05-CONTEXT.md (D-01, D-03 for TARGET_URL semantics; D-06 for DRY_RUN pre-check note (informational only — implemented in Plan 05-03); D-08, D-10 for cache-bust proof assertions (implemented in Plan 05-03 only))
  </read_first>
  <action>
At the top of scripts/smoke-feedback-v2.mjs (after the existing line ~52 env-var setup), add a TARGET_URL detection block. Read process.env.TARGET_URL or null into a const named TARGET_URL. Read process.argv to detect a --canary flag and extract its following value (v1, v2, or null) into a const named CANARY_KIND. Per CONTEXT D-01 and D-03 the semantics are: when TARGET_URL is unset, the harness runs in unit mode with today's exact behavior — ALL 5 scenarios run with stubbed fetch and dynamic import. When TARGET_URL is set, the harness runs in canary mode: skip the dynamic import of submit.ts, skip the globalThis.fetch stub install, and skip scenarios 3-5 (each emitting a SKIP: line for traceability).

Branch the rest of the file on if (TARGET_URL). In the canary branch: do NOT install the registerHooks block (lines 66-82 in the original — only needed when importing submit.ts which canary mode does not do); do NOT install the globalThis.fetch stub (lines 84-141 in the original — canary mode hits the real network). STILL dynamically import createSession from src/lib/auth.ts (the canary needs a valid maison_session cookie to satisfy checkAuth on the live endpoint — operator's local DASHBOARD_PASSWORD env var must match the Vercel project's, document this requirement in a code comment near the createSession import). Replace makeRequest with a canary-mode helper named canaryPost(payload) that mints the cookie identically via createSession() but performs fetch(TARGET_URL + '/api/feedback/submit', ...) with method POST, headers Content-Type application/json plus cookie maison_session=ENCODED_TOKEN, and body JSON.stringify(payload), returning the Response.

For the 5 existing scenarios at lines 211, 241, 292, 310, 355: in canary mode, wrap with the following dispatch — if (TARGET_URL and CANARY_KIND === 'v1') call only runCanaryV1(); if (TARGET_URL and CANARY_KIND === 'v2') call only runCanaryV2(); if (TARGET_URL and not CANARY_KIND) call runCanaryV1() then runCanaryV2() sequentially. In all canary cases, scenarios 3-5 emit console.log of SKIP: scenario NAME (unit-mode only — requires fetch stub + dynamic import) and do not count toward pass/fail.

For Task 1 of THIS plan, the runCanaryV1() and runCanaryV2() bodies are STUBS only. The stub minimum body sends a minimal-valid payload (same shape as the unit-mode scenario 1 / scenario 2 use) to TARGET_URL + '/api/feedback/submit' with the cookie set by canaryPost, asserts response.status === 200 only, and logs PASS: <canary kind> stub on success or throws on failure. The FULL assertion bodies (issue title shape, gh issue view, PR poll, cache-bust proof, cleanup) land in Plans 05-02 (runCanaryV1 body) and 05-03 (runCanaryV2 body). What matters for Task 1 is the SEAM: named arrow-function-or-async-function declarations for runCanaryV1 and runCanaryV2 placed BEFORE the final report block (line 386), and the orchestrating if (TARGET_URL) dispatcher that calls them. The unit-mode run() invocations and final report block stay byte-equivalent.

Add a top-of-file usage comment block matching the existing convention (lines 1-47) documenting TARGET_URL and the --canary v1 / --canary v2 args. Reference CONTEXT D-01 and D-03 by ID.

Do NOT add any new package dependencies. Use only Node 24 builtins plus the existing dynamic-import pattern. Do NOT remove any existing scenario assertions — they must still pass when invoked in unit mode (regression guard).
  </action>
  <verify>
    <automated>npx tsx scripts/smoke-feedback-v2.mjs 2>&amp;1 | tail -5 | grep -E "^5 passed, 0 failed$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const TARGET_URL" scripts/smoke-feedback-v2.mjs` returns >= 1
    - `grep -c "process.argv.includes('--canary')" scripts/smoke-feedback-v2.mjs` returns >= 1
    - `grep -c "runCanaryV1" scripts/smoke-feedback-v2.mjs` returns >= 2 (definition + call site)
    - `grep -c "runCanaryV2" scripts/smoke-feedback-v2.mjs` returns >= 2 (definition + call site)
    - `grep -c "TARGET_URL + '/api/feedback/submit'" scripts/smoke-feedback-v2.mjs` returns >= 1 (real fetch in canary mode hits the deployed endpoint)
    - Unit-mode regression: `npx tsx scripts/smoke-feedback-v2.mjs` (no TARGET_URL env var) returns exit code 0 with final line `5 passed, 0 failed`
    - Canary-mode dry-test (against unreachable URL — confirms seam wiring, not live PASS): `TARGET_URL=http://127.0.0.1:1 npx tsx scripts/smoke-feedback-v2.mjs --canary v1` exits NON-ZERO (the fetch fails — but the harness reaches the canary code path; runCanaryV1 was invoked, evidenced by output mentioning v1 or canary, NOT `5 passed, 0 failed`)
    - Skip lines present: `TARGET_URL=http://127.0.0.1:1 npx tsx scripts/smoke-feedback-v2.mjs --canary v1 2>&amp;1 | grep -c "^SKIP:"` returns >= 3 (scenarios 3-5 skipped under canary mode)
    - File parses cleanly: `node --check scripts/smoke-feedback-v2.mjs` exit code 0
  </acceptance_criteria>
  <done>
    Smoke harness is dual-mode. Unit mode: byte-for-byte preserves the 5/5 pass. Canary mode: real fetch against TARGET_URL with the same auth-cookie shape; scenarios 3-5 emit SKIP lines. Seam in place for Plans 05-02 and 05-03 to extend with the live OPS-04 / OPS-05 assertion bodies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/canary.sh — thin bash wrapper around the harness</name>
  <files>scripts/canary.sh</files>
  <read_first>
    - .planning/phases/05-post-deploy-verification/05-CONTEXT.md (D-02 for wrapper shape, D-03 for DEPLOY_URL default semantics)
    - scripts/smoke-feedback-v2.mjs (the just-modified file; canary.sh must invoke it with the correct env var and argv)
  </read_first>
  <action>
Create scripts/canary.sh as a NEW file: a thin bash wrapper that exists to honor OPS-04's literal ROADMAP wording "curl with old-shape JSON" — i.e., the operator-facing entry point is bash, not Node, even though the harness underneath is Node.

Required behavior per D-02 and D-03:
- Set the shell strict mode (set -euo pipefail) at the top
- Set DEPLOY_URL to use parameter expansion defaulting to https://www.moulinareves.com (overridable via env var per D-03)
- Accept exactly one positional argument: v1, v2, or empty string
- If arg is v1: exec the harness with TARGET_URL set to DEPLOY_URL and pass --canary v1 to the harness
- If arg is v2: exec the harness with TARGET_URL set to DEPLOY_URL and pass --canary v2 to the harness
- If no arg: exec the harness with only TARGET_URL set (no --canary flag) — per D-02 this runs both v1 and v2 sequentially
- If arg is anything else: echo a usage message ("usage: scripts/canary.sh [v1|v2]") to stderr and exit 2

Add a header comment block referencing CONTEXT D-02 (wrapper rationale) and D-03 (DEPLOY_URL override). Note in the header that DASHBOARD_PASSWORD env var MUST be set locally and MUST match the Vercel project's value, otherwise checkAuth on the live endpoint returns 401.

After creating, run chmod +x scripts/canary.sh so npm-scripts can invoke it directly.

Do NOT embed curl calls in this wrapper. The harness owns all HTTP; the wrapper just sets env and delegates. The ROADMAP's "curl" wording is satisfied semantically by an HTTP POST against the live endpoint; the implementation tool is fetch inside Node, but the operator surface is bash.
  </action>
  <verify>
    <automated>test -x scripts/canary.sh &amp;&amp; bash -n scripts/canary.sh &amp;&amp; scripts/canary.sh badarg 2>&amp;1 | grep -c "usage:" | grep -q 1</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/canary.sh exists and is executable: `test -x scripts/canary.sh` exits 0
    - Bash syntax valid: `bash -n scripts/canary.sh` exits 0
    - Default DEPLOY_URL is the production URL: `grep -c 'DEPLOY_URL.*moulinareves.com' scripts/canary.sh` returns >= 1
    - Override seam present: `grep -c '${DEPLOY_URL:-' scripts/canary.sh` returns >= 1
    - Three exec paths present: `grep -c -- '--canary v1' scripts/canary.sh` >= 1, `grep -c -- '--canary v2' scripts/canary.sh` >= 1, and the no-arg path runs without `--canary`
    - Invalid arg rejected: `scripts/canary.sh badarg` writes "usage:" to stderr and exits with code 2
    - References CONTEXT D-02 and D-03: `grep -cE "D-0[23]" scripts/canary.sh` returns >= 1
    - DASHBOARD_PASSWORD pre-condition documented: `grep -c "DASHBOARD_PASSWORD" scripts/canary.sh` returns >= 1
  </acceptance_criteria>
  <done>
    Bash wrapper exists, executable, syntactically valid, dispatches the three argv shapes correctly. DASHBOARD_PASSWORD operator pre-condition is documented in the header comment.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add canary npm scripts to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (current 26 LOC — preserve all existing scripts, deps, and structure)
    - .planning/phases/05-post-deploy-verification/05-CONTEXT.md (D-04 for the three npm-script entries)
  </read_first>
  <action>
Edit package.json to add THREE new entries to the scripts object, per CONTEXT D-04. The three entries are: canary:v1 mapped to "scripts/canary.sh v1"; canary:v2 mapped to "scripts/canary.sh v2"; canary mapped to "scripts/canary.sh" (no arg — runs both v1 and v2 sequentially per D-02 default).

Place them after the existing astro entry. Preserve JSON syntax (no trailing commas inside the scripts object). Do NOT modify any other key in package.json — no dep changes, no devDep changes, no engines changes, no name/version changes. Do NOT add any new dep (npm scripts call the already-checked-in bash wrapper, which calls already-checked-in npx tsx).

After saving, validate the JSON parses (use node -e to JSON.parse the file content; exit 0 expected).
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if(p.scripts['canary:v1']!=='scripts/canary.sh v1') process.exit(1); if(p.scripts['canary:v2']!=='scripts/canary.sh v2') process.exit(1); if(p.scripts['canary']!=='scripts/canary.sh') process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['canary:v1'])"` outputs `scripts/canary.sh v1`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['canary:v2'])"` outputs `scripts/canary.sh v2`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['canary'])"` outputs `scripts/canary.sh`
    - Existing scripts preserved: `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); ['dev','prebuild','build','preview','astro'].forEach(k=>{if(!p.scripts[k]) throw new Error('missing '+k)}); console.log('ok')"` outputs `ok`
    - No deps added: `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const deps=Object.keys(p.dependencies||{}).length; const dev=Object.keys(p.devDependencies||{}).length; console.log(deps+','+dev)"` outputs `6,1`
    - JSON parses: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
    - npm sees the scripts: `npm run 2>&amp;1 | grep -c "canary:" ` returns >= 2
  </acceptance_criteria>
  <done>
    Three new npm-script entries land in package.json. No deps changed. Existing scripts preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → harness/wrapper | Operator-controlled; trusted. Env vars (DEPLOY_URL, DASHBOARD_PASSWORD) come from operator's local shell or .env.local. |
| harness → deployed endpoint | Crosses the public internet (Plans 05-02 / 05-03 only — this plan does not hit live). HTTPS-only via the Vercel project's TLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | scripts/canary.sh argv parsing | mitigate | Strict positional-arg validation (exactly `v1`, `v2`, or empty); anything else exits 2 with usage message. No eval, no string-glob args. |
| T-05-02 | Information disclosure | DASHBOARD_PASSWORD env var | mitigate | Operator sources from .env.local (gitignored per CLAUDE.md). canary.sh header documents the requirement without embedding the value. git diff of this plan shows no secret-shaped strings (grep for `DASHBOARD_PASSWORD = ` would catch accidents). |
| T-05-03 | Tampering | npm-script string in package.json | mitigate | Scripts are exact string commands (scripts/canary.sh v1), not template strings. No env var interpolation at npm level (that's the wrapper's job). |
| T-05-04 | Denial of service | Accidental hammering of /api/feedback/submit | accept | Tooling plan only — no live hits. Plans 05-02 / 05-03 each fire ONE canary POST per run. Risk to client-feedback issue stream is bounded (1 + 1 issues per Phase 5 execution, cleaned up in finally blocks per D-07). |
| T-05-05 | Elevation of privilege | maison_session cookie leakage in harness logs | mitigate | The cookie is HMAC-signed with DASHBOARD_PASSWORD; minting it is harmless (a fresh one is created per invocation). The harness already prints the request body for failure debug — confirm it does NOT print the cookie. Audit: `grep -c "console.log.*cookie" scripts/smoke-feedback-v2.mjs` should be 0. |
</threat_model>

<verification>
Phase-level Plan 05-01 verification:

1. Unit-mode regression: `npx tsx scripts/smoke-feedback-v2.mjs` returns exit 0 with `5 passed, 0 failed`.
2. Canary-mode seam: `TARGET_URL=http://127.0.0.1:1 npx tsx scripts/smoke-feedback-v2.mjs --canary v1` exits non-zero (fetch fails to unreachable URL) and outputs at least 3 `SKIP:` lines confirming scenarios 3-5 are skipped under canary mode.
3. Wrapper executable: `test -x scripts/canary.sh` exits 0 and `bash -n scripts/canary.sh` exits 0.
4. npm scripts wired: `npm run canary:v1`, `npm run canary:v2`, `npm run canary` all recognized.
5. OPS-02 fence intact: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0.
6. No new dependencies: package.json dependency counts unchanged.
</verification>

<success_criteria>
- All 3 tasks' acceptance criteria pass
- Unit-mode regression: `5 passed, 0 failed` from the unmodified harness invocation
- Canary-mode seam reachable: --canary v1 and --canary v2 invoke distinct code paths (runCanaryV1 / runCanaryV2)
- `npm run canary:v1` is a recognized script
- OPS-02 fence still clean
- No new package deps
</success_criteria>

<output>
After completion, create `.planning/phases/05-post-deploy-verification/05-01-SUMMARY.md` with:
- Files modified (3): scripts/smoke-feedback-v2.mjs, scripts/canary.sh (NEW), package.json
- LOC delta (rough)
- Confirmation that unit-mode `5 passed, 0 failed` regression holds
- Confirmation that canary-mode seams exist (greppable assertions from acceptance criteria)
- requirements-completed: []  (this plan builds tooling — OPS-04 / OPS-05 are SATISFIED by Plans 05-02 and 05-03 running the actual canaries against the live deploy)
- Note: this plan ENABLES OPS-04 / OPS-05 verification, but does not itself satisfy them. The satisfaction comes from the live canary runs in 05-02 and 05-03.
</output>
