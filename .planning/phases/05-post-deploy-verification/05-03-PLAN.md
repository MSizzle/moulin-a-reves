---
phase: 05-post-deploy-verification
plan: 3
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - scripts/smoke-feedback-v2.mjs
  - .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md
autonomous: true
gap_closure: false
requirements:
  - OPS-05
user_setup:
  - service: github-cli
    why: "Plan 05-03 calls `gh variable get DRY_RUN` (pre-flight assert), `gh issue view`, `gh pr list`, `gh pr view`, `gh pr close`, `gh issue close`, and `git push --delete origin <branch>` (with `gh api repos/.../git/refs/heads/<branch> -X DELETE` as fallback). All must work with the operator's existing gh auth."
    env_vars: []
    dashboard_config:
      - task: "Confirm DRY_RUN repo variable is set to true before running this canary"
        location: "Repo Settings -> Secrets and variables -> Actions -> Variables tab -> DRY_RUN"
        command_check: "gh variable get DRY_RUN"
        expected: "true"
        operator_command_to_set: "gh variable set DRY_RUN -b true"
        rationale: "Without DRY_RUN=true, the Claude Action would squash-merge the canary's test PR into main, polluting the codebase with canary marker text. The canary aborts (exit 2) if DRY_RUN is not 'true'. This safety toggle is operator-owned, NOT canary-owned (per CONTEXT D-06)."
must_haves:
  truths:
    - "Before opening an issue, the canary asserts `gh variable get DRY_RUN` returns the literal string `true` and aborts (exit 2) with a clear operator message if not (per CONTEXT D-06)."
    - "A schemaVersion 2, batch true payload (2 small text-only edits) POSTed to https://www.moulinareves.com/api/feedback/submit with client-feedback label returns HTTP 200 with body shape ok true, issueNumber integer, issueUrl string."
    - "The created GitHub issue title matches the regex /^\\[Feedback\\] batch of [0-9]+ edits — /, the body contains exactly ONE fenced json block, and the body contains either an AUTO-ELIGIBLE or NEEDS-REVIEW autonomy hint string matching the expected verdict for the canary's edits."
    - "Within 5 minutes of issue creation (polled at 10s intervals), the Claude Action opens a PR at branch feedback/issue-<N>-batch-<M> and posts a result comment on the issue."
    - "Because DRY_RUN is true, the Action halts at the squash-merge step (claude.yml:122 gate). The PR exists but is NOT merged; main is unmodified."
    - "Cache-bust proof (CONTEXT D-08): curl -sL of the deployed root URL returns HTML containing the exact substring `const feedbackVer = \"3\"` (the inline loader block from BaseLayout.astro:1025 emits this from FEEDBACK_INJECT_VER='3')."
    - "Asset HEAD probe (CONTEXT D-10): curl -sI of /feedback-inject.js?v=3 returns HTTP 200 (the cache-busted asset is reachable)."
    - "Cleanup runs in a finally-equivalent block (mandatory per CONTEXT D-07): test PR closed, remote branch deleted, test issue closed. Failure to clean up is a hard fail (exit 3)."
    - "An evidence file at .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md records the full audit trail: issue number, PR number, branch name, autonomy verdict, cache-bust grep output, asset HEAD response, cleanup confirmation."
  artifacts:
    - path: scripts/smoke-feedback-v2.mjs
      provides: "runCanaryV2() body — the OPS-05 live-fetch scenario with DRY_RUN pre-check, payload POST, issue/PR/result-comment assertions, cache-bust proof, asset HEAD probe, full cleanup."
      contains: "runCanaryV2"
    - path: .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md
      provides: "Audit trail for OPS-05 satisfaction: all assertion targets plus cache-bust + asset proof."
      min_lines: 50
  key_links:
    - from: scripts/smoke-feedback-v2.mjs runCanaryV2
      to: gh variable get DRY_RUN
      via: "execFileSync pre-flight assert; abort exit 2 if not 'true'"
      pattern: "gh.*variable.*get.*DRY_RUN"
    - from: scripts/smoke-feedback-v2.mjs runCanaryV2
      to: https://www.moulinareves.com/api/feedback/submit
      via: "fetch POST schemaVersion 2, batch true, 2 edits with i18nAttr (AUTO-ELIGIBLE forcing pattern from smoke scenario 2), maison_session cookie, client-feedback label"
      pattern: "schemaVersion: 2"
    - from: scripts/smoke-feedback-v2.mjs runCanaryV2
      to: gh pr list filtered by head branch
      via: "poll-with-timeout (10s intervals, 5min cap)"
      pattern: "feedback/issue-"
    - from: scripts/smoke-feedback-v2.mjs runCanaryV2 cache-bust proof
      to: https://www.moulinareves.com/
      via: "curl -sL plus grep for const feedbackVer = \"3\""
      pattern: "feedbackVer"
    - from: scripts/smoke-feedback-v2.mjs runCanaryV2 asset HEAD probe
      to: https://www.moulinareves.com/feedback-inject.js?v=3
      via: "curl -sI; assert HTTP 200"
      pattern: "feedback-inject.js"
---

<objective>
Execute the OPS-05 batched canary against the LIVE deployment at https://www.moulinareves.com. This plan ships the body of runCanaryV2() (the seam was added in Plan 05-01) and runs it to produce verifiable evidence that the v2 batch path AND the cache-bust ship correctly post-Phase-4.

Per CONTEXT.md D-05: v2 canary uses the client-feedback label (Action fires) plus DRY_RUN=true repo variable (Action halts at merge step). This proves the full pipeline — issue then Action then PR plus result comment plus autonomy verdict — without leaving a real merge in main. The client-feedback-test alternative is rejected because it would skip the Action entirely and fail OPS-05's "ONE Claude PR with all edits applied" success criterion.

OPS-05's ROADMAP success criterion (.planning/ROADMAP.md line 92): "A v2 batched canary (using the client-feedback-test label OR DRY_RUN=true first) produces exactly ONE issue containing all edits in the JSON block, ONE Claude PR with all edits applied, and the autonomy verdict matches expectation (passes if every edit passes; gates to review otherwise); browser network tab confirms feedback-inject.js?v=<NEW_VER> is fetched (cache-bust verified end-to-end)."

Purpose: Close OPS-05.
Output: runCanaryV2() body + a committed evidence file proving the canary passed + the Phase 5 OPS-02 fence assertion remains 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-post-deploy-verification/05-CONTEXT.md
@.planning/phases/05-post-deploy-verification/05-01-PLAN.md
@.planning/phases/05-post-deploy-verification/05-02-PLAN.md
@.github/CLAUDE_FEEDBACK.md
@.github/workflows/claude.yml

<interfaces>
Read these interfaces from src/pages/api/feedback/submit.ts handleV2Batch and .github/CLAUDE_FEEDBACK.md §8 before modifying.

v2 SUCCESS RESPONSE (live endpoint, status 200) per submit.ts:590-603: object with ok true, issueNumber as integer, issueUrl as GitHub URL string. Optional warning and commitErrors fields if any per-edit photo commit failed. A photo-less batch (this canary) never has warning.

v2 TITLE SHAPE (submit.ts:500-503): literal pattern `[Feedback] batch of <N> edits — <comma-separated unique pageRoutes (60 chars max)>`. Example for 2 edits on / and /the-compound/: `[Feedback] batch of 2 edits — /, /the-compound/`.

v2 ISSUE BODY (submit.ts:467-494): starts with the literal string `**A client left feedback on the live site (<N> edits).**`; contains exactly ONE fenced json block (assertion target); contains the literal substring `Autonomy hint: AUTO-ELIGIBLE` when batchAuto is true, OR `Autonomy hint: NEEDS-REVIEW` when any edit failed the per-edit gate.

AUTO-ELIGIBLE per edit (submit.ts:175-177): intent must be either change-wording or replace-photo AND signalCount(edit) must be >= 2.

signalCount in validate.ts increments per locator signal present: i18nKey plus i18nAttr together count as 1 signal; nearbyText length >= 20 chars counts as 1 signal; nearestHeading presence counts as 1 signal; domPath presence counts as 1 signal; etc.

To force AUTO-ELIGIBLE in this canary (matching unit-mode scenario 2 at lines 241-289): each edit has i18nKey, i18nAttr equal to 'data-i18n', and a nearbyText of at least 30 chars; that yields at least 2 signals per edit which yields AUTO-ELIGIBLE per edit which yields batchAuto true which yields the AUTO-ELIGIBLE autonomy hint.

ACTION FIRES (claude.yml:42): an issue labelled exactly `client-feedback` triggers the Action. The Action steps (claude.yml:70-106) run Claude Code on the issue and open a PR at branch feedback/issue-<N>-<slug>. The squash-merge step (claude.yml:120-136) is gated by `steps.labels.outputs.auto == 'true' && env.DRY_RUN != 'true'`. With DRY_RUN equal to true, the merge does NOT happen. The PR stays open. The result comment is posted at claude.yml:138-154.

PR BRANCH PATTERN (CLAUDE_FEEDBACK.md §8 line 246, claude.yml:126): the convention is `feedback/issue-<N>-batch-<M>` where N is the issue number and M is the edit count. The Action squash-merge step explicitly filters with a prefix match: `gh pr list --json number,headRefName --jq '[.[] | select(.headRefName | startswith("feedback/issue-<ISSUE>-"))]'`. The prefix match accepts both single-edit and batch branches.

CACHE-BUST PROOF (CONTEXT D-08 plus BaseLayout.astro:1024-1037): the Astro template uses `<script is:inline define:vars={{ feedbackVer: FEEDBACK_INJECT_VER }}>` at line 1025. At build time, define:vars interpolates into the inline JS as the literal `const feedbackVer = "3";` in the rendered HTML. Running curl -sL against the root URL and grepping for the pattern `const feedbackVer = "[0-9]+"` should return exactly `const feedbackVer = "3"`.

ASSET HEAD PROBE (CONTEXT D-10): running curl -sI against /feedback-inject.js?v=3 returns a response whose first line matches the regex /^HTTP\/[12](\.[01])?\s+200/ (HTTP/1.1 200 or HTTP/2 200).

DRY_RUN GATE (claude.yml:55): the workflow reads `DRY_RUN: ${{ vars.DRY_RUN }}` which pulls from repo Variables (not Secrets). The bare subcommand `gh variable get DRY_RUN` returns the value as a plain string (the literal `true` or `false`, without quotes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement runCanaryV2() body with DRY_RUN pre-check, live POST, PR poll, cache-bust proof, asset HEAD probe, and full cleanup</name>
  <files>scripts/smoke-feedback-v2.mjs</files>
  <read_first>
    - scripts/smoke-feedback-v2.mjs (Plan 05-02-modified version; runCanaryV2 stub already exists from 05-01)
    - src/pages/api/feedback/submit.ts (lines 432-610 — handleV2Batch; lines 500-503 — title; lines 474-476 — autonomy hint strings; lines 590-603 — success response)
    - src/pages/api/feedback/validate.ts (signalCount + INTENTS — to understand AUTO-ELIGIBLE conditions)
    - .github/CLAUDE_FEEDBACK.md (§8, lines 194-281 — full batch protocol the Action follows)
    - .github/workflows/claude.yml (lines 39-49 Action gate, lines 55 + 120-136 DRY_RUN gate, lines 138-154 result comment)
    - src/lib/feedback-version.ts (FEEDBACK_INJECT_VER='3' — the value the cache-bust grep must match)
    - src/layouts/BaseLayout.astro (lines 1024-1037 — the inline define:vars block that emits `const feedbackVer = "3"` into deployed HTML)
    - .planning/phases/05-post-deploy-verification/05-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10)
  </read_first>
  <action>
Inside scripts/smoke-feedback-v2.mjs, replace the stub body of runCanaryV2() (added by Plan 05-01 Task 1) with the full OPS-05 canary scenario. Implement the following six phases sequentially in the function body.

Phase A is pre-flight gates that MUST run BEFORE any state mutation. Step A1: assert TARGET_URL is set; if not, log a FAIL line stating TARGET_URL is not set and exit with code 2. Step A2: assert `gh --version` succeeds by wrapping execFileSync with gh and ['--version'] in try/catch; on failure log a FAIL line stating gh CLI is not available and exit 2. Step A3 implements the DRY_RUN safety gate per CONTEXT D-06. Invoke execFileSync from node:child_process with gh and the arguments ['variable', 'get', 'DRY_RUN'] with encoding utf8, then trim the result. The expected value is the literal string `true` (no quotes). If the value is not exactly `true`, log this exact operator message: `FAIL: DRY_RUN repo variable is "<actual_value>" but must be "true" for the v2 canary. Run: gh variable set DRY_RUN -b true (then re-run this canary). Do NOT remove this safety check — without DRY_RUN=true the Action will squash-merge the canary's test PR into main.` Then exit with code 2. Do NOT auto-set DRY_RUN — that is an operator-owned safety toggle per CONTEXT D-06.

Phase B is the payload POST. State mutation begins here, so everything from here through Phase F runs inside a single try/finally. Step B1: build a tsSuffix const set to new Date().toISOString(). Step B2: build the edits array with 2 entries (per CONTEXT Claude's Discretion picking 2-3 small text-only edits). Each entry is an object with these exact fields: intent set to 'change-wording'; pageRoute set to '/' for the first edit and '/the-compound/' for the second; confirmationAccepted set to true; intentDetail as a nested object with newTextEn set to 'Canary v2 edit 1 — Phase 5 verification ' + tsSuffix for edit 1 and 'Canary v2 edit 2 — Phase 5 verification ' + tsSuffix for edit 2, and okToTranslate set to true; i18nKey set to 'home.canary.test1' for edit 1 and 'compound.canary.test2' for edit 2; i18nAttr set to 'data-i18n'; nearbyText set to 'long enough nearby text for at least one locator signal for canary v2 testing'; nearestHeading set to 'Welcome' for edit 1 and 'The Compound' for edit 2. Add a code comment explaining: "Fake i18nKey values are intentional — the canary verifies the pipeline mechanics (issue creation, Action fire, PR open, result comment, autonomy hint, cache-bust proof), not edit application. The Action may label the issue needs-review after locator resolution; that is an acceptable OPS-05 outcome. The autonomy hint at issue creation time still says AUTO-ELIGIBLE because the per-edit signal-count gate uses presence-of-i18nKey, not validity-of-i18nKey."

Step B3: POST the payload via the canaryPost helper (from Plan 05-01 Task 1) using a top-level batch envelope: an object with schemaVersion set to 2, batch set to true, and edits set to the edits array. Assert response.status === 200; on failure log a FAIL line including the actual status code and response body text, then throw.

Step B4: Parse the response body as JSON. Assert body.ok === true and typeof body.issueNumber === 'number' and typeof body.issueUrl === 'string'; on failure log and throw.

Step B5: Capture body.issueNumber to an outer-scope variable named issueNumber. This variable is needed for Phase D, Phase F, and the evidence file in Task 2. Log a PASS line `PASS: v2 canary HTTP 200 with issueNumber=` followed by the integer.

Phase C is issue shape assertions. Step C1: invoke execFileSync with gh and arguments ['issue', 'view', String(issueNumber), '--json', 'title,labels,body']; parse the resulting JSON. Step C2: assert the title matches the regex /^\\[Feedback\\] batch of \\d+ edits — / — it must NOT have the [TEST] prefix and must NOT match the single-edit shape. On failure log a FAIL line including the actual title and throw. Step C3: assert the labels array contains an entry where name equals the literal string `client-feedback` (NOT client-feedback-test). On failure log and throw. Step C4: assert the issue body contains exactly ONE fenced JSON block. Build a regex via the RegExp constructor (RegExp(String.fromCharCode(96).repeat(3) + "json", "g")) — this matches three backticks followed by the literal json. Apply body.match(regex) and assert the length of the resulting array is exactly 1. On failure log and throw. Step C5: assert the body contains the literal substring `Autonomy hint: AUTO-ELIGIBLE` (per the canary's AUTO-eligible edit shape). On failure log `FAIL: expected AUTO-ELIGIBLE autonomy hint, got: ` followed by the first 300 chars of the body, then throw. Step C6: log `PASS: v2 canary issue body matches batch shape (title + single json fence + AUTO-ELIGIBLE hint)`.

Phase D is the PR poll. The Action fires asynchronously and typically takes 30-90s after issue creation per CONTEXT Claude's Discretion. Implement a poll-with-timeout loop: 10s intervals, 5min cap. Set const N to issueNumber, const M to edits.length (which is 2), and const expectedBranchPrefix to the string `feedback/issue-` + N + '-batch-' + M. Declare two outer-scope variables prNumber set to null and prBranch set to null. Capture pollStart from Date.now(). Set POLL_INTERVAL_MS to 10000 and POLL_CAP_MS to 5 * 60000. Loop while Date.now() minus pollStart is less than POLL_CAP_MS. Inside the loop, invoke execFileSync with gh and arguments ['pr', 'list', '--state', 'open', '--json', 'number,headRefName', '--search', 'head:' + expectedBranchPrefix] with encoding utf8 and JSON.parse the output. Find an entry where headRefName equals expectedBranchPrefix or where headRefName.startsWith('feedback/issue-' + N + '-'). If found, set prNumber and prBranch from that entry and break. Else await a setTimeout promise for POLL_INTERVAL_MS. If the loop exits with prNumber still null, log `FAIL: v2 canary timed out waiting for PR creation (5min). Check Action logs at https://github.com/MSizzle/moulin-a-reves/actions for issue ` + N + '.' and throw. Otherwise log `PASS: PR opened at branch ` + prBranch + ', PR #' + prNumber.

Then poll for the result comment. Wait up to 60 additional seconds (use the same 10s polling cadence) for a comment to appear on the issue. Invoke execFileSync with gh and arguments ['issue', 'view', String(N), '--json', 'comments', '--jq', '.comments[].body'] which returns a newline-separated list of comment bodies. Per claude.yml:145, when DRY_RUN is true AND auto-approved label is set the comment is `Dry run: this would have been auto-applied. A PR was opened for inspection; no merge was performed.` Look for the literal substring `Dry run` as the primary signal (other valid signals include `needs a person to look at it` per claude.yml:150 — accept any of those substrings as proof a result comment was posted). When found, log `PASS: result comment posted on issue ` + N. If the 60s wait elapses with no matching comment, log `FAIL: no result comment on issue ` + N + ' within 60s of PR creation.' and throw.

Phase E is the cache-bust proof and asset HEAD probe per CONTEXT D-08 and D-10. Step E1: invoke execFileSync with curl and arguments ['-sL', TARGET_URL + '/'] with encoding utf8 to capture the deployed HTML into a const named html. Assert html includes the exact substring `const feedbackVer = "3"`. On failure log `FAIL: cache-bust grep — expected 'const feedbackVer = "3"' in deployed HTML; got: ` followed by the first 1000 chars of html, then throw. On success log `PASS: cache-bust grep — const feedbackVer = "3" present in deployed HTML`. Step E2: invoke execFileSync with curl and arguments ['-sI', TARGET_URL + '/feedback-inject.js?v=3'] with encoding utf8 to capture the HEAD response into a const named head. Take the first line by splitting on newlines. Assert that first line matches the regex /^HTTP\\/[12](\\.[01])?\\s+200/ (matches HTTP/1.1 200 or HTTP/2 200). On failure log `FAIL: asset HEAD — expected 200, got: ` + first_line, then throw. On success log `PASS: asset HEAD — /feedback-inject.js?v=3 returned 200`.

Right before exiting the try block, set an outer-scope flag named assertionsPassed to true.

Phase F is cleanup, inside a finally block. CONTEXT D-07 makes cleanup mandatory; failure to clean up is a hard fail. Order matters per CONTEXT D-07 security note: close PR BEFORE deleting branch (GitHub rejects branch delete if a PR references it). Declare a local errs array initialized empty. If prNumber is not null, wrap a call to execFileSync with gh and arguments ['pr', 'close', String(prNumber), '--comment', 'Closed by canary — Phase 5 OPS-05 verification (' + new Date().toISOString() + ')'] in try/catch; on catch push a descriptive error string into errs. If prBranch is not null, await a 2-second setTimeout (lets GitHub propagate the PR close before branch delete), then wrap a call to execFileSync with git and arguments ['push', '--delete', 'origin', prBranch] in try/catch; on catch (the git push may fail if local auth differs) fall back to invoking execFileSync with gh and arguments ['api', 'repos/MSizzle/moulin-a-reves/git/refs/heads/' + prBranch, '-X', 'DELETE'] in a nested try/catch; on the nested catch push a descriptive error string into errs. If issueNumber is not null, wrap calls to execFileSync with gh and arguments ['issue', 'comment', String(issueNumber), '--body', 'Closed by canary — Phase 5 OPS-05 verification (' + new Date().toISOString() + ')'] and then execFileSync with gh and arguments ['issue', 'close', String(issueNumber)] in try/catch; on catch push a descriptive error string into errs.

After the finally block executes: if errs is empty AND assertionsPassed is true, log `PASS: cleanup — PR #` + prNumber + ` closed, branch ` + prBranch + ` deleted, issue #` + issueNumber + ` closed`, then log `=== v2 canary: PASS ===`, then exit 0. If errs is non-empty and assertionsPassed is true, log a WARN header `WARN: cleanup partial — manual action required:` followed by each entry in errs on its own indented line, then log `=== v2 canary: PASS (cleanup partial) ===` and exit 3 (per CONTEXT discretion exit-code semantics). If assertionsPassed is false (the try block threw), the assertion failure already exited; log `=== v2 canary: FAIL ===` if reached and exit 1.

Exit semantics summary: exit 0 means all assertions PASS and cleanup PASS; exit 1 means an assertion failed (the try block threw and the throw propagated to the harness); exit 2 means a setup pre-check failed (DRY_RUN not true, gh not on PATH, TARGET_URL unset); exit 3 means assertions PASS but cleanup failed and the operator must manually verify the test artifacts are gone.

Code quality: do NOT embed fenced markdown code blocks inside the Node source. Use plain JavaScript template literals or string concatenation. The cleanup pattern described above is illustrative — the executor adapts it to the JavaScript file's existing style and naming conventions.
  </action>
  <verify>
    <automated>scripts/canary.sh v2 2>&amp;1 | tee /tmp/v2-canary-out.txt | grep -c "=== v2 canary: PASS ===" | grep -q 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gh.*variable.*get.*DRY_RUN" scripts/smoke-feedback-v2.mjs` returns >= 1
    - `grep -c "schemaVersion: 2" scripts/smoke-feedback-v2.mjs` returns >= 2 (existing scenario 2 + new runCanaryV2)
    - `grep -c "batch: true" scripts/smoke-feedback-v2.mjs` returns >= 2
    - `grep -c "feedback/issue-" scripts/smoke-feedback-v2.mjs` returns >= 1 (PR branch match)
    - `grep -c "AUTO-ELIGIBLE" scripts/smoke-feedback-v2.mjs` returns >= 1 (autonomy hint assertion)
    - `grep -c 'const feedbackVer = "3"' scripts/smoke-feedback-v2.mjs` returns >= 1 (cache-bust grep target)
    - `grep -c "feedback-inject.js?v=3" scripts/smoke-feedback-v2.mjs` returns >= 1 (asset HEAD URL)
    - `grep -c "git.*push.*--delete.*origin" scripts/smoke-feedback-v2.mjs` returns >= 1 (branch cleanup)
    - `grep -c "gh.*pr.*close" scripts/smoke-feedback-v2.mjs` returns >= 1
    - `grep -c "gh.*issue.*close" scripts/smoke-feedback-v2.mjs` returns >= 1
    - `grep -c "finally" scripts/smoke-feedback-v2.mjs` returns >= 2 (one already in 05-02 runCanaryV1; plus one in runCanaryV2)
    - `grep -c "=== v2 canary: PASS ===" scripts/smoke-feedback-v2.mjs` returns >= 1
    - Pre-flight gate works: if DRY_RUN is unset or not the literal `true`, `scripts/canary.sh v2` exits 2 with stderr containing `DRY_RUN repo variable`
    - Live run (with DRY_RUN=true pre-set): `npm run canary:v2` exits 0 with `=== v2 canary: PASS ===` in stdout
    - Live run stdout contains all expected PASS lines: `PASS: v2 canary HTTP 200`, `PASS: v2 canary issue body matches batch shape`, `PASS: PR opened at branch feedback/issue-`, `PASS: result comment posted on issue`, `PASS: cache-bust grep — const feedbackVer = "3" present in deployed HTML`, `PASS: asset HEAD — /feedback-inject.js?v=3 returned 200`, `PASS: cleanup`
    - After live run, test artifacts cleaned: `gh pr view <prNumber> --json state --jq .state` returns CLOSED; `git ls-remote --heads origin <prBranch>` returns empty; `gh issue view <issueNumber> --json state --jq .state` returns CLOSED
    - Unit-mode regression preserved: `npx tsx scripts/smoke-feedback-v2.mjs` (no TARGET_URL) returns `5 passed, 0 failed`
    - OPS-02 fence intact: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0
  </acceptance_criteria>
  <done>
    runCanaryV2() runs end-to-end against the live deploy. PASS banner printed. Test issue, PR, and branch fully cleaned up in finally. DRY_RUN gate proved its safety value (Action ran but did not merge). Cache-bust proof + asset HEAD probe both PASS. Operator can re-run `npm run canary:v2` at any time and get an idempotent PASS, provided DRY_RUN remains true.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture canary evidence to .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md AND assert final OPS-02 fence</name>
  <files>.planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md</files>
  <read_first>
    - /tmp/v2-canary-out.txt (full canary log captured by Task 1's tee redirect)
    - .planning/phases/05-post-deploy-verification/05-CONTEXT.md (D-05, D-06, D-07, D-08, D-10 — what to record)
    - .planning/REQUIREMENTS.md (OPS-05 success criterion verbatim)
    - .planning/phases/05-post-deploy-verification/05-CANARY-V1-EVIDENCE.md (existing format from Plan 05-02 — match structure for consistency)
  </read_first>
  <action>
Create .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md as the audit trail for OPS-05 satisfaction. Match the format established by 05-CANARY-V1-EVIDENCE.md (Plan 05-02 Task 2).

Section 1 — YAML frontmatter at the top of the file. Required keys: requirement set to OPS-05; verified_at set to the ISO 8601 UTC timestamp of the canary run; target_url set to https://www.moulinareves.com; status set to passed; issue_number set to the integer N from the canary run; issue_url set to the GitHub URL; pr_number set to the integer PR number; pr_url set to the GitHub PR URL; pr_branch set to the branch string (e.g., feedback/issue-<N>-batch-2); autonomy_verdict set to AUTO-ELIGIBLE (the verdict the canary observed in the issue body); dry_run_state_at_canary set to the string true (with quotes around the value to preserve the YAML string type); issue_state_after_cleanup set to closed; pr_state_after_cleanup set to closed; branch_state_after_cleanup set to deleted; feedback_inject_ver_in_deployed_html set to the string 3 (quoted); feedback_inject_js_v3_http_status set to the integer 200.

Section 2 — A heading plus one-paragraph summary explaining what OPS-05 is, how this canary satisfies it, and pointing to the ROADMAP line that defines it (line 92).

Section 3 — A heading `## Canary log` followed by a fenced code block containing the verbatim contents of /tmp/v2-canary-out.txt (the full canary stdout captured by Task 1's tee).

Section 4 — A heading `## Issue snapshot` followed by a JSON-fenced block. The content is the output of execFileSync with gh and arguments ['issue', 'view', String(issueNumber), '--json', 'number,title,labels,state,closedAt,body'] run against the now-closed test issue.

Section 5 — A heading `## PR snapshot` followed by a JSON-fenced block. The content is the output of execFileSync with gh and arguments ['pr', 'view', String(prNumber), '--json', 'number,title,headRefName,baseRefName,state,closedAt,body'].

Section 6 — A heading `## Result comment from Action` followed by a fenced code block. Paste the Action's result comment from the issue thread verbatim. Confirm it contains the "Dry run" substring (or note whatever the Action emitted, per claude.yml:145).

Section 7 — A heading `## Cache-bust proof (D-08, D-10)` containing two sub-sections. First sub-section: the verbatim output of the command `curl -sL https://www.moulinareves.com/ | grep -oE 'const feedbackVer = "[0-9]+"'` — should be exactly `const feedbackVer = "3"`. Second sub-section: the first 3 lines of the command `curl -sI https://www.moulinareves.com/feedback-inject.js?v=3` — status line plus content-type plus content-length or cache-control.

Section 8 — A heading `## Assertions` followed by a markdown table with these columns: number, assertion, expected, actual, status. Fifteen rows in this order: DRY_RUN repo var (expected the literal "true"), HTTP status (expected 200), Response shape (expected ok+issueNumber+issueUrl), Issue title (expected matches regex /^\\[Feedback\\] batch of \\d+ edits — /), Issue label (expected contains client-feedback), Single json fence in body (expected count 1), Autonomy hint (expected AUTO-ELIGIBLE substring), PR created (expected branch starting with feedback/issue-<N>-batch-), PR not merged because DRY_RUN (expected open or closed-without-merge), Result comment posted (expected Dry run substring), Cache-bust HTML (expected const feedbackVer = "3"), Asset HEAD (expected HTTP 200 on /feedback-inject.js?v=3), Cleanup PR closed (expected state CLOSED), Cleanup branch deleted (expected git ls-remote empty for that branch), Cleanup issue closed (expected state CLOSED). All status cells should be PASS.

Section 9 — A heading `## OPS-02 fence (Phase 5 final)` followed by the verbatim output of the command `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l`. The output MUST be `0`. State this explicitly in one sentence below the output: "Phase 5's modifications (scripts/smoke-feedback-v2.mjs, scripts/canary.sh, package.json) are all OUTSIDE the OPS-02 fence; the editor flow remains byte-for-byte unchanged."

Section 10 — A one-sentence conclusion confirming OPS-05 is satisfied, plus a pointer that the full v1.1 milestone is now ready for `/gsd-verify-work 5` and `/gsd-audit-milestone v1.1`.

If Task 1's canary run FAILED (assertions or cleanup): do NOT create this file. The plan fails; the orchestrator transitions to gap-closure mode.

After writing: verify the file exists, contains the OPS-05 frontmatter, references pr_number, has at least 50 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md &amp;&amp; grep -c "^requirement: OPS-05" .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md | grep -q 1 &amp;&amp; grep -c "^pr_number:" .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md | grep -q 1 &amp;&amp; grep -c "feedback_inject_ver_in_deployed_html" .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md | grep -q 1 &amp;&amp; wc -l < .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md | awk '{exit ($1 >= 50) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md` exists
    - Frontmatter contains `requirement: OPS-05` and `status: passed` and `target_url: https://www.moulinareves.com`
    - `issue_number:` field is a real integer
    - `pr_number:` field is a real integer (NOT a placeholder)
    - `pr_branch:` field starts with `feedback/issue-`
    - `dry_run_state_at_canary:` field is the literal string `"true"` (quoted in YAML to preserve string type)
    - `feedback_inject_ver_in_deployed_html: "3"` is present
    - `feedback_inject_js_v3_http_status: 200` is present
    - `issue_state_after_cleanup: closed` and `pr_state_after_cleanup: closed` and `branch_state_after_cleanup: deleted` all present
    - File contains a `## Canary log` section with at least the strings `PASS: v2 canary HTTP 200` and `=== v2 canary: PASS ===`
    - File contains a `## Issue snapshot` section with a JSON fence
    - File contains a `## PR snapshot` section with a JSON fence
    - File contains a `## Result comment from Action` section whose body contains EITHER the `Dry run` substring (claude.yml:145, auto-approved + DRY_RUN=true path) OR the `needs a person to look at it` substring (claude.yml:150, needs-review path) — both phrasings prove a result comment was posted (the ACTION-03 / OPS-05 mechanic being verified, independent of which classifier Claude landed on). Concrete check: `grep -Ec 'Dry run|needs a person to look at it' .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md` returns ≥ 1.
    - File contains a `## Cache-bust proof` section containing the literal `const feedbackVer = "3"`
    - File contains an `## Assertions` table with at least 15 data rows
    - File contains an `## OPS-02 fence (Phase 5 final)` section asserting `0` lines
    - File is >= 50 lines
    - `gh pr view <pr_number> --json state --jq .state` returns CLOSED (post-cleanup verification)
    - `git ls-remote --heads origin <pr_branch>` returns empty (post-cleanup verification)
    - `gh issue view <issue_number> --json state --jq .state` returns CLOSED (post-cleanup verification)
    - OPS-02 fence final: `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Evidence file captured. OPS-05 has a falsifiable, dated, audit-grade artifact with PR + cache-bust proof + cleanup confirmation. OPS-02 fence verified clean. Phase 5 complete. v1.1 milestone is ready for `/gsd-verify-work 5` and `/gsd-audit-milestone v1.1`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → harness | Operator-controlled; trusted. DRY_RUN pre-check ensures operator has acknowledged the safety toggle. |
| harness → www.moulinareves.com/api/feedback/submit | Crosses public internet over HTTPS. Authenticated via maison_session HMAC cookie. |
| harness → github.com/MSizzle/moulin-a-reves (via gh CLI) | Authenticated via operator's local gh auth. Includes PR-creation and branch-delete operations. |
| Action workflow → main branch | Gated by DRY_RUN; with DRY_RUN=true the workflow never reaches the squash-merge step. |
| harness → curl (curl -sL, curl -sI) | Read-only HTTP requests against the deployed site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | DRY_RUN unset → unintentional merge of canary edits into main | mitigate | Pre-check (Phase A Step A3) asserts `gh variable get DRY_RUN` returns the literal `true` BEFORE creating the issue; aborts with exit 2 and explicit operator message if not. Do NOT auto-set DRY_RUN — it is an operator-owned safety toggle (CONTEXT D-06). |
| T-05-14 | Tampering | Test issues + PRs polluting production label/branch namespace | mitigate | Cleanup in finally (Phase F) is MANDATORY (CONTEXT D-07). Cleanup runs even on mid-canary failure. Order: close PR first, then delete branch (GitHub rejects branch delete if PR open). Failure to clean up exits 3 with operator instructions. |
| T-05-15 | Information disclosure | Canary leaking sensitive info into public issue body | mitigate | Payload content is fixed in source (`Canary v2 edit N — Phase 5 verification` + ISO timestamp); no real client data, no internal URLs, no PII. Issue is closed within minutes. |
| T-05-16 | Repudiation | Lost audit trail after cleanup | mitigate | Cleanup leaves `Closed by canary` comment trail on issue + PR. Evidence file (Task 2) captures issue + PR snapshots BEFORE cleanup is verified. Combined with git commit timestamp, the run is fully reconstructable. |
| T-05-17 | Denial of service | Race condition on PR cleanup (Action still processing when canary tries to delete branch) | mitigate | Phase F sequencing: close PR via gh pr close first (this works even if Action is mid-run); then 2-second await for GitHub propagation; then attempt git push --delete; on git failure fall back to `gh api refs/heads/<branch> -X DELETE` (per CONTEXT D-07). |
| T-05-18 | Tampering | Canary payload accidentally hits a real `client-feedback` workflow if testMode were set | accept | OPS-05's design intentionally USES the production label so the Action fires; DRY_RUN halts the merge. testMode is NOT set (would route to client-feedback-test which skips the Action and fails OPS-05's PR-side success criterion). The DRY_RUN gate is the correct isolation seam here (CONTEXT D-05). |
| T-05-19 | Elevation of privilege | gh CLI scoped to org-wide writes | mitigate | The canary uses only gh issue view/comment/close, gh pr list/view/close, gh variable get, gh api refs heads DELETE — all scoped to the operator's own repository. No org-level operations. No repo-settings modifications. |
| T-05-20 | Spoofing | Operator running canary against a TARGET_URL impersonating production | mitigate | DEPLOY_URL defaults to https://www.moulinareves.com in canary.sh; override is explicit env var. The canary's payload is non-sensitive (canary marker text); worst case of a malicious TARGET_URL is the malicious server receives the canary text + a valid maison_session cookie (the cookie is HMAC-signed but reusable for the canary's session — recommend operator regen DASHBOARD_PASSWORD if a misdirected canary is suspected). Document this caution in CONTEXT D-03 forward-link in canary.sh header. |
</threat_model>

<verification>
Phase-level Plan 05-03 verification:

1. Live canary PASS: `npm run canary:v2` exits code 0 with `=== v2 canary: PASS ===` in stdout (assuming DRY_RUN=true pre-set).
2. Pre-flight gate works: `gh variable set DRY_RUN -b false && npm run canary:v2` exits 2 with stderr containing `DRY_RUN repo variable`. (Don't actually run this — operator can mentally verify; the unit-mode tests confirm the gate code exists.)
3. Test artifacts cleaned post-canary: `gh pr view <pr_number> --json state --jq .state` returns CLOSED; `git ls-remote --heads origin <pr_branch>` is empty; `gh issue view <issue_number> --json state --jq .state` returns CLOSED.
4. Cache-bust proof confirmed: `curl -sL https://www.moulinareves.com/ | grep -oE 'const feedbackVer = "[0-9]+"'` returns `const feedbackVer = "3"`.
5. Asset reachable: `curl -sI https://www.moulinareves.com/feedback-inject.js?v=3 | head -1` matches HTTP 200.
6. Evidence file present: `.planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md` exists with OPS-05 frontmatter, all assertion targets, and OPS-02 fence section asserting 0.
7. Unit-mode regression intact: `npx tsx scripts/smoke-feedback-v2.mjs` returns `5 passed, 0 failed`.
8. OPS-02 fence intact (PHASE 5 FINAL): `git diff main -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` returns 0.
9. OPS-05 requirement satisfied: the canary log + issue + PR + result comment + cache-bust proof + asset HEAD + cleanup confirmation form the complete audit trail.
</verification>

<success_criteria>
- All 2 tasks' acceptance criteria pass
- Live OPS-05 canary returns exit 0 with PASS banner (DRY_RUN pre-set to true by operator)
- Test issue + PR + branch created, asserted, fully cleaned up
- Cache-bust proof + asset HEAD probe both PASS (proves cache-bust ship took effect end-to-end on the deployed site)
- Evidence file committed
- OPS-02 fence still 0 (Phase 5's modifications all outside the fence)
- Unit-mode regression intact
</success_criteria>

<output>
After completion, create `.planning/phases/05-post-deploy-verification/05-03-SUMMARY.md` with:
- Files modified (2): scripts/smoke-feedback-v2.mjs (runCanaryV2 body filled in), .planning/phases/05-post-deploy-verification/05-CANARY-V2-EVIDENCE.md (NEW)
- Live canary execution timestamp + issue number + PR number + branch name + autonomy verdict
- Cache-bust grep output (the exact string `const feedbackVer = "3"`)
- Asset HEAD response status line
- Cleanup confirmation (PR closed, branch deleted, issue closed)
- OPS-02 fence final assertion result (0)
- requirements-completed: [OPS-05]
- Note: with this plan complete, Phase 5 is done. v1.1 milestone has 25/25 requirements satisfied (Phase 4: 23 + Phase 5: 2). Next: `/gsd-verify-work 5` then `/gsd-audit-milestone v1.1` then `/gsd-complete-milestone v1.1`.
</output>
