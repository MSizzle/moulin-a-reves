---
phase: 08-matcher-endpoint-match-inject-overlay-side-panel-per-page-mo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/feedback-version.ts
  - CLAUDE.md
autonomous: true
requirements: [OPS-01, OPS-04]
must_haves:
  truths:
    - "src/lib/feedback-version.ts exports a second named const MATCH_INJECT_VER set to '1'"
    - "FEEDBACK_INJECT_VER remains exactly '4' (byte-identical to pre-v1.3 baseline)"
    - "CLAUDE.md §Feedback mode contains a one-line pointer to /api/feedback/match + per-page review flow"
    - "Downstream plans (03 BaseLayout loader, 04 feedback.astro iframe builder) can import MATCH_INJECT_VER without further changes to this file"
  artifacts:
    - path: "src/lib/feedback-version.ts"
      provides: "MATCH_INJECT_VER named export (cache-bust constant)"
      contains: "export const MATCH_INJECT_VER ="
    - path: "CLAUDE.md"
      provides: "Operator-discovery note for per-page review flow"
      contains: "/api/feedback/match"
  key_links:
    - from: "src/lib/feedback-version.ts"
      to: "BaseLayout.astro + feedback.astro (downstream consumers in Plans 03 and 04)"
      via: "named ESM import { MATCH_INJECT_VER }"
      pattern: "import\\s+\\{[^}]*MATCH_INJECT_VER[^}]*\\}\\s+from\\s+['\"]\\.\\./lib/feedback-version['\"]"
---

<objective>
Add the cache-bust source-of-truth constant `MATCH_INJECT_VER` to the existing `src/lib/feedback-version.ts` module (D-23) and add a one-line operator-discovery note to `CLAUDE.md` §Feedback mode pointing future Claude sessions at the per-page review flow + `/api/feedback/match` (OPS-04).

Purpose: Locks the cache-bust contract for the new `public/feedback-match-inject.js` script before any consumer plan (03 BaseLayout loader, 04 feedback.astro iframe builder) needs it. Mechanically tiny but architecturally load-bearing — Plans 03/04 import this const, and the v1.1 OPS-02 fence requires `FEEDBACK_INJECT_VER` to stay at `'4'` (byte-identical).

Output: One extended module (`feedback-version.ts` gains a second named export + matching comment block) and one extended doc section (`CLAUDE.md` §Feedback mode gains a single bullet line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-matcher-endpoint-match-inject-overlay-side-panel-per-page-mo/08-CONTEXT.md
@.planning/phases/08-matcher-endpoint-match-inject-overlay-side-panel-per-page-mo/08-PATTERNS.md
@src/lib/feedback-version.ts
@CLAUDE.md

<interfaces>
<!-- Current full contents of src/lib/feedback-version.ts (13 lines). -->
<!-- The new MATCH_INJECT_VER export is APPENDED below the existing FEEDBACK_INJECT_VER line. -->
<!-- DO NOT change the existing comment block or the FEEDBACK_INJECT_VER value. -->

From src/lib/feedback-version.ts (existing, READ-ONLY for FEEDBACK_INJECT_VER):
```typescript
// Single source of truth for the client-feedback inject cache-bust version.
//
// public/feedback-inject.js is loaded with `defer` from BaseLayout.astro and
// is cached aggressively by the Vercel CDN (same landmine as editor-inject.js,
// see CLAUDE.md). BUMP THIS STRING on every behavioural change to
// public/feedback-inject.js so the `?v=` query string changes and clients
// stop being served the stale script.
//
// Imported by:
//   - src/layouts/BaseLayout.astro  (the `?feedback=1` inline loader)
//   - src/pages/feedback.astro      (the iframe `src` query string)
export const FEEDBACK_INJECT_VER = '4';
```

From CLAUDE.md §"Feedback mode (`?feedback=1` → auto-code → auto-deploy)" (target section for one-line addition):
The section currently documents the v1.1 sibling-to-editor flow, cache-bust contract, `feedback-incoming/` rule, and v2 batch submissions §8 link. The new one-line bullet sits as a new bullet at the END of the existing list (after the v2 batch submissions bullet) so the section stays chronological-by-milestone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append MATCH_INJECT_VER export to src/lib/feedback-version.ts (D-23, OPS-01)</name>
  <files>src/lib/feedback-version.ts</files>
  <action>
APPEND to the existing `src/lib/feedback-version.ts` (do NOT rewrite the file; preserve the existing 13 lines byte-for-byte — `FEEDBACK_INJECT_VER = '4'` MUST remain unchanged). Add a blank line after the existing `export const FEEDBACK_INJECT_VER = '4';`, then append the following block verbatim from `08-PATTERNS.md` §"src/lib/feedback-version.ts (EXTEND)" → "Phase 8 addition (D-23)":

  - Comment block explaining (1) what `MATCH_INJECT_VER` cache-busts, (2) the two import sites (`src/layouts/BaseLayout.astro` for the conditional `?feedback=1&matchSet=…` loader per D-22, and `src/pages/feedback.astro` for the iframe `src` query string when building per-page match URLs), and (3) the "BUMP THIS STRING on every behavioural change" instruction — exact phrasing matching the existing `FEEDBACK_INJECT_VER` comment style.
  - Then: `export const MATCH_INJECT_VER = '1';` (initial value `'1'` per D-23).

Do NOT touch `FEEDBACK_INJECT_VER` (Phase 9's OPS-02 fence check covers `feedback-inject.js`, and the matching version constant must stay at `'4'` because the script byte-content is unchanged).
  </action>
  <verify>
    <automated>node -e "const m = require('./src/lib/feedback-version.ts'); console.log(m.FEEDBACK_INJECT_VER, m.MATCH_INJECT_VER);" 2>/dev/null || npx tsx -e "import('./src/lib/feedback-version.ts').then(m => { if (m.FEEDBACK_INJECT_VER !== '4') process.exit(1); if (m.MATCH_INJECT_VER !== '1') process.exit(2); console.log('OK'); })" || (grep -E "^export const FEEDBACK_INJECT_VER = '4';" src/lib/feedback-version.ts && grep -E "^export const MATCH_INJECT_VER = '1';" src/lib/feedback-version.ts)</automated>
  </verify>
  <done>
- `grep -c "^export const FEEDBACK_INJECT_VER = '4';" src/lib/feedback-version.ts` returns `1`.
- `grep -c "^export const MATCH_INJECT_VER = '1';" src/lib/feedback-version.ts` returns `1`.
- File has exactly two exported constants (`grep -cE '^export const' src/lib/feedback-version.ts` returns `2`).
- The line `export const FEEDBACK_INJECT_VER = '4';` is byte-for-byte unchanged from the pre-v1.3 baseline (verify with `git diff` showing only additions, zero deletions, zero modifications to lines 1-13).
- `npm run build` (or `astro check`) compiles without errors against the new export.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add per-page review pointer to CLAUDE.md §Feedback mode (OPS-04)</name>
  <files>CLAUDE.md</files>
  <action>
LOCATE the existing `## Architectural Constraints` → `### Feedback mode (?feedback=1 → auto-code → auto-deploy)` subsection in `CLAUDE.md`. APPEND ONE new bullet at the END of the existing bullet list (after the `**Batch submissions (v2 schema).**` bullet) — do NOT modify existing bullets, do NOT rename the section heading.

Bullet copy (exact text — quote the literal so the executor doesn't paraphrase):

  > **Per-page review (v1.3).** A second flow on `/feedback` lets the operator paste a freeform edit list for one page; `POST /api/feedback/match` (Claude Haiku-backed, auth-gated, `prerender = false`) maps each line to a catalog element ID using `dist/edit-catalogs/<route>.json` from Phase 7, and a side panel turns each Approve into a v2 staged edit in `mar_feedback_staged_v1` — the v1.1 corner chip then submits the full batch unchanged. The new client surface lives in `public/feedback-match-inject.js` with its own `MATCH_INJECT_VER` cache-bust constant; `public/feedback-inject.js` stays byte-for-byte unchanged (OPS-02 fence). `ANTHROPIC_API_KEY` is server-only (Vercel Production scope); missing key degrades the matcher to a structured 500 without affecting the per-element click flow.

The bullet's purpose is operator/agent-discovery only — it must appear in the same Markdown bullet style as the surrounding section (leading `- `, bold first phrase). It is NOT load-bearing for any code path; future Claude sessions read this section to discover the second flow exists.

Do NOT touch any other section of `CLAUDE.md` (the file has cross-cutting concerns documentation that is not in scope for this phase).
  </action>
  <verify>
    <automated>grep -c "Per-page review (v1.3)" CLAUDE.md</automated>
  </verify>
  <done>
- `grep -c "Per-page review (v1.3)" CLAUDE.md` returns exactly `1`.
- `grep -c "/api/feedback/match" CLAUDE.md` returns at least `1` (and exactly `1` if no other section mentions it).
- `grep -c "MATCH_INJECT_VER" CLAUDE.md` returns at least `1`.
- The bullet sits inside the `### Feedback mode` subsection (verify by extracting the section: `awk '/### Feedback mode/,/^### /' CLAUDE.md | grep -c "Per-page review (v1.3)"` returns `1`).
- The existing `**Batch submissions (v2 schema).**` bullet is still present and unmodified (`grep -c "Batch submissions (v2 schema)" CLAUDE.md` returns `1`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | This plan only touches a module-scope constant and a documentation file; no runtime trust boundaries are crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | `feedback-version.ts` (cache-bust constant) | accept | Constant is bumped by humans in source control; tampering requires write access to the repo. Same threat model as `FEEDBACK_INJECT_VER` (in place since v1.1). |
| T-08-01-02 | Information Disclosure | `CLAUDE.md` operator note | accept | The note documents a public route (`/api/feedback/match`) that is auth-gated at the route handler (T-08-02-* register, Plan 02). Disclosing the route's existence to a future Claude session is intentional (OPS-04). No secrets in the note. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Per RESEARCH.md Package Legitimacy Audit fallback policy, this plan introduces zero new packages so the SC threat is N/A. (Plan 02 installs `@anthropic-ai/sdk` — its SC threat lives there.) |

Plan-level disposition rationale: this plan changes a single TypeScript const and one Markdown bullet. No code paths execute, no network calls are made, no user input is parsed. The OPS-02 fence assertion is mechanical (byte-identity of the existing `FEEDBACK_INJECT_VER` line and of `public/feedback-inject.js` — neither file touched here).
</threat_model>

<verification>
Run from the repo root:

1. `git diff src/lib/feedback-version.ts | grep -c '^-'` returns `0` (the existing FEEDBACK_INJECT_VER line and comment block are append-only — no deletions).
2. `git diff CLAUDE.md | grep -c '^-'` returns `0` (additive only — the bullet appended at end of the §Feedback mode subsection).
3. `grep -cE "^export const (FEEDBACK_INJECT_VER|MATCH_INJECT_VER)" src/lib/feedback-version.ts` returns `2`.
4. `node -e "import('./src/lib/feedback-version.ts').then(m => { if (typeof m.MATCH_INJECT_VER !== 'string') process.exit(1); })"` (or equivalent `astro check` clean run) succeeds.
5. OPS-02 fence check (passive — no files in fence list were touched): `git diff -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts public/feedback-inject.js src/pages/api/feedback/submit.ts src/pages/api/feedback/validate.ts | wc -l` returns `0`.

## OPS-02 fence assertion (must-have)

This plan touches `src/lib/feedback-version.ts` and `CLAUDE.md` ONLY. The OPS-02 fenced files are NOT in this plan's `files_modified` list:
- `public/editor-inject.js` — not touched
- `public/editor` — not touched
- `public/guardrails.js` — not touched
- `src/pages/api/site` — not touched
- `middleware.ts` — not touched
- `public/feedback-inject.js` — not touched
- `src/pages/api/feedback/submit.ts` — not touched
- `src/pages/api/feedback/validate.ts` — not touched
</verification>

<success_criteria>
- `src/lib/feedback-version.ts` exports both `FEEDBACK_INJECT_VER = '4'` (unchanged) and `MATCH_INJECT_VER = '1'` (new).
- `CLAUDE.md` §Feedback mode contains a discoverable one-line pointer to the per-page review flow + `/api/feedback/match`.
- The change set is fully additive; `git diff -- src/lib/feedback-version.ts CLAUDE.md` shows only `+` lines for the new content.
- OPS-02 fence files are byte-identical to pre-v1.3 baseline (no incidental touches).
</success_criteria>

<output>
Create `.planning/phases/08-matcher-endpoint-match-inject-overlay-side-panel-per-page-mo/08-01-SUMMARY.md` when done. Summary must record:
- Confirmation that `FEEDBACK_INJECT_VER` stayed at `'4'`.
- The line number where `MATCH_INJECT_VER = '1'` now lives (for downstream plans to reference).
- The bullet's location in `CLAUDE.md` (section anchor + ordinal position in the bullet list).
</output>
