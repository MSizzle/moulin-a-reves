---
milestone: v1.1
milestone_name: Batch Feedback Pipeline
audited: 2026-05-21T11:00:00Z
re_audited: 2026-05-21T18:00:00Z
status: passed
scores:
  requirements: 25/25
  phases: 2/2
  integration: 6/6
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 04-batch-pipeline-implementation
    items:
      - "D-03 Vercel tier reconciliation: RESOLVED. Phase 5 D-15 carry-forward confirmed operator chose Hobby tier (3 MB final). Both MAX_BATCH_BYTES constants remain at 3 MB. No action needed; closed."
      - "Smoke harness retarget seam: CLOSED. Phase 5 plan 05-01 implemented TARGET_URL env-var mode in smoke-feedback-v2.mjs. The same harness now serves both unit mode (5/5 pass) and canary mode (real fetch against deployed URL)."
      - "WR-01 (INFO): MAX_IMAGE_BYTES (12 MB per-photo) > MAX_BATCH_BYTES (3 MB per-batch). A first photo over 3 MB trips 'batch is full' message instead of a per-photo over-limit message. Deferred — low UX priority."
      - "WR-05 (INFO): MAX_IMAGE_BYTES imported in submit.ts but missing from the void ritual (lines 16-21). Cosmetic — not a runtime issue. Deferred."
      - "SUMMARY frontmatter inconsistency: 10 of 11 Phase 4 SUMMARY files use non-standard `requirements:` key or omit `requirements-completed:`. Source of truth is VERIFICATION.md (unambiguous). Metadata hygiene only. Deferred."
re_audit:
  previous_status: gaps_found
  previous_score: "23/25"
  gaps_closed:
    - OPS-04
    - OPS-05
  regressions: []
  notes: "Phase 5 completed; live canaries verified deployed pipeline works end-to-end. OPS-04: issue #89 created on deployed /api/feedback/submit with schemaVersion:1 shape, closed after canary. OPS-05: issue #96 created, Claude Action fired, PR #97 opened at feedback/issue-96-batch-2, DRY_RUN=true gate halted squash-merge, result comment captured verbatim, cache-bust proof: const feedbackVer = \"3\" in deployed HTML + /feedback-inject.js?v=3 HEAD = HTTP 200."
nyquist:
  enabled: false
  note: "workflow.nyquist_validation = false in .planning/config.json — skipped per workflow gate."
---

# Milestone v1.1: Batch Feedback Pipeline — Audit Report

**Initial audit:** 2026-05-21T11:00:00Z — `gaps_found` (Phase 5 unstarted; OPS-04 + OPS-05 unsatisfied)
**Re-audit:** 2026-05-21T18:00:00Z — **PASSED** (Phase 5 complete; all 25 requirements satisfied with live evidence)
**Scope:** Phases 4 + 5 (per ROADMAP.md)

---

## Milestone Definition of Done (from ROADMAP overview)

> "v1.1 Batch Feedback Pipeline replaces the current 1-edit-per-deploy feedback flow with a batched submission model: client stages multiple edits in a single session via a corner chip + panel UI, submits them as one POST, the server creates one GitHub issue containing all edits, and the Claude Code Action produces one branch / one commit / one PR / one deploy. Targets a ~10× reduction in deploys per client review pass."

Two phases:
1. **Phase 4: Batch Pipeline Implementation** — everything that ships in the PR (client UI, server v2 schema, issue construction, Action manual, cache-bust, editor-flow fence)
2. **Phase 5: Post-Deploy Verification** — canary checks that only run after Vercel deploys the merged code

The milestone is shippable when Phase 4 lands (a single ~450 LOC PR) AND Phase 5 canaries confirm the deployed pipeline works end-to-end against real GitHub + Claude Action infrastructure.

---

## Phase-Level Verification

| Phase | Status | Plans | Reqs Satisfied | VERIFICATION.md | Notes |
|---|---|---|---|---|---|
| 4: Batch Pipeline Implementation | ✅ **passed** | 11/11 | 23/23 | yes (re-verified after gap closure 2026-05-21T10:30Z) | 3 BLOCKERs (CR-01, CR-02, CR-03) closed via gap-closure plans 04-09/10/11; OPS-01 cache-bust re-bumped to `'3'` |
| 5: Post-Deploy Verification | ✅ **passed** | 3/3 | 2/2 | yes (verified 2026-05-21T17:30Z) | Live canaries completed: OPS-04 (issue #89, schemaVersion:1 regression), OPS-05 (issue #96, PR #97, DRY_RUN gate, cache-bust proof) |

---

## Requirements Coverage Matrix (3-source cross-reference)

Sources cross-referenced:
- **VERIFICATION.md** (`.planning/phases/04-batch-pipeline-implementation/04-VERIFICATION.md` and `.planning/phases/05-post-deploy-verification/05-VERIFICATION.md`) — the goal-backward verifier's per-REQ-ID tables
- **SUMMARY.md frontmatter** (`requirements-completed` field across 11+3 SUMMARY files)
- **REQUIREMENTS.md traceability table** (status column)

| REQ-ID | VERIFICATION | SUMMARY frontmatter | Traceability | Final Status |
|---|---|---|---|---|
| STAGE-01 | satisfied | listed (04-04) | satisfied | **satisfied** |
| STAGE-02 | satisfied | listed (04-04) | satisfied | **satisfied** |
| STAGE-03 | satisfied | listed (04-04) | satisfied | **satisfied** |
| STAGE-04 | satisfied | listed (04-04) | satisfied | **satisfied** |
| STAGE-05 | satisfied | listed (04-04) | satisfied | **satisfied** |
| STAGE-06 | satisfied (cap value lowered to 3 MB per D-03; documented) | listed (04-04) | satisfied | **satisfied** |
| STAGE-07 | satisfied | listed (04-04) | satisfied | **satisfied** |
| API-01 | satisfied | non-standard (04-03 uses `requirements:`) | satisfied | **satisfied** (frontmatter format inconsistent — tech debt) |
| API-02 | satisfied | non-standard | satisfied | **satisfied** |
| API-03 | satisfied (after CR-01 closure) | non-standard | satisfied | **satisfied** |
| API-04 | satisfied | non-standard (04-01 omits) | satisfied | **satisfied** |
| API-05 | satisfied (after CR-03 closure) | non-standard | satisfied | **satisfied** |
| API-06 | satisfied (after CR-01 + CR-02 closure) | non-standard | satisfied | **satisfied** |
| ISSUE-01 | satisfied | non-standard | satisfied | **satisfied** |
| ISSUE-02 | satisfied | non-standard | satisfied | **satisfied** |
| ISSUE-03 | satisfied | non-standard | satisfied | **satisfied** |
| ISSUE-04 | satisfied (after CR-03 closure) | non-standard | satisfied | **satisfied** |
| ACTION-01 | satisfied (documented in CLAUDE_FEEDBACK §8) | non-standard | satisfied | **satisfied** |
| ACTION-02 | satisfied (documented; live proof: PR #97 at `feedback/issue-96-batch-2` — one branch, one PR) | non-standard | satisfied | **satisfied** (live-proven via OPS-05) |
| ACTION-03 | satisfied (documented; live proof: Claude comment "Applied 2 of 2 edits" on issue #96) | non-standard | satisfied | **satisfied** (live-proven via OPS-05) |
| OPS-01 | satisfied (FEEDBACK_INJECT_VER = `'3'` after 04-11) | non-standard | satisfied | **satisfied** |
| OPS-02 | satisfied (fence diff = 0 vs pre-Phase-4 baseline `2c37581`; intact through Phase 5) | non-standard | satisfied | **satisfied** |
| OPS-03 | satisfied (CLAUDE.md:262 bullet present) | non-standard | satisfied | **satisfied** |
| **OPS-04** | **satisfied** — issue #89 closed, all 6 assertions PASS | listed (05-02) | satisfied | **satisfied** (live canary 2026-05-21T15:14Z) |
| **OPS-05** | **satisfied** — issue #96, PR #97, cache-bust proof, 15 assertions PASS | listed (05-03) | satisfied | **satisfied** (live canary 2026-05-21T15:51Z) |

**Score: 25/25 requirements satisfied.** No orphaned requirements. No orphans, no duplicates.

---

## Cross-Phase Integration (from gsd-integration-checker)

All 6 critical wiring points **WIRED** end-to-end. No regressions found in Phase 5.

| # | Wiring Point | Verdict | Evidence |
|---|---|---|---|
| A | `FEEDBACK_INJECT_VER` single-source-of-truth → 2 consumers | WIRED | `feedback-version.ts:12 = '3'`; `BaseLayout.astro:3,1025`; `feedback.astro:5,29,145`. No hardcoded version. Live proof: `const feedbackVer = "3"` in deployed HTML (OPS-05 cache-bust assertion). |
| B | `validateEdit` shared between v1 and v2 paths (D-15 mirror) | WIRED | `submit.ts:5` imports from `./validate`; `handleV1:166` + `handleV2Batch:382` both call same symbol. Live proof: OPS-04 v1 canary (issue #89) + OPS-05 v2 canary (issue #96) both produced issues with correct autonomy hints via the same validator. |
| C | `feedback.astro` parent forwarder spreads all 7 structured fields | WIRED | `feedback.astro:191-222` spreads `errors`/`cap`/`limit`/`actual`/`error`/`warning`/`commitErrors`. Inject branches at 1113/1150/1159/1172 all reachable. |
| D | Client+server `MAX_BATCH_BYTES = 3 MB` parity (D-03) | WIRED | `submit.ts:53` and `feedback-inject.js:32` both 3 MB. D-03 operator follow-up: RESOLVED — Hobby tier confirmed (see tech debt). |
| E | `CLAUDE_FEEDBACK.md §8` matches `handleV2Batch` body shape | WIRED | Title regex, single ```json``` fence, autonomy hint strings, branch/PR convention all match between server output and Action manual. Live proof: issue #96 body matches §8 spec exactly; PR #97 at `feedback/issue-96-batch-2` naming convention correct. |
| F | Smoke scenario 4 exercises real decoded-bytes cap | WIRED | `smoke-feedback-v2.mjs:307-352`: reads cap from `submit.ts` source, sends `'A'.repeat(perEditB64Len)`. Old `'AAAA'` stub gone. 5/5 scenarios pass; unit-mode regression confirmed in Phase 5 (T13). |

**OPS-02 fence verification (final — Phase 4 + Phase 5 combined):**
- Pre-Phase-4 baseline: commit `2c37581` (`feat(feedback): client annotate → auto-code → auto-deploy pipeline (#73)`)
- `git diff 2c37581 -- public/editor-inject.js public/editor public/guardrails.js src/pages/api/site middleware.ts | wc -l` → **0**
- Phase 5 commits touched only `scripts/`, `package.json`, and `.planning/` — all outside the fence
- `git diff origin/main~9 -- [fenced files] | wc -l` → **0** (confirmed during Phase 5 T14)
- **PASS — fence intact across all 14 Phase 4 + Phase 5 plans**

---

## End-to-End Flows

| Flow | Status | Notes |
|---|---|---|
| Stage → batch submit → GitHub issue → result rendered | ✅ PROVEN | Smoke harness 5/5 scenarios pass; covers v1 back-compat, v2 happy path, edit-count cap, byte cap (real cap defence post-CR-02), per-edit validation. Unit-mode regression confirmed in Phase 5. |
| Deploy → cache-bust verified end-to-end (browser net tab `?v=3`) | ✅ PROVEN | `const feedbackVer = "3"` in deployed HTML (curl grep on `https://www.moulinareves.com/`); `HTTP/2 200` on `/feedback-inject.js?v=3`. Captured in `05-CANARY-V2-EVIDENCE.md`. |
| Deploy → v1 regression canary against deployed endpoint | ✅ PROVEN | schemaVersion:1 POST to `https://www.moulinareves.com/api/feedback/submit` → issue #89 created via `handleV1` dispatch, all 6 assertions PASS. Captured in `05-CANARY-V1-EVIDENCE.md`. |
| Issue → Action → one branch / one commit / one PR / one result comment | ✅ PROVEN | Issue #96 → Action fired → PR #97 at `feedback/issue-96-batch-2` (one branch, one PR). Action posted "Applied 2 of 2 edits." then DRY_RUN gate comment. Captured in `05-CANARY-V2-EVIDENCE.md`. |

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| STAGE-01..07 | `feedback-inject.js` → `sessionStorage` → chip/panel UI | WIRED | — |
| API-01 | `feedback-inject.js submitBatch` → POST `/api/feedback/submit` `{schemaVersion:2,batch:true}` → `handleV2Batch` | WIRED | — |
| API-02 | cached browser POST `{schemaVersion:1}` → `handleV1` dispatch | WIRED | Live-proven (OPS-04) |
| API-03/04 | `handleV1` + `handleV2Batch` → `./validate.ts:validateEdit` (shared) | WIRED | — |
| API-05/06 | `handleV2Batch` → `approxDecodedBytes` cap gate + sequential photo commits + `patchIssueBody` | WIRED | — |
| ISSUE-01..04 | `handleV2Batch` → `createIssue` → GitHub issue with title/body/autonomy-hint | WIRED | Live-proven (OPS-05) |
| ACTION-01..03 | `CLAUDE_FEEDBACK.md §8` → `claude.yml` Action → one branch/commit/PR + result comment | WIRED | Live-proven (OPS-05; PR #97, comment on #96) |
| OPS-01 | `feedback-version.ts` → `BaseLayout.astro:1025` + `feedback.astro:29` → `?v=3` in HTML | WIRED | Live-proven (cache-bust HTML grep) |
| OPS-02 | `git diff [fenced paths]` = 0 | WIRED | Clean across all 14 plans |
| OPS-03 | `CLAUDE.md:262` batch bullet | WIRED (self-contained) | — |
| OPS-04 | Live canary: schemaVersion:1 POST → issue #89 → CLOSED | WIRED | Live-proven |
| OPS-05 | Live canary: schemaVersion:2 POST → issue #96 → PR #97 → result comment → cache-bust proof → cleanup | WIRED | Live-proven |

**Requirements with no cross-phase wiring:** OPS-03 (`CLAUDE.md` batch bullet) — self-contained documentation edit with no runtime cross-phase dependency. Not a missing connection; by design.

---

## Tech Debt (deferred — non-blocking)

### Status after Phase 5

1. **D-03 Vercel tier reconciliation — RESOLVED.** Phase 5 confirmed Hobby tier (3 MB cap final). Both `MAX_BATCH_BYTES` constants remain at 3 MB in `submit.ts:53` and `feedback-inject.js:32`. No operator action needed. Closed.

2. **Smoke harness retarget seam — CLOSED.** Phase 5 plan 05-01 implemented `TARGET_URL` env-var mode in `scripts/smoke-feedback-v2.mjs`. Canary mode does real `fetch()` against the deployed URL when `--canary v1|v2` is passed and `TARGET_URL` is set. Unit mode (no args) continues to pass 5/5. The seam is implemented and validated by both OPS-04 and OPS-05 live runs.

3. **WR-01 (INFO, deferred):** `MAX_IMAGE_BYTES` (12 MB per-photo) > `MAX_BATCH_BYTES` (3 MB per-batch). A single photo over 3 MB trips "batch is full" rather than a per-photo over-limit message. Low UX priority; candidate for v1.2.

4. **WR-05 (INFO, deferred):** `MAX_IMAGE_BYTES` imported in `submit.ts` but absent from the `void` ritual (lines 16–21). Cosmetic; not a runtime issue. Candidate for v1.2 hygiene pass.

5. **SUMMARY frontmatter hygiene (INFO, deferred):** 10 of 11 Phase 4 SUMMARY files use non-standard `requirements:` key or omit `requirements-completed:`. Phase 5 SUMMARY files also vary. Source of truth is VERIFICATION.md in each phase (unambiguous). Candidate for a one-time backfill or SUMMARY template update in v1.2.

---

## Verdict

**Status: `passed`**

- **Phase 4:** All 23 self-contained requirements satisfied; all 6 critical wiring points wired; OPS-02 fence intact (0 lines across 14 plans); smoke harness 5/5; gap-closure trio (04-09/10/11) closed CR-01 + CR-02 + CR-03 + OPS-01 re-bump.
- **Phase 5:** All 2 live-evidence requirements satisfied; 16/16 truths verified; OPS-02 fence intact through Phase 5 commits; unit-mode regression 5/5; D-12 honoured (no new workflows).
- **Live canary evidence:** Issue #89 (OPS-04, closed), issue #96 + PR #97 (OPS-05, both closed, branch deleted), cache-bust `const feedbackVer = "3"` in deployed HTML, `/feedback-inject.js?v=3` HEAD = HTTP 200.

The milestone is complete. All 25 requirements satisfied. Both phases passed. All 4 E2E flows proven. Recommended next: `/gsd-complete-milestone v1.1`.

---

## Re-Audit (2026-05-21)

**Re-audit date:** 2026-05-21T18:00:00Z
**Prior status:** `gaps_found` (audit 2026-05-21T11:00:00Z)
**Prior score:** 23/25 requirements, 1/2 phases, 1/2 flows

### What changed since the prior audit

The prior audit's only gaps were Phase 5's two unstarted requirements (OPS-04 and OPS-05). Phase 5 was blocked on Phase 4's PR merge and Vercel auto-deploy; the prior audit correctly identified this as a pre-condition-not-met, not an implementation defect.

Both gaps are now SATISFIED with live canary evidence:

**OPS-04 — v1 regression canary (2026-05-21T15:14Z):**
- POST `schemaVersion:1` payload to `https://www.moulinareves.com/api/feedback/submit` with `testMode:true`
- HTTP 200 returned, issue #89 created via `handleV1` dispatch arm
- Title, label (`client-feedback-test`), and body (no `batch of` substring) all correct
- Issue #89 CLOSED in finally cleanup
- All 6 assertions PASS. Evidence: `05-CANARY-V1-EVIDENCE.md`

**OPS-05 — v2 batched canary (2026-05-21T15:51Z):**
- Pre-flight: `gh variable get DRY_RUN` → `"true"` (safety gate)
- POST `schemaVersion:2, batch:true, edits:[2]` payload to deployed endpoint
- HTTP 200, issue #96 created with title `[Feedback] batch of 2 edits — /, /the-compound/`
- Issue body: single fenced JSON block, `Autonomy hint: AUTO-ELIGIBLE`
- Claude Action fired → PR #97 at `feedback/issue-96-batch-2` (one branch, one PR)
- Action result comment (from Claude): "Applied 2 of 2 edits. [...] Would auto-merge: yes."
- DRY_RUN gate comment (from `claude.yml:145`): "Dry run: this would have been auto-applied. A PR was opened for inspection; no merge was performed."
- Cache-bust proof: `curl -sL https://www.moulinareves.com/ | grep -oE 'const feedbackVer = "[0-9]+"'` → `const feedbackVer = "3"`
- Asset HEAD probe: `curl -sI https://www.moulinareves.com/feedback-inject.js?v=3` → `HTTP/2 200`
- Cleanup: PR #97 CLOSED, branch `feedback/issue-96-batch-2` deleted, issue #96 CLOSED
- All 15 assertions PASS. Evidence: `05-CANARY-V2-EVIDENCE.md`

**Tech debt resolution:**
- D-03 Vercel tier: RESOLVED (Hobby tier confirmed; 3 MB cap final)
- Smoke harness retarget seam: CLOSED (TARGET_URL mode implemented in 05-01)
- WR-01, WR-05, SUMMARY frontmatter: still INFO, still deferred to v1.2

**Regressions:** None. OPS-02 fence clean at 0 through all Phase 5 commits. Unit-mode smoke harness 5/5 regression confirmed. D-12 honoured (no new workflows).

---

_Initial audit: 2026-05-21T11:00:00Z_
_Re-audit: 2026-05-21T18:00:00Z_
_Auditor: Claude (gsd-audit-milestone orchestrator + gsd-integration-checker)_
